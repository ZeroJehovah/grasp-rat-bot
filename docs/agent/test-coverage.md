# Current Test Coverage Notes


- Latest bot self-test count: `311`.
- Latest strategy module self-test count: `89` (added in `bootstrap-0.4.269`; also checked by the bot self-test suite as of `bootstrap-0.4.275`).
- Latest combat-log analyzer self-test count: `88`.
- Latest combat replay self-test count: `1` local replay case plus skipped historical fixtures when retained logs are absent.
- Bot strategy self-tests are now maintained in `src/node/run-self-test.js`; `grasp-rat-bot.js --self-test` delegates to that extracted Node-only module while browser runtime source assembly is owned by `src/browser/runtime-source.js` and the ordered fragment registry in `src/browser/runtime-fragments-source.js`.
- Strategy module self-tests are maintained in `src/strategy/self-test.js` and cover action priority classification, action arbitration hold logic, target-switch diagnostic event/oscillation detection, attack-worth target eligibility behavior, exit-motion lock/decorated targetless-decision behavior, coin diagnostics build/de-duplication behavior, coin motion direction/pulse/metadata behavior, coin target identity/matching/incidental-pickup/snapshot-helper behavior, coin progress failure/escape/state-transition/ignored-action/cleanup helper behavior, coin route metadata/action-metadata/closer-first/held-route behavior, opportunity choice matching/stability/persistence/missing-held behavior, opportunity candidate construction behavior, opportunity pick orchestration behavior, patrol scan/safe-spacing/wait direction behavior, post-attack drop coin/wait behavior, stamina-budget summary/selector behavior, combat constants validation, opportunity constants validation, and ROI calculations. All 89 strategy module tests pass independently, and `src/node/run-self-test.js` includes a summary case so `node grasp-rat-bot.js --self-test` fails if the strategy module suite fails.
- Browser UI/runtime source is now exposed through `src/browser/runtime-source.js`, which normalizes runtime config, renders `browserRuntimeFragments(config)` with `renderRuntimeFragments(fragments)`, and no longer depends on a separate `runtime-assembly-source.js` adapter. Static verification checks the registry renderer, fragment list, runtime-bootstrap closure, status-panel runtime wrapper, combat-log runtime wrapper, control-login runtime wrapper, restored runtime state source entry, runtime utility source entries, and executable helper modules under `src/browser/runtime/`. Large browser fragments remain split across `src/browser/target-overlay-source.js`, `src/browser/target-whitelist-source.js`, `src/browser/status-panel-source.js`, `src/browser/status-panel-runtime-source.js`, `src/browser/combat-log-source.js`, `src/browser/combat-log-runtime-source.js`, `src/browser/control-login-source.js`, `src/browser/control-login-runtime-source.js`, `src/browser/important-log-source.js`, `src/browser/combat-history-source.js`, `src/browser/entity-refresh-source.js`, `src/browser/classify-source.js`, `src/browser/coin-safety-source.js`, `src/browser/target-selection-source.js`, `src/browser/combat-movement-source.js`, `src/browser/combat-aim-source.js`, `src/browser/combat-state-source.js`, `src/browser/combat-fire-source.js`, `src/browser/combat-leave-cover-source.js`, `src/browser/combat-action-source.js`, `src/browser/opportunity-stamina-source.js`, `src/browser/opportunity-snapshot-source.js`, `src/browser/post-attack-source.js`, `src/browser/opportunity-actions-source.js`, `src/browser/opportunity-route-source.js`, `src/browser/opportunity-candidate-source.js`, `src/browser/opportunity-choice-source.js`, `src/browser/opportunity-pick-source.js`, `src/browser/patrol-source.js`, `src/browser/opportunity-clear-source.js`, `src/browser/exit-motion-source.js`, `src/browser/coin-progress-runtime-source.js`, `src/browser/coin-target-runtime-source.js`, `src/browser/native-state-source.js`, `src/browser/native-control-source.js`, `src/browser/coin-motion-runtime-source.js`, `src/browser/return-block-source.js`, `src/browser/entity-activity-source.js`, `src/browser/stamina-runtime-source.js`, `src/browser/exit-relogin-source.js`, `src/browser/pending-exit-source.js`, `src/browser/leave-command-source.js`, `src/browser/auto-login-source.js`, `src/browser/leave-flow-source.js`, `src/browser/offline-safety-source.js`, `src/browser/page-native-snapshot-source.js`, `src/browser/action-arbitration-source.js`, `src/browser/network-quality-source.js`, `src/browser/network-quality-summary-source.js`, `src/browser/restored-runtime-state-source.js`, `src/browser/runtime-utils-source.js`, and `src/browser/runtime-summary-source.js`; `src/browser/page-global-core.js` provides the shared page-global adapter used by both the bundler spike and generated runtime. Static verification checks that the CLI and production bundler use the runtime-source boundary, that the legacy `src/browser/bot-source.js` and `src/browser/runtime-assembly-source.js` files are gone, that fragment modules export raw browser source and inline required shared/page-global/strategy helpers where needed, that target whitelist matching/refresh/polling, action arbitration, target-switch diagnostics, attack/combat/kill history bookkeeping, entity refresh/activity memory, entity/coin/combat classification, coin safety/diagnostics/pickers, target selection/opportunistic-shot helpers, combat movement/pressure/dodge helpers, combat aim/opponent-profile/trade-estimate helpers, combat state/trend/tick-gap helpers, combat shooting/precision/intercept aim helpers, combat leave-cover helpers, combat action construction/logging helpers, opportunity stamina/budget helpers, opportunity snapshot/AFK-stamina scoring helpers, post-attack drop source helpers, opportunity action source helpers, opportunity route source helpers, opportunity candidate source helpers, opportunity choice source helpers, opportunity pick source helpers, patrol source helpers, opportunity clear source helpers, exit motion source helpers, restored runtime state helpers, runtime utility helper injection/runtime module reuse, status panel runtime helper binding, combat-log runtime helper binding, control-login runtime helper binding, coin progress runtime source helpers, coin target/pickup bookkeeping, native movement/shot control, coin motion wrapper/core inlining, return-block helper/scanning source generation, entity activity helper source generation, stamina runtime helper source generation, exit/relogin hold helper source generation, pending-exit and pursuit-tracking helper source generation, leave command completion/issue and Clash rescue helper source generation, auto/manual login source generation, leave flow source generation, offline safety assessment source generation, network quality sampling/ACK tracking/summary generation, control-login bot identity, pause globals, manual-login bypass globals, `startLinuxDoLogin` guards, page-native snapshot observer state/constructors, and Clash rescue hook reads are present in the expected source modules/generated runtime, and that the final generated remote runtime still has no unresolved runtime `require()` calls.
- `src/browser/runtime-bootstrap-source.js` now owns runtime bootstrap source generation, and static verification checks its source-factory shape, injection point, page-global adapter injection, runtime config page-global read, previous-bot page-global read, preserved-state/defaults/target-whitelist imports, runtime defaults inlining, target whitelist helper inlining, and generated runtime helper presence.
- `src/browser/bot-object-source.js` now owns the runtime `bot` object and status/control API source, and static verification checks its source-factory shape, injection point, pending-exit state wiring, combat/important log state, post-login zoom state, `stop()`, `setPaused()`, `status()`, network-quality summary reporting, and pending-exit status summaries.
- `src/browser/persistent-last-self-source.js` now owns persistent last-self source generation, and static verification checks the inline/bundled source-factory shape, injection point, read helper, write helper, `LAST_SELF_STATE_KEY` storage anchor, `src/browser/runtime/persistent-last-self.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/persistent-exit-source.js` now owns persistent exit source generation, and static verification checks the inline/bundled source-factory shape, injection point, read helper, write helper, `refreshExitDetail` anchor, `src/browser/runtime/persistent-exit.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/persistent-clear-source.js` now owns persistent clear source generation, and static verification checks the inline/bundled source-factory shape, injection point, exit clear helper, pending-exit clear helper, `PENDING_EXIT_STATE_KEY` anchor, `src/browser/runtime/persistent-clear.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/pending-exit-persistence-source.js` now owns pending-exit persistence source generation, and static verification checks the inline/bundled source-factory shape, injection point, reload normalizer, storage normalizer, reader, writer, initial-state chooser, `PENDING_EXIT_STATE_KEY` anchor, `src/browser/runtime/pending-exit-persistence.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/refresh-exit-detail-source.js` now owns exit-detail refresh source generation, and static verification checks the inline/bundled source-factory shape, injection point, refresh helper, hold remaining-time update, stamina-budget and stamina-exhausted summary branches, display finalization anchor, `src/browser/runtime/refresh-exit-detail.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/restored-coin-failures-source.js` now owns restored coin-failure source generation, and static verification checks the inline/bundled source-factory shape, injection point, restore helper, preserved failure reads, severe ignore restoration, hard ignore restoration, `src/browser/runtime/restored-coin-failures.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/restored-runtime-state-source.js` now owns restored runtime-state source generation, and static verification checks the inline/bundled source-factory shape, config-aware fragment entry, restored failure/enemy/offline/pending-exit state wiring, cold-load reload marker preservation, `src/browser/runtime/restored-runtime-state.js` adapter, bundler spike execution, final bundled helper body, and durable pending-exit restore checks across both pre-bundle and final dist text shapes.
- `src/browser/login-snapshot-gate-source.js` now owns login snapshot gate source generation, and static verification checks the inline/bundled source-factory shape, injection point, required-count helper, state normalizer, last-sample timestamp, reset reason anchors, `src/browser/runtime/login-snapshot-gate.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/runtime-diagnostics-source.js` now owns runtime diagnostics source generation, and static verification checks the inline/bundled source-factory shape, config-aware fragment entry, diagnostics object initialization, value merging, swallowed-error behavior, `src/browser/runtime/runtime-diagnostics.js` adapter, bundler spike execution, and final bundled helper body.
- `src/browser/exit-relogin-source.js` now owns exit/relogin source generation with bundled display-helper direct core binding, actor/repeat-delay, enemy-leave-streak read/write/update, summary/action-construction, hold/suppress base-helper, suppress reason wrapper cleanup, start-exit-audit wrapper cleanup, suppress writer helper, suppress writer wrapper cleanup, actor wrapper cleanup, login-suppress clear wrapper cleanup, HP relogin-delay wrapper cleanup, offline display-reason wrapper cleanup, injury summary wrapper cleanup, pursuit summary wrapper cleanup, offline summary wrapper cleanup, combat summary wrapper cleanup, enemy-leave-streak wrapper cleanup, enemy suppress fixed-reason wrapper cleanup, stamina hold selector wrapper cleanup, stamina budget hold wrapper cleanup, offline unsafe predicate wrapper cleanup, offline suppress wrapper cleanup, pending stamina suppress wrapper cleanup, pending unsafe suppress wrapper cleanup, clear relogin hold wrapper cleanup, hold read/clear binding helper, and hold cleanup binding helper slices, and static verification checks the inline/bundled source-factory shape, config-aware fragment entry, remaining-hold wait display preference across wrapper/core text shapes, bundled wait-display wrapper removal, summary/displayReason mutation behavior, enemy actor normalization/fallback ordering, repeat-delay config handling, enemy streak localStorage read/write/reset expiry, normalized `bot.enemyLeaveStreak` state, detail repeat metadata, combat/pursuit/injury/offline summary formatting, combat leave action cover metadata, HP relogin delay repeat-minimum handling, bundled HP relogin delay wrapper removal and direct core helper injection, bundled offline display-reason wrapper removal and direct core use from tick/combat-state, bundled injury summary wrapper removal and direct core use from tick/leave-flow, bundled pursuit summary wrapper removal and direct core use from tick/leave-flow, bundled offline summary wrapper removal and direct core use from tick/leave-flow/refresh-exit-detail, bundled combat summary wrapper removal and direct core use from leave-flow/combatLeaveAction binding, suppress-reason matching, direct bundled suppress-reason core binding, runtime-bound suppress writer, suppress writer reuse/zero-delay/new-hold side effects, direct pending-exit fixed-reason enemy suppress writer bound-core calls with runtime-bound streak updates, direct bundled pending-exit offline suppress bound-core calls with storage/writer-helper binding, direct bundled pending-exit suppress cleanup and 403 recovery cleanup through `clearLoginSuppressMatchingBoundCore()`, direct bundled bot status enemy streak reads through `readEnemyLeaveStreakBoundCore()`, direct bundled leave-flow unsafe offline predicate core calls, direct bundled leave-flow pending stamina suppress, pending unsafe suppress, and start-exit-audit bound-core calls, runtime-bound exit-trigger audit reset/event recording, pending unsafe suppress reason selection and login suppress priming through the runtime-bound unsafe-minimum/pending-reason selector, bundled removal of the obsolete unsafe-minimum, pending-reason, suppress-writer, actor, login-suppress clear, enemy-leave-streak, pending-stamina suppress, pending-unsafe suppress, offline suppress, start-exit-audit, and clear relogin hold wrappers, stamina hold selection through runtime-bound budget/reset composition and bundled removal of obsolete stamina hold/budget wrappers, offline zero-hold and stamina-hold suppress persistence/delegation through the bound suppress writer, pending stamina login suppress priming through the runtime-bound budget/reset selector, persistent enemy/offline hold remaining-time restore and suppress storage cleanup through runtime-bound key/clock adapters, enemy/offline recovery cleanup through tick/hold-read runtime-bound storage/key adapters, pending-exit scope preservation, unsafe offline relogin-delay predicates, `src/browser/runtime/exit-relogin.js` adapter exports, bundler spike execution, and final bundled helper body across wrapper/core text shapes.
- Production remote generation now goes through the shared esbuild wrapper in `scripts/remote-bot-bundle.js`. The retained module spike is covered by `npm run test:bundler-spike` and still proves ordinary shared/strategy imports plus the shared page-global core in a browser IIFE. The full generated-runtime candidate remains covered by `npm run test:remote-bundled` and uses the same shared bundler in non-production mode. Static objective verification regenerates the bundled production output, checks manifest `production` / bundler / direct-source hash metadata, syntax-checks the bundled dist with `vm.Script`, rejects unresolved relative imports/requires and CommonJS exports in the final dist, and keeps source/runtime behavior checks anchored to the canonical direct generated source.
- Recent important coverage:
  - leave-success exit confirmation now has bot self-tests for refreshing before confirmation, marking restored pending-exit state as reloaded, retrying the original pending exit when refreshed state is still online, and confirming only when refreshed state is offline. Static verification requires durable pending-exit storage, durable ordinary pending combat-log storage on reload/failure paths, no ordinary combat-log `localStorage` persistence inside the per-frame queue hot path, startup restore, no direct plain `leave-success` confirmation, no normal leave-success session-end important log before confirmation, and clear/update paths for persisted pending exits;
  - static verification requires `leaveCommandTimeoutMs = 3000` for all asynchronous `leave()` calls while keeping the pending-exit retry floor at `leaveRetryMinMs = 10000`;
  - static verification requires leave HTTP 403 to enter optional Clash leave rescue before the 403 risk-control fallback: 403 is no longer excluded from rescue, each exit round starts by switching the game group to `S2-自动`, stale 403 request history is cleared before retrying through the ordered `S2-自动 -> DIRECT -> S2-手动` stages, pending-exit retries reset the stage order back to `S2-自动`, stage attempts are persisted on the pending exit, and pending-exit 403 confirmation is gated behind rescue scheduling/exhaustion;
  - session-mismatch recovery now has bot self-tests for explicit live-session takeover bypass state, the rule that takeover bypass still requires learned login-point safety, the one-time controlled refresh before same-user takeover, fresh snapshot-self live evidence, and post-exit blockers including pending exit, login suppress, exit-reset snapshot gate, reconnect churn, and offline-ish WebSocket state;
  - persistent login-point safety now has bot self-test coverage for preserving `lastLoginAt` across hot updates, explicit entry summary text, pre-login-only gate behavior after entry, dynamic safety radius selection (`>=80` last-exit HP uses `17000cm`; low/unknown uses `30000cm`), and the bootstrap fallback rule that blocks unattended relogin only after a learned point or explicit bot missing-point state exists. Static verification requires localStorage login-point coordinates to win over empty/stale memory, newer persisted last-exit HP context to win over stale memory, missing login points to block the safety gate explicitly, OAuth/callback/session timestamp inference to feed login-point recording, persisted reset/last-exit HP context, reset paths to clear only the safety streak without clearing the persisted point, dynamic radius helpers, exit trigger/confirmation passing self HP into the gate reset, no post-entry `login point safety gate` leave path, login-point danger limited to same-day damage actors and evidence-gated Active mode while clearing stale player danger on snapshot errors, damage-actor persistence requiring incoming-bullet ownership/firing/engaged-combat evidence, automatic bootstrap/native login interceptors plus the `startLinuxDoLogin` guard, and explicit manual login bypass markers that keep the sidebar login button clickable;
  - static verification now requires the old standalone `/snapshot` connectivity streak to stay disabled for automatic relogin: `snapshotLoginGateStatus()` must satisfy on learned login-point safety alone, `summarizeReloginGateStatus()` must not require a separate snapshot streak, and Tampermonkey/extension relogin panels must not render the removed `快照接口连通性` row;
  - legacy combat/recent-combat global sampling outage helpers still have bot self-tests for explicit offline summary text, combat-triggered offline leave gating, the default non-combat guard, and top-level `exit` summary fallback for `control-global-sampling-outage` frames without `leave` detail, even though active bot-owned `/snapshot` and `/minimap` polling is now disabled;
  - combat/recent-combat tick/frame gaps now have bot self-tests for explicit offline summary text, combat-triggered offline leave gating, non-combat guard behavior, the guard that stale recent combat-frame context alone does not exit during a coin route, the `tick-reentry-gap` diagnosis when a new tick arrives while the previous tick is still stuck, the `combat-log-gap-with-active-tick` diagnosis when ticks continue but combat frames stop with live combat context, and top-level `exit` summary fallback for `control-combat-tick-gap` frames without `leave` detail;
  - local snapshot coin at `185m` cannot beat visible/native coin;
  - local snapshot-only coin is not chased;
  - nearby native coin with stale snapshot metadata still reports visible coin reason;
  - far snapshot clusters beyond local authority are ignored by ordinary coin pickup;
  - nearby known coin fields beat farther snapshot fields by ROI because snapshot-only fields are no longer pickup destinations;
  - low-value far snapshot coin waits for visible coin refresh both before and after the old delayed fallback threshold;
  - low 1h stamina exits instead of waiting when the nearest safe visible coin is unaffordable;
  - low 1h stamina exits before the old delayed snapshot branch could run;
  - 1h budget below a foot coin exits instead of bypassing long-window budget;
  - low 1d stamina goes toward the nearest safe visible/native coin instead of waiting for better ROI, while snapshot-only final coins are ignored;
  - low long-window stamina target-only budget blocks still wait, but report `wait-for-visible-coin-refresh` when no visible/native coin is present;
  - stale daily stamina holds are contradicted by preserved session stamina evidence, while zero remaining 1d evidence does not clear the hold;
  - daily-only 1d stamina exhaustion summaries render as `一天体力到达限制，退出等待重连` instead of falling back to stale combat or exit detail;
  - static verification requires Tampermonkey/extension panel reason scoping to current decisions only, so active combat fire-discipline reasons such as `combat-stamina-hold` cannot fall through to stale stamina/offline exit summaries, and empty current reasons hide the `原因` row instead of filling it from historical exits;
  - 150m coin beats richer 200m coin by nearby opportunity priority;
  - same-value 150m coin beats a sticky older 213m coin target;
  - similar same-tier ROI targets choose immediately instead of waiting;
  - held similar ROI targets keep the current target to avoid jitter;
  - missing held 54m coin target prevents a same-value 151m visible coin from taking over during source flicker;
  - nearby Drop=3 AFK target beats far single coin by stamina ROI;
  - Drop=3 AFK target in attack range beats 400m visible coin;
  - out-of-range AFK Drop targets cool down after observed 5s stamina consumption, while newly visible AFK-like targets remain eligible and in-range AFK targets ignore that out-of-range stamina cooldown;
  - recently active idle targets are not treated as AFK profit during the 12s recent-activity cooldown;
  - low target motion shrinks combat aim jitter;
  - moving combat targets use intercept aim, and the intercept solver leads lateral targets with render-delay compensation;
  - CDP validation on 2026-06-17 showed direct float `vel` components such as `0.6 0.8` and `0.9239 0.3827` do not move the player, while `2 1` collapses to the same 45° diagonal as `1 1`; movement remains 8-direction quantized and does not accept continuous float vectors;
  - real incoming bullets use an 8-direction threat-field dodge, including multi-bullet threat count logging and a regression check that the selected legal direction improves CPA safety over a worse fixed diagonal;
  - short-window opponent motion profiling identifies repeated lateral flips and feeds aim confidence;
  - low-confidence distant moving combat targets throttle fire to the configured point-fire cadence;
  - high-HP static HP-gap and losing exchange-rate samples first record a combat disadvantage observation, then trigger `combat-hp-disadvantage-leave` after the configured confirmation window;
  - zero-damage `tradeEstimate` windows no longer trigger a high-HP exit by themselves when projected death is still far away, but they still trigger once the predicted death horizon is near;
  - precise incoming signed-lane direction overrides a stale opposite strafe lock;
  - pressure-close diagonal strafe keeps a target-closing axis while preserving dodge movement;
  - close target jitter and long no-damage aim widening stay capped at `0.14rad`;
  - short 5s stamina exhaustion stops combat movement while keeping shooting enabled;
  - very close combat still backs away, but 50m mid-range no longer backs away by default;
  - long no-damage combat target triggers `combat-pressure-close`;
  - combat aim uses live/native/render precision only and no longer exposes snapshot authority fields;
  - real incoming bullet pressure can switch moving-target aim to live/native precision before intercept lead, while lateral live targets under real-bullet pressure or server stall can stay on live intercept instead of collapsing to live precision; replay self-tests include 2026-06-17 xmsthc (`0 -> 28` estimated hits) and Motor (`1 -> 25`) samples plus 2026-06-18 Noah_Z (`0 -> 5` dynamic, `7` pure live-intercept), lockcc (`1 -> 8` dynamic), BeingS (`0 -> 7` dynamic/live-intercept in the old high-HP exit window), and long tyshine (`14 -> 36` dynamic, `38` pure live-intercept);
  - visible/native AFK Drop targets and visible native coins are covered without reintroducing richer snapshot-only targets or delayed snapshot pickup;
  - native visible coin routes beat isolated single coins when route-level stamina ROI is better, while weaker routes still lose to higher-yield visible AFK Drop targets;
  - visible coin route metadata, including per-coin route points for overlay drawing, is exposed on actions; same-first-coin routes keep that overlay metadata even when the first coin's single-coin ROI is higher than the whole route score; held routes keep their first coin through near-tie replans and still switch when a competing route score is clearly better; near realtime coins remain the first route target when a route continues toward a farther field, routes cannot skip a much closer safe local coin as their first target, route legs through invulnerable danger are rejected, and whole routes are skipped when the 1h/1d stamina budget cannot afford the full route;
  - snapshot-only coins remain ignored for ordinary pickup when no visible/realtime profit exists, so the fallback decision is `wait-for-visible-coin-refresh`;
  - visible/native ordinary 1-coin selection near a native/realtime visible non-idle invulnerable player is blocked before the later avoidance-flee branch, even when that player is not in the Active threat set; a snapshot-only invulnerable player does not block visible coin pickup. Idle invulnerable Active players with full 5s stamina, no movement, no firing, and no recent activity do not trigger proactive avoidance and can still allow ordinary coin pickup. Invulnerability alias self-tests cover positive millisecond aliases such as `invulnerableRemainingMs` even when an earlier tick alias is zero;
  - ordinary visible/native coins near non-invulnerable realtime Active players remain selectable; self-tests cover both 1-coin and high-value visible coins taking the normal opportunity path near such players;
  - held visible/native coin targets now have bot self-test coverage for both source-gap jitter protection and immediate release when current visible coin sources no longer contain the held target. Static verification requires the `visible-coin-disappeared` clear path and the regression self-test;
  - held high-value visible/native coins now have bot self-test coverage for resisting AFK Drop target switches during the opportunity hold window;
  - held nearby single visible coin targets now have bot self-test coverage for blocking a farther coin-route first target during the opportunity hold window, while still allowing a same-first route upgrade to preserve route metadata. Static verification requires the held single-coin route helper/guard and both self-tests;
  - final action arbitration has bot self-test coverage for keeping recent safety over profit, keeping recent combat over recovery, and refusing to keep profit over a new combat action. As of `bootstrap-0.4.275`, runtime and self-tests both use `src/strategy/action-priority.js` and `src/strategy/action-arbitration.js` as the authoritative implementation for action focus summaries and final-action hold rules. Static verification requires the runtime arbitration helper, priority-band rank helper, execution before target-switch diagnostics, status exposure, hot-update preserved state, default hold window, high-value visible coin return-block bypass, and these self-tests;
  - target-switch diagnostics now have strategy module self-test coverage for recording target-switch events, rounding previous-decision score/stamina summaries, and detecting reversed pair oscillation. As of `bootstrap-0.4.276`, runtime target-switch diagnostics use `src/strategy/action-switch-diagnostics.js` as the authoritative implementation while preserving the browser wrapper, status exposure, standalone combat-log `target-switch` entries, and final-action-before-diagnostics execution order;
  - coin diagnostics now have strategy module self-test coverage for building near-coin summaries, ignored/snapshot-only near lists, count fields, and de-duplicating filtered entries while keeping the shortest observed distance. As of `bootstrap-0.4.277`, runtime coin diagnostic summary construction uses `src/strategy/coin-diagnostics.js` while the main runtime wrapper still owns config/state access, stamina-affordability checks, and threat-specific diagnostics;
  - coin motion now has strategy module self-test coverage for axis approach, near-stuck single-axis pickup, lock release thresholds, pickup pulse tiers, failure slowdown, exact-coordinate stop, stale-lock clearing, and motion metadata. As of `bootstrap-0.4.286`, runtime coin motion direction, approach-lock update intent, pickup pulse timing, and metadata construction use `src/strategy/coin-motion.js` while the main runtime wrapper still owns `bot.coinApproachLock`, coin failure/progress counters, config access, and action construction;
  - coin target identity now has strategy module self-test coverage for key generation, ID/radius matching, tracked target reconstruction from the last decision/target/progress state, native coin snapshot normalization/filtering, point-to-segment distance, incidental pickup candidate detection, and snapshot worth/reason helpers. As of `bootstrap-0.4.289`, runtime coin target identity, matching, native snapshot normalization, incidental pickup detection, and snapshot helper logic use `src/strategy/coin-target.js` while the main runtime wrapper still owns native coin source access, normalization, config access, collection side effects, session accounting, snapshot memory writes, and the runtime snapshot-only predicate;
  - coin progress helpers now have strategy module self-test coverage for ignore count increment/capping, decay, reason-specific ignore bases, stale-coin escape direction away from the target, fallback phase selection, intent/expiry checks, attempt improvement metadata, close-stale detection, progress initialization/improvement/stale transitions, stuck-ignore record/action metadata, no-progress ignore action metadata staying age-free, and ignored-coin cleanup intent. As of `bootstrap-0.4.293`, runtime coin failure ignore/backoff, stale coin escape direction construction, coin-progress intent checks, attempt expiry/update, progress record updates, ignored progress records, ignored patrol action construction, and ignored cleanup intent use `src/strategy/coin-progress.js` while the main runtime wrapper still owns `bot` state writes, `clearOpportunityChoiceFor()`, config access, and escape direction state writes;
  - coin route planning now has strategy module self-test coverage for route metadata construction, closer-first route guarding, and held route stabilization. As of `bootstrap-0.4.278`, runtime coin route planning uses `src/strategy/coin-route.js` for the core route builder/picker while the main runtime wrapper still owns config/state access, visible coin filtering, stamina affordability, threat blocking, diagnostic recording, and held opportunity choice lookup;
  - coin route action metadata now has strategy module self-test coverage for route metadata rounding/defaults, point preservation, fallback first distance, and null-route behavior. As of `bootstrap-0.4.294`, `buildCoinAction()` uses `src/strategy/coin-route.js` for route metadata construction while the main runtime wrapper still owns action construction, movement direction, stamina cost, score, and config-dependent action kind;
  - opportunity choice stability now has strategy module self-test coverage for key parsing, coordinate-based same-coin matching, switch-margin hold, high-value coin blocking an enemy switch, and oscillation locking. As of `bootstrap-0.4.279`, runtime opportunity choice stability uses `src/strategy/opportunity-choice.js` while the main runtime wrapper still owns `bot.opportunityChoice`, `bot.opportunitySwitchLock`, config access, and action construction;
  - opportunity candidate construction now has strategy module self-test coverage for same-id coin de-duplication, route metadata preservation, route display merge, enemy action-kind descriptors, and route score comparison. As of `bootstrap-0.4.280`, runtime opportunity candidate construction uses `src/strategy/opportunity-candidates.js` while the main runtime wrapper still owns route picking, runtime callbacks, action builders, missing-held cleanup, and persisted opportunity choice state;
  - opportunity choice persistence now has strategy module self-test coverage for persisted choice/action metadata and missing-held route metadata. As of `bootstrap-0.4.281`, runtime opportunity choice persistence uses `rememberOpportunityChoiceCore()` from `src/strategy/opportunity-choice.js` while the main runtime wrapper still owns the `bot.opportunityChoice` state write;
  - missing-held opportunity construction now has strategy module self-test coverage for candidate reconstruction, visible-missing clear requests, and snapshot held-choice preservation. As of `bootstrap-0.4.282`, runtime missing-held opportunity construction uses `buildMissingHeldOpportunityCore()` from `src/strategy/opportunity-choice.js` while the main runtime wrapper still owns visible source lookup, stale visible coin cleanup, diagnostics, and action closures;
  - post-attack drop wait target selection now has strategy module self-test coverage for visible coin coverage checks, resolved wait target selection, and covered/threat-blocked skips. As of `bootstrap-0.4.283`, runtime post-attack drop wait selection uses `pickPostAttackDropWaitTargetCore()` from `src/strategy/post-attack-drop.js` while the main runtime wrapper still owns attack-history resolution state, config access, threat callbacks, and action construction;
  - post-attack drop coin matching now has strategy module self-test coverage for matched visible drops, amount/score/radius filtering, and amount-first selection. As of `bootstrap-0.4.284`, runtime post-attack drop coin selection uses `pickPostAttackDropCoinCore()` from `src/strategy/post-attack-drop.js` while the main runtime wrapper still owns safe coin filtering, stamina diagnostics, attack-history resolution mutation, kill-reward attribution, config access, and action construction;
  - stamina-budget summaries and selectors now have strategy module self-test coverage for daily budget limiting, blocked opportunity summary selection, nearest coin exit summaries, and daily-final visible coin selection. As of `bootstrap-0.4.285`, runtime stamina-budget summary/selector logic uses `src/strategy/stamina-budget.js` while the main runtime wrapper still owns measured stamina budgets, safe coin filtering, distance/stamina callbacks, relogin delay config, and leave/action construction;
  - static verification covers coin candidate diagnostics for ignored/threat-blocked/stamina-blocked/snapshot-only nearby coins, standalone `coin-diagnostics` misc logging, distinct `best-opportunity-coin-route` vs `best-opportunity-visible-coin` Chinese labels, loader panel current-time rendering, merged visible-invulnerable coin threat filtering, and the related self-tests;
  - already engaged out-of-range combat targets hold position instead of chasing until `combatDisengageRange`;
  - recently damaged low-HP out-of-range finish targets within 160m reengage without firing out of range;
  - targets beyond `combatDisengageRange` clear combat state without installing a retreat-ignore cooldown, so they can re-enter if they later come back inside `combatAttackRange`;
  - retreating edge-range combat targets suppress fire with `target-retreating-edge`;
  - low-HP retreating edge targets with high self HP use `combat-finish-pressure`, keeping the ordinary retreating-edge suppression for healthy targets and preserving out-of-range hold outside `combatAttackRange`;
  - retreating edge targets that are still applying real target-owned bullet pressure can use the guarded far no-damage `retreating-fighter-close` path without disabling ordinary pure-escape suppression;
  - retreat-ignored targets are not reselected without incoming bullets, while real incoming bullets can still re-engage the shooter for defense;
  - retreat-ignored Active threats inside `combatAttackRange` produce `combat-active-threat-wait` instead of allowing foot-coin pickup in the same danger window;
  - recovering state keeps already engaged stationary, active, and non-retreating targets inside `combatDisengageRange` under combat priority;
  - full-HP and non-full states both keep non-idle invulnerable targets under safety flee behavior, including non-Active invulnerable nearby targets;
  - real incoming bullet shooter overrides an existing engaged target;
  - synthetic firing pressure does not override an existing engaged target;
  - emergency close combat spacing can override real-bullet dodge when too close, and combat leave cover uses spacing escape while exit is pending;
  - low-HP close-risk combat exits before waiting for nominal HP disadvantage when under 50 HP and only within 5 HP of the target at close range;
  - short 5s stamina exhaustion suppresses combat leave movement cover while preserving shoot intent;
  - hostile combat/critical HP exit summaries include actionable context;
  - moving/non-idle invulnerable active players block same-direction coin routes while allowing coins in the opposite direction;
  - pursuit leave thresholds shorten for non-full HP and non-idle invulnerable chasers;
  - repeated same-enemy leave/relogin defensive backoff is disabled, leaving anti-camping to login-point safety;
  - offline reconnect churn summary reports an explicit `WebSocket 反复重连` reason, and sampling outage summary reports explicit network sampling timeout wording;
  - local combat-log collector write smoke test passed for JSONL append;
  - combat-log analyzer reports missing top-level `exit`, reason-specific required-delay issues, and login-during-hold issues from collected JSONL;
  - combat-log monitor mode smoke test passed with `npm run monitor -- --watch-count 1 --latest 2`;
  - fresh objective monitor script smoke test passed by confirming `npm run monitor:objective:fresh -- --watch-count 1 --watch-interval-ms 250` exits non-zero with expected no-current-log evidence issues when no fresh `.105` logs are present;
  - objective observer script smoke test passed by confirming `npm run monitor:objective:observe -- --watch-count 1 --watch-interval-ms 250` exits zero while still printing expected missing `.109` evidence issues;
  - objective status script smoke tests passed: `node scripts/objective-status.js --self-test` covers missing-evidence, safe-only, exit-only, and synthetic complete fixtures, `node scripts/objective-status.js` exits zero and reports granular requirement status, and `node scripts/objective-status.js --fail-on-incomplete` exits non-zero while fresh evidence is missing;
  - combat-log version filter smoke test passed with zero matching entries/issues for `--min-version bootstrap-0.4.104` on current historical logs;
  - manifest-based `analyze:current` / `monitor:current -- --watch-count 1` smoke tests passed and currently match zero `bootstrap-0.4.104` local log entries; current manifest target is now `bootstrap-0.4.109`;
  - `validate:current` and `monitor:current:strict -- --watch-count 1` currently fail as expected with `no-matching-entries` because no `bootstrap-0.4.109` live logs are present yet;
  - `validate:objective` and `monitor:objective -- --watch-count 1` currently fail as expected with `no-matching-entries`, `no-matching-exit-events`, `no-active-in-range-combat-events`, and `no-hp-disadvantage-exit-events` because no `bootstrap-0.4.109` live logs or required objective evidence samples are present yet;
  - `monitor:objective:observe -- --watch-count 1 --watch-interval-ms 250` currently exits zero with the same missing-evidence report because it is intended for observation, not proof-of-completion gating;
  - combat-log analyzer `npm test` covers temporary JSONL fixtures for missing top-level `exit`, default zero unsafe-delay auditing, and manifest exact-version filtering;
  - combat-log analyzer `npm test` covers `missing-exit-reason` for top-level `exit` objects that omit explicit `exit.reason`;
  - combat-log analyzer `npm test` covers `generic-exit-reason` for top-level `exit.reason` values like `cooldown` that do not explain the real exit root cause;
  - combat-log analyzer `npm test` covers `safeReloginAllowed` safe offline zero-delay exits as safe relogin evidence rather than unsafe-delay issues;
  - combat-log analyzer `npm test` covers `--require-entries` success when current entries exist and `no-matching-entries` failure when they do not;
  - combat-log analyzer `npm test` covers `--fail-on-audit-issue` semantics: evidence-only gaps do not count as audit failures, while missing/generic exit reasons and behavior regressions do;
  - combat-log analyzer `npm test` covers `--require-exit-events` success when current exit events exist and `no-matching-exit-events` failure when current logs contain no exit sample;
  - combat-log analyzer `npm test` covers Active-in-range combat-response evidence counts, `--require-active-combat-events`, and `no-active-in-range-combat-events`;
  - combat-log analyzer `npm test` covers Active-in-range coin-action regression detection using only actionable realtime evidence, so `native`/`render`/`realtime` Active targets still fail when coin actions override them, while snapshot-only Active context does not create a false combat regression;
  - combat-log analyzer `npm test` covers `mode=Active` / `active:false` stationary/full-stamina entities as Active-in-range evidence for coin-action regression and combat-response evidence;
  - combat-log analyzer `npm test` covers Active-in-range combat-response evidence from current `decision.target` fields even when `nearbyEntities` is absent, while coin targets do not false-positive as Active players;
  - combat-log analyzer `npm test` covers HP-disadvantage combat-exit evidence counts, `--require-hp-disadvantage-exit-events`, and `no-hp-disadvantage-exit-events`;
  - combat-log analyzer `npm test` covers login suppress/enemy-hold context extraction on exit events;
  - combat-log analyzer `npm test` covers combat-end events preserving top-level `exit` and relogin delay instead of becoming missing-exit false positives;
  - combat-log analyzer `npm test` covers `login-attempt-during-exit-hold` from both current decision login and prior `lastLogin.ignoredSuppressMs`, plus `manual-login-cleared-exit-hold`, without treating those relogin events as missing-exit false positives;
  - combat-log analyzer `npm test` covers top-level `suspended:login-suppressed` and `suspended:manual-login` combat-end reasons as relogin/session-end exit events and reason-count evidence without missing-exit false positives;
  - daily-summary self-test filters non-combat safety avoidance closures such as legacy `recovery-avoid-humans` out of active-combat statistics;
  - daily-summary self-test folds post-combat recovery waits and generic post-combat timeouts into `敌方逃离` instead of exposing `战后恢复` or `交火停止` as combat results;
  - daily-summary self-test covers mutually exclusive top-level active-combat outcomes for HP disadvantage, low HP, critical HP, enemy flee, target switch, and relogin-wait exit closures;
  - combat-log analyzer `npm test` covers `ambiguous-opportunity-wait` and `coin-action-with-active-player-in-range` behavior-regression detection;
  - combat-log analyzer self-tests cover the current Active-near-coin policy exceptions: low-Drop non-threat Active players, high-value visible coin priority, attack-range boundary Active players with no threat evidence, and continued detection for real high-Drop moving Active blockers;
  - combat-log analyzer `npm test` covers exit/behavior reason-count summaries;
  - combat-log analyzer `npm test` covers exit safety delay summaries for safe exits, unsafe exits under the default zero minimum, and reason-specific required delays;
  - combat-log analyzer `npm test` covers reason-specific required-delay auditing for `stamina-budget-coin-leave`, including `exit-delay-below-required`; missing reason-specific delay evidence is now counted under `requiredDelayMissing` instead of being treated as below required;
  - combat-log analyzer `npm test` covers manifest source-hash match, missing-hash, and mismatch evidence issues;
  - daily-summary self-test covers explicit `pickedCoins=0` placeholder sessions not falling back to raw `coinsGained`, preventing fake `1000币` refresh/total收益 rows in the daily report;
  - daily-summary self-test covers the `实际战斗收益统计` section: it renders as a Markdown table, keeps confirmed picked kills, keeps chat-confirmed unpicked kills as `0币（未拾取）`, and keeps HP-disadvantage exits as failed/zero-profit battle rows while excluding enemy-flee/target-switch outcomes;
  - daily-summary self-test covers over-budget picked kill rewards: if drop-match/chat evidence exceeds normalized session collected coins, the extra reward is demoted to confirmed-but-unpicked Drop and is not revived in actual battle profit rows;
  - daily-summary self-test covers death-count lifecycle reporting: current-day and cross-day `self.death_count` increments add a `生命周期统计` section, split the login / actual battle profit / active-combat tables by lifecycle, and render death loss from the last pre-death Drop and Loss preview fields;
  - daily-summary self-test covers inferred closeout sessions contributing to login totals for duration, stamina, refreshed coins, active-player kill count, and reward while keeping completed/inferred/incomplete counts separate;
  - daily-summary self-test covers inferred closeouts rendering attached concrete combat/offline exit evidence instead of generic next-login closeout text, and non-win active-combat summaries being reconciled to `胜利：击杀确认` when the same session has a confirmed kill;
  - daily-summary self-test covers actual 1d remaining/limit stamina delta overriding undercounted per-login stamina totals;
  - daily-summary self-test covers default daily report output under `docs/reports/YYYY-MM/daily-YYYY-MM-DD.md`, and coin-balance self-test covers default monthly output under `docs/reports/YYYY-MM/monthly-YYYY-MM.md`;
  - daily-summary command-line generation now shares the chunked JSONL reader for current-day entries and previous-day death-count lookup, covering large retained daily logs that exceed Node's single-string limit;
  - combat replay self-tests still cover historical 2026-06-14/15/16 combat windows for analyzer continuity when those local detailed logs are present, including the old authority-divergence and stale-snapshot cases. After detailed-log retention cleanup removes old ignored JSONL, the replay self-test reports those local samples under `skipped` instead of failing. The replay loader streams selected JSONL ranges instead of reading entire large log files into one string, and replay reports now include hypothetical finish-pressure shots for old low-HP retreating-edge windows. Current live combat behavior is covered by bot/static checks that require native/realtime/render aim only and no snapshot-authority fields.
  - local combat-log collector sourceHash propagation smoke test passed for payload-level sourceHash on session events;
  - combat-log analyzer now streams JSONL in chunks, so `node scripts/objective-status.js` can audit very large historical log directories without hitting Node's single-string size limit;
  - static objective build verifier now checks current manifest/source hash alignment, key objective constants, runtime ambiguous-wait removal, bootstrap layout CSS, sourceHash wiring, bootstrap version consistency, compact panel dot controls, panel GitHub repository link placement, network-quality latency/loss header pill and no-sample tooltips, removed visible section/status titles, tooltip-only metric labels, raw remaining/limit stamina formatting, panel `today(current login)` metric formatting, no plus sign on coin profit, `Log` label rendering, dynamic latency/loss pill width and two-decimal loss text, post-exit last-self/stamina fallback, relogin/no-self safety waits keeping inline manual login visible, A-side panel reason scoping to the current decision only, current stamina-exhaustion reason priority in A-side panels, no persisted/older exit summaries in panel `原因` fallback, post-login zoom-out scheduling flow, join-mode Active non-AFK combat handling, evidence-gated Active-mode login-point blockers, AFK recent-activity cooldown gating, idle-invulnerable non-avoidance and moving-invulnerable avoidance, held high-value coin protection from AFK Drop switches, final action arbitration, target-switch diagnostics/status/misc logging, logged combat target mode/safety fields, combat session-boundary sourceHash logging, combat tick/frame gap offline gating and `runtime` diagnostics, login-suppressed/manual-login combat-log suspension, combat-log bounded batch/persist caps with no queue-time ordinary `localStorage` persistence, pending-exit top-level exit fallback, specific exit reason preservation during leave cooldown, `leaveOffline` cooldown current-summary priority, leave HTTP 403 Clash rescue before risk-control fallback, zero defensive relogin delay handling, zero-hold offline exit detail preservation, longest reason-specific exit suppress delay selection, important local/remote summary logs including combat HP start backfill from the first valid sample, daily 1d stamina delta total correction, explicit daily combat result labels, safety-avoidance filtering, and enemy-flee folding for target loss, safe offline immediate relogin behavior, stale enemy relogin hold clearing after online recovery, combat-only sampling outage offline gating, persistent login-point merge/missing-point behavior, login-point dynamic radius and last-exit HP persistence, logged-out login-point safety range overlay rendering, login-point safety as a hard gate even for live-session takeover bypass, damage-actor persistence requiring incoming-bullet ownership/firing/engaged-combat evidence, non-invulnerable active combat priority over avoidance, low-value Active combat gating, high-value visible coin priority, daily-stamina final visible-coin behavior, opponent-probe combat defaults/reason preservation, combat out-of-range hold/disengage hysteresis, retreating-edge fire suppression plus low-HP finish-pressure exception, safe close movement override evidence under real bullet pressure, real-bullet live precision aim logging, render-visible realtime combat source merging, snapshot-safe combat-log entity merging, active-threat wait before coin pickup, live/native/render aim-only combat logging, native visible coin route scoring/safety coverage, visible profit-before-snapshot ordering, direct native WebSocket movement/shooting dispatch and repeat constants, default direct movement local key/prediction sync, server-marker probe default-off status exposure, reduced status logging, split combat-log collector layout, shared-source module imports with single-file generated runtime inlining, extracted Node self-test delegation, extracted browser runtime-source boundary, extracted page-global core and adapter-based bot installation, extracted browser source builder delegation, production esbuild remote build/direct-source verification, retained bundler spike coverage, extracted browser UI/combat-log/important-log/control-login/native-state/runtime-summary source factories with helper inlining, remote username-only target whitelist polling/failure preservation including zero-width/bidi control stripping, the standalone `dist/target-whitelist.json`, the session-mismatch recovery snapshot-gate restriction and controlled-refresh prerequisite, and the stationary full-stamina Active coin-override self-test.
  - static objective build verifier also checks the current compact network quality display: one header `latency/loss` pill after `BOT` / `WS` / `Log`, `??ms/??.??%` unknown placeholders, independent latency/loss colors, a white slash, dynamic width, two-decimal loss text, and no visible `延迟` / `丢包` labels on the clock row.
  - static objective build verifier also checks the post-login one-way zoom-out fit and the `postLoginZoomFitTolerance = 0.05` default, so the login fit pass finishes once the visible range fits and cannot zoom back in at the threshold.
  - static objective build verifier checks the action-settlement stall offline gate defaults, main tick integration, exposed status, canonical wait/leave reasons, and offline summary text so a WS-open-but-actions-not-settling state is routed through the same safe/unsafe offline leave timing as websocket offline.
  - static objective build verifier checks Tampermonkey/extension panel reason filtering for relogin/no-self waits: wait-only gate text is filtered from `原因`, concrete exit summaries are preferred over `displayReason`, and the metadata/runtime bootstrap versions match.
  - static objective build verifier checks daily and monthly Markdown reports default to the unified `docs/reports/YYYY-MM/` directory, while daily Markdown can still be printed explicitly with `--stdout`.
  - bot self-tests cover preserving the outer specific top-level combat-log `exit.reason` when leave detail reason is generic `cooldown`;
  - bot self-tests cover top-level combat-log `exit.safeReloginAllowed` / `exit.offlineSafety` for safe offline immediate relogin evidence;
  - bot self-tests cover the session-mismatch recovery login gate staying subject to the learned login-point snapshot safety check instead of bypassing it;
  - bot self-tests cover top-level combat-log `exit` summaries for non-exit decisions, pending-exit decisions, canonical combat leave reason preservation, pending unsafe suppress fields, and confirmed longer relogin holds;
  - bot self-tests cover top-level combat-log `exit` fallback to decision-level hold/relogin fields for confirmed wait frames with `leave: null`;
  - bot self-tests cover shared runtime/display helper behavior for circular-safe JSON stringification, bigint cloning, combat-log ID sanitizing, distance/duration formatting, actor labels, and HP display;
  - bot self-tests cover target whitelist username normalization stripping invisible format controls, including a firing `Firefox\u200e` target matching the plain `Firefox` whitelist entry and parser deduplication against the plain spelling;
  - movement axis handoff/brake cases remain covered;
  - coin pickup pulse length now decreases by near-distance band and repeated pickup failures;
  - low-Drop in-range Active targets at `Drop <= 4` are ignored for proactive combat without incoming-fire evidence, but enter combat before low-value coins when they own an incoming bullet;
  - proactive Active combat waits for enough long-cycle stamina to cover the configured `100000ms` kill budget, while incoming-bullet evidence still allows defensive combat under that budget;
  - high-value visible coins override active combat, recovery wait, incoming-bullet pressure, close non-invulnerable combat, and non-idle invulnerable avoidance when HP is healthy, while low-HP pickups still require no incoming bullet/current combat/nearby non-invulnerable real threat;
  - stationary full-stamina join-mode Active targets are not treated as AFK and beat coin pickup;
  - Active combat HP gap/low HP disadvantage exits fire before taking a bullet;
  - combat spacing and measured-dodge aim spread remain covered;
  - far no-damage active duels now cover the new 100m+ close-in branch, including the bad-HP-gap guard, the rule that low-stamina far pressure saves reserve for movement/dodge instead of relaxing fire cadence, and the retreating-edge guard that prevents far no-damage close-in from chasing an enemy that is already leaving.
  - passive coin-runner combat is covered by bot self-tests that verify close-in movement, visible live intercept aim, the 45m close band taking precedence over normal preferred spacing, the new 2500ms activation confirm window, and the lockout that disables passive-runner reuse after target-owned real bullet evidence. Replay self-tests cover the two 2026-06-20 mango windows and the 2026-06-22 high-cost mango window; the new 2026-06-22 replay improves passive-runner estimated hits from logged `78/664` to `131/660` with `4451cm` simulated approach.
  - opponent-probe opening fire has bot self-test coverage for preserving the high 5s dodge reserve, using 520ms point-fire while reserve is full, and ending once target-owned real-bullet history is present. Replay self-test coverage includes the 2026-06-26 11:05 biliee window, where the probe keeps `5/14` opening shots, skips `9`, saves about `4500ms` 5s stamina, and projects first-pressure stamina from `2442ms` to `6942ms`.
  - slightly out-of-range engaged targets now have bot self-test coverage for the pressured `combat-out-of-range-reengage` branch, the target-owned pressure HP-gap override, the non-pressure base HP-gap guard, the retreat-only hold guard, safe-close bias under recoverable target-owned pressure, and the losing-pressure pure-dodge guard. Replay self-test coverage includes the 2026-06-20 `蕉灼の仓鼠` window where the reengage scenario improves the logged `4/38` dynamic baseline to `44/45` while stepping from `14565cm` back inside range without firing out of range first, plus the 2026-06-22 biliee pressure sample where target-owned pressure reengage creates `23` estimated hits with `5675cm` simulated approach instead of taking the 79HP early stop-loss. The 2026-06-30 `colloq168` replay validates the safe-close movement gate in a max-range pressure fight: far no-damage close dynamic improves from `9/70` baseline hits to `15/70`, with `9315cm` simulated approach while unsafe straight-on out-of-range pressure remains pure dodge.
  - low-threat finishing pressure now has bot self-test coverage for keeping burst fire while bullet risk is absent, starting at low target HP, and staying bounded above that HP band. Replay self-test coverage includes the 2026-06-21 `mango` 09:13 window improving estimated hits from logged `178/456` to `334/774` with `318` extra simulated shots, plus the 06:43 high-stamina kill improving from `95/486` to `164/612` with `126` extra simulated shots.
  - target-owned real-bullet pressure fire has bot self-test coverage for the mid-HP pressure window, and sustained pressure no-damage stop-loss has bot self-test coverage for waiting above the 70HP stop-loss gate and exiting a losing target-owned pressure trade after 10s without target damage once self HP reaches that later gate. Replay coverage for 2026-06-21 pressure samples shows 07:56 biliee improving from logged `3/56` to `11/72`, 09:21 biliee from `5/112` to `38/192`, ahy6a from `9/76` to `13/90`, and the 10:15 biliee stop-loss sample exiting about 13s before the logged end at `67/79` after `11596ms` no-damage.
- Static objective build verifier checks that manual pause removes the target overlay and that overlay rendering suppresses paused decisions.
- Static objective build verifier checks that `src/browser/offline-safety-source.js` owns offline threat summaries, offline safety assessment, and active-combat wait threat source generation, and that those helpers are present in the generated runtime.
- Static objective build verifier checks that `src/browser/leave-command-source.js` owns the wait-timeout helper, leave command result/failure summary helpers, Clash rescue scheduling/retry helpers, pending-exit result update/confirmation helpers, and leave request completion/issue helpers, and that those helpers are present in the generated runtime.
- Static objective build verifier checks that `src/browser/auto-login-source.js` owns `maybeStartAutoLogin()` and `forceLoginNow()`, including page-global `startLinuxDoLogin` reads, `pageGlobal` call binding, and live-session takeover bypass handling, and that those functions are present in the generated runtime.
- Static objective build verifier checks that `src/browser/leave-flow-source.js` owns `leaveOffline()`, `leaveForInjury()`, `leaveForPursuit()`, `leaveForCombat()`, and `leaveDuringEnemyHold()`, including pending-exit duplicate leave guards, inline offline unsafe predicate, pending-unsafe suppress, and start-exit-audit calls, bundled direct runtime-core predicate, bound-core pending-unsafe suppress, and bound-core start-exit-audit calls, and generated runtime presence.
- Static objective build verifier checks that `src/browser/runtime-diagnostics-source.js` owns `recordRuntimeDiagnostics()`, including guarded `bot.runtimeDiagnostics` initialization, diagnostic value merging, and swallowed diagnostics write errors.
- Static objective build verifier checks that `src/browser/tick-safety-source.js` owns `recordUnhandledTickError()`, `runTickSafely()`, and `runCallbackSafely()`, including bounded error recording, console logging, runtime diagnostics recording, and async callback error capture.
- Static objective build verifier checks that production remote builds can hand `attack-worth` and `exit-motion` helper bindings to esbuild through `src/browser/runtime/attack-worth.js` and `src/browser/runtime/exit-motion.js`, while local injection/print-source keeps the direct inline `attackWorthTaking()` and exit-motion helper paths. Final dist still exposes target whitelist blocking, AFK profit target handling, reward-ratio gating, exit-motion stop-lock calculation, and targetless post-exit decision rewriting through strategy cores and browser wrappers.
- Static objective build verifier checks that `src/browser/runtime/array-count.js` owns the executable `arrayCount()` helper, that `src/browser/array-count-source.js` inlines that helper into the generated runtime with stable indentation, and that the bundler spike imports and executes the helper through the browser module path.
- Static objective build verifier checks that `src/browser/runtime/runtime-utils.js` owns the executable runtime utility helper exports through the shared implementation, that `src/browser/runtime-utils-source.js` inlines those helpers into the generated runtime through the browser runtime module path, and that the bundler spike imports and executes runtime utils through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/display-format.js` owns the executable display helper exports through the shared implementation, that `src/browser/status-panel-runtime-source.js` binds those helpers into the status-panel source factory through the browser runtime module path, and that the bundler spike imports and executes display formatting through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/target-whitelist.js` owns the executable target whitelist helper exports through the shared implementation, that `src/browser/runtime-bootstrap-source.js` inlines those helpers into the generated runtime bootstrap through the browser runtime module path, and that the bundler spike imports and executes target whitelist parsing through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/exit-summary.js` owns the executable exit summary helper exports through the shared implementation, that runtime bootstrap, combat-log runtime, and control-login runtime source factories import those helpers through the browser runtime module path, and that the bundler spike imports and executes offline leave summary formatting through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/browser-preserved-state.js` owns the executable preserved-state helper export through the shared implementation, that `src/browser/runtime-bootstrap-source.js` inlines it into the generated runtime bootstrap through the browser runtime module path, and that the bundler spike imports and executes preserved-state cloning through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/runtime-defaults.js` owns the executable runtime-defaults helper export through the shared implementation, that `src/browser/runtime-bootstrap-source.js` inlines it into the generated runtime bootstrap through the browser runtime module path, and that the bundler spike imports and executes runtime default construction through that browser module path. As of `bootstrap-0.4.375`, the current browser runtime source factories no longer import shared helpers directly via `../shared/*`; the remaining shared helper ownership is reached through explicit `src/browser/runtime/` modules.
- Static objective build verifier checks that `src/browser/runtime/action-priority.js` owns the executable action-priority helper exports through the strategy implementation, that `src/browser/action-arbitration-source.js` inlines action-priority helpers through the browser runtime module path, and that the bundler spike imports and executes action focus summaries through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/action-switch-diagnostics.js` owns the executable action-switch diagnostics helper exports through the strategy implementation, that `src/browser/action-arbitration-source.js` inlines target-switch diagnostics through the browser runtime module path, and that the bundler spike imports and executes action-switch diagnostics through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/action-arbitration.js` owns the executable final-action arbitration helper exports through the strategy implementation, that `src/browser/action-arbitration-source.js` inlines final-action arbitration through the browser runtime module path, and that the bundler spike imports and executes final-action arbitration through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/coin-diagnostics.js` owns the executable coin diagnostics helper exports through the strategy implementation, that `src/browser/coin-safety-source.js` inlines coin diagnostics through the browser runtime module path, and that the bundler spike imports and executes coin diagnostics through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/coin-motion.js` owns the executable coin motion helper exports through the strategy implementation, that `src/browser/coin-motion-runtime-source.js` inlines coin motion through the browser runtime module path, and that the bundler spike imports and executes coin direction and motion metadata through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/coin-target.js` owns the executable coin target helper exports through the strategy implementation, that `src/browser/coin-target-runtime-source.js` inlines coin target helpers through the browser runtime module path, and that the bundler spike imports and executes coin target key, snapshot, and matcher helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/coin-progress.js` owns the executable coin progress helper exports through the strategy implementation, that `src/browser/coin-progress-runtime-source.js` inlines coin progress helpers through the browser runtime module path, and that the bundler spike imports and executes coin progress failure, attempt, and intent helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/coin-route.js` owns the executable coin route helper exports through the strategy implementation, that `src/browser/opportunity-route-source.js` inlines coin route helpers through the browser runtime module path, and that the bundler spike imports and executes coin route key and action metadata helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/opportunity-choice.js` owns the executable opportunity choice helper exports through the strategy implementation, that `src/browser/opportunity-choice-source.js` inlines opportunity choice helpers through the browser runtime module path, and that the bundler spike imports and executes stable choice and choice persistence helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/opportunity-candidates.js` owns the executable opportunity candidate helper exports through the strategy implementation, that `src/browser/opportunity-candidate-source.js` inlines opportunity candidate helpers through the browser runtime module path, and that the bundler spike imports and executes candidate combination and best coin score helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/post-attack-drop.js` owns the executable post-attack drop helper exports through the strategy implementation, that `src/browser/post-attack-source.js` inlines post-attack drop helpers through the browser runtime module path, and that the bundler spike imports and executes visible-coin and drop-picker helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/stamina-budget.js` owns the executable stamina budget helper exports through the strategy implementation, that `src/browser/opportunity-stamina-source.js` inlines stamina budget helpers through the browser runtime module path, and that the bundler spike imports and executes daily-limit and nearest-coin budget helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime/opportunity-constants.js` owns the executable opportunity constants exports through the strategy implementation, that `src/browser/runtime-bootstrap-source.js` injects opportunity constants through the browser runtime module path, and that the bundler spike imports and executes opportunity constants helpers through that browser module path.
- Static objective build verifier checks that `src/browser/runtime-fragments-source.js` returns validated named runtime fragment objects through `runtimeFragment()`, and that `src/browser/runtime-source.js` now renders only named fragment objects instead of accepting bare string/function fragments.
- Static objective build verifier checks that `src/browser/runtime-fragments-source.js` uses explicit fragment names for key runtime fragments such as `runtime-bootstrap`, `choose-action`, and `startup`, that the old inferred `runtimeFragmentName()` path stays removed, and that explicit entries are materialized through `materializeRuntimeFragments()`.
- Static objective build verifier checks that remote browser runtime source enables bundled-runtime mode, that the `array-count` fragment is config-aware, and that production remote builds can hand that fragment to esbuild through `src/browser/runtime/array-count.js` while local injection/print-source keeps the direct inline helper path.
- Static objective build verifier checks that production remote builds can hand `safeStringify`, `safeJsonClone`, and `sanitizeCombatLogIdPart` to esbuild through `src/browser/runtime/runtime-utils.js`, while local injection/print-source keeps the direct inline helper path.
- Static objective build verifier checks that production remote builds can hand `runtime-bootstrap` shared helper bindings to esbuild through browser runtime modules for preserved state, runtime defaults, target whitelist, and exit-summary helpers, while local injection/print-source keeps the direct inline helper path. The verifier also accepts esbuild numeric literal forms such as `5e4` when proving config defaults in final dist.
- Static objective build verifier checks that production remote builds can hand `status-panel-runtime` display-format bindings to esbuild through `src/browser/runtime/display-format.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes meter-formatting, escaping, duration, actor label, and HP display helpers.
- Static objective build verifier checks that production remote builds can hand `combat-log-runtime` exit-summary binding to esbuild through `src/browser/runtime/exit-summary.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes combat-log exit summary construction.
- Static objective build verifier checks that production remote builds can hand `control-login-runtime` stamina-exhaustion label binding to esbuild through `src/browser/runtime/exit-summary.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes relogin stamina label formatting.
- Static objective build verifier checks that bundled combat-action leave construction imports `combatLeaveActionCore()` / `combatExitSummaryCore()` directly through `src/browser/runtime/exit-relogin.js`, keeps the inline/local `combatLeaveAction()` path for CDP/print-source, and removes `function combatLeaveAction(` / `function combatExitSummary(` declarations from final dist.
- Static objective build verifier checks that production remote builds can hand `coin-motion-runtime` helper bindings to esbuild through `src/browser/runtime/coin-motion.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes coin direction, pickup pulse, approach-lock, and motion metadata helpers.
- Static objective build verifier checks that production remote builds can hand `coin-target-runtime` helper bindings to esbuild through `src/browser/runtime/coin-target.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes coin target identity, matching, native snapshot, incidental pickup, and snapshot navigation helpers.
- Static objective build verifier checks that production remote builds can hand `coin-progress-runtime` helper bindings to esbuild through `src/browser/runtime/coin-progress.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes coin failure, stale-escape, progress tracking, ignored-coin action, and cleanup helpers.
- Static objective build verifier checks that production remote builds can hand `coin-safety` diagnostics helper bindings to esbuild through `src/browser/runtime/coin-diagnostics.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes coin summary, filter diagnostic, and diagnostics-object helpers.
- Static objective build verifier checks that production remote builds can hand `opportunity-stamina` stamina-budget helper bindings to esbuild through `src/browser/runtime/stamina-budget.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes daily budget limiting, blocked opportunity summaries, nearest-coin stamina exits, and daily final coin selection.
- Static objective build verifier checks that production remote builds can hand `post-attack` drop helper bindings to esbuild through `src/browser/runtime/post-attack-drop.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes visible-drop detection, resolved attack filtering, drop-coin candidate metadata, post-attack coin selection, and wait-target selection.
- Static objective build verifier checks that production remote builds can hand `opportunity-route` coin-route helper bindings to esbuild through `src/browser/runtime/coin-route.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes route picker, route action metadata, and route builder helpers.
- Static objective build verifier checks that production remote builds can hand `opportunity-candidate` helper bindings to esbuild through `src/browser/runtime/opportunity-candidates.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes coin/enemy candidate construction, combined opportunity construction, and best-coin scoring helpers.
- Static objective build verifier checks that production remote builds can hand `opportunity-choice` helper bindings to esbuild through `src/browser/runtime/opportunity-choice.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes stable choice selection, missing-held opportunity restoration, oscillation locking, and opportunity choice persistence helpers.
- Static objective build verifier checks that production remote builds can hand `action-arbitration` priority, target-switch diagnostics, and final-action arbitration helper bindings to esbuild through `src/browser/runtime/action-priority.js`, `src/browser/runtime/action-switch-diagnostics.js`, and `src/browser/runtime/action-arbitration.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes target-switch snapshots, band ranking, final action arbitration, and hysteresis reasons.
- Static objective build verifier checks that production remote builds can hand `opportunity-clear` helper binding to esbuild through `src/browser/runtime/opportunity-clear.js`, while local injection/print-source keeps the direct inline helper path and final dist still exposes opportunity choice clearing through the strategy core.
- Static objective build verifier checks that production remote builds can hand `opportunity-pick` helper binding to esbuild through `src/browser/runtime/opportunity-pick.js`, while local injection/print-source keeps the direct inline picker path and final dist still exposes best-opportunity orchestration through the strategy core.
- Static objective build verifier checks that production remote builds can hand `patrol` direction helper binding to esbuild through `src/browser/runtime/patrol.js`, while local injection/print-source keeps the direct inline patrol path and final dist still exposes scan-coin, safe-spacing, wait, and patrol-heading side-effect behavior through the strategy core and wrapper.
- Static objective build verifier checks that `src/browser/choose-action-source.js` owns `chooseAction(self)`, including coin diagnostics construction, high-value visible coin priority, post-attack drop coin handling, stamina-budget exits, best-opportunity selection, and the visible-coin wait fallback.
- Static objective build verifier checks that `src/browser/tick-source.js` owns `tick(source = 'timer')`, including pending-exit handling, login/no-self handling, offline leave handling, action selection, coin progress tracking, final action arbitration, important combat tick logging, and combat-log tick logging.
- Static objective build verifier checks that `src/browser/startup-source.js` owns startup install/restore flow, including persisted exit-audit restore, combat-log pending restore, important-log restore, native login gate installation, page-global bot installation, previous-bot shutdown, page-native observer installation, target whitelist polling, startup tick, and timer tick safety.
- Static objective build verifier checks that `src/browser/entity-refresh-source.js` owns `markRecentMovement()` and `refreshGlobalState()`, including passive active-API-disabled diagnostics, and that those functions are present in the generated runtime.
- `node scripts/verify-objective-build.js` now includes source/dist checks that movement and shooting prefer direct sends over the existing native page WebSocket, that movement repeats use the configured 50ms/220ms cadence, that default nonzero direct movement keeps local page key/prediction sync for combat safety, that optional server-marker probe mode is off by default, that stop cleanup cancels/repeats direct control safely, that shared source modules are still inlined into the generated single-file runtime without runtime `require()` calls, that `src/browser/page-native-snapshot-source.js` owns passive snapshot observer source generation, that `src/browser/target-whitelist-source.js` owns target whitelist matching/refresh/polling source generation, that `src/browser/action-arbitration-source.js` owns action priority, target-switch diagnostics, and final-action arbitration source generation, that `src/browser/combat-history-source.js` owns attack history, combat engagement, kill history, and drop-matched kill attribution source generation, that `src/browser/entity-refresh-source.js` owns entity refresh/activity memory source generation, that `src/browser/classify-source.js` owns entity/coin/combat classification source generation, that `src/browser/coin-safety-source.js` owns coin safety/diagnostics/picker source generation, that `src/browser/target-selection-source.js` owns high-value coin, combat target, engaged target, and opportunistic-shot source generation, that `src/browser/combat-movement-source.js` owns combat movement, bullet-pressure, spacing, and dodge action source generation, that `src/browser/combat-aim-source.js` owns aim jitter, motion sampling, opponent profiling, and trade-estimate source generation, that `src/browser/combat-state-source.js` owns combat target identity, disadvantage/trend state, sampling outage, combat tick-gap, and native tick interval source generation, that `src/browser/combat-fire-source.js` owns combat shooting discipline, precision/live/intercept aim target, and dynamic aim strategy source generation, that `src/browser/combat-leave-cover-source.js` owns combat leave-cover action source generation, that `src/browser/combat-action-source.js` owns combat action construction and combat-state metadata source generation, that `src/browser/opportunity-stamina-source.js` owns opportunity stamina/budget and daily stamina final-run source generation, that `src/browser/opportunity-snapshot-source.js` owns snapshot coin destination and AFK stamina cooldown scoring source generation, that `src/browser/opportunity-candidate-source.js` owns opportunity candidate source generation, route source injection, candidate core inlining, visible route coin wrapper, best coin score wrapper, and profitable combat target comparison source generation, that `src/browser/opportunity-choice-source.js` owns opportunity choice source generation, stable picker core inlining, persistence core inlining, missing-held core inlining, options wrapper, and missing-held wrapper source generation, that `src/browser/opportunity-pick-source.js` owns best opportunity picker source generation, route opportunity selection, and candidate core call wiring, that `src/browser/patrol-source.js` owns patrol direction source generation, distant-coin scan patrol, and safe-spacing patrol branches, that `src/browser/opportunity-clear-source.js` owns opportunity choice clearing source generation, choice clearing, and switch-lock reset anchors, that `src/browser/exit-motion-source.js` owns exit motion stop lock, post-exit decision rewrite, target cleanup, and overlay cleanup anchors, that `src/browser/coin-progress-runtime-source.js` owns coin progress strategy core inlining, runtime options, failure ignore state writes, stale-coin escape state writes, ignored-coin cleanup, and `trackCoinProgress()` source generation, that `src/browser/coin-target-runtime-source.js` owns coin target identity, native coin snapshot, incidental pickup, tracked pickup, and pickup session accounting source generation, that `src/browser/native-control-source.js` owns direct/native movement dispatch, motion stop, shooting, and shot attempt source generation, that `src/browser/coin-motion-runtime-source.js` owns coin motion core inlining, pickup failure/slow-attempt wrappers, approach-lock updates, and coin motion metadata source generation, that `src/browser/return-block-source.js` owns return-block flee/scan/action source generation, that `src/browser/entity-activity-source.js` owns entity clock/distance/activity/stamina/AFK classification helper source generation, that `src/browser/stamina-runtime-source.js` owns HP/stamina summary/reset/deferred-leave helper source generation, that `src/browser/network-quality-source.js` owns network quality sampling/ACK tracking source generation, that `src/browser/network-quality-summary-source.js` owns network quality status summary source generation, that `src/browser/auto-login-source.js` owns automatic/manual login source generation, that `src/browser/leave-flow-source.js` owns leave flow source generation, that control-login post-login zoom bot checks, pause global reads, manual-login bypass markers, `startLinuxDoLogin` gate installation, page-native snapshot observer globals, and Clash rescue hook reads use `src/browser/page-global-core.js`, that session-mismatch recovery can bypass the login snapshot gate only through explicit live-session takeover state guarded by no-exit/no-disconnect context checks with fresh snapshot-self evidence allowed as live-session evidence, that explicit manual login bypasses snapshot/login-point gates while automatic relogin still honors them, and that unsafe alive-self entry after a relogin/exit context rechecks persisted login-point safety before any profit/combat action.
