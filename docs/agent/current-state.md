# Current State

Update this file for every remote bot release or handoff-relevant version change. At minimum, keep the latest remote bot, manifest SHA-256, latest release commit, release note, and live-validation target current.

- Latest remote bot: `bootstrap-0.4.243`.
- Latest manifest SHA-256: `49549fb11531012acaed564e71580b4993257f607908def69af0012323d1c6c9`.
- Latest release commit: `pending` (`bootstrap-0.4.243` panel profit stats and exit status persistence), previous release commit `8da748b` (`bootstrap-0.4.242` remove standalone snapshot connectivity relogin gate).
- Latest bootstrap A versions: Tampermonkey `0.4.60`, extension `0.1.39`.
- Tampermonkey `0.4.60` and extension `0.1.39` change the embedded panel profit row to show `today(current login)` pairs for login time, stamina spent, coin profit, and kill count, remove the visible `+` prefix from coin profit, reserve stable widths for the header latency/loss dots (`1234ms` and `00.00%` scale), render the log toggle as `Log`, and keep the last exit detail plus last known 1d stamina visible after an exit/page refresh by preserving expired exit summaries and persisting the last self summary.
- `bootstrap-0.4.243` adds `status().todaySession` and `status().lastSelf` for panel reporting. Today totals are summed from current-day important session records plus the active session without double-counting the current important session id, and the last self summary is persisted to `localStorage.graspRatLastSelfState` so the bootstrap can still render daily remaining stamina after a refresh while logged out. Safe/offline zero-hold exits now preserve the latest exit detail with hold fields cleared instead of deleting the persistent summary. Live validation should confirm that after an exit and refresh the panel still shows the previous exit reason, the 1d remaining stamina line, `Log`, loss with two decimals, and profit metrics in `a(b)` format with no coin `+`.
- Tampermonkey `0.4.59` and extension `0.1.38` remove the standalone `快照接口连通性` relogin row from the embedded panel. Relogin-related panel state now shows only cooldown and `登录点安全`; `/snapshot` errors still appear on the login-point safety row because they reset the 12-sample safe streak.
- `bootstrap-0.4.242` removes the independent 3-success `/snapshot` connectivity condition from automatic relogin. `loginSnapshotGate` remains as the wrapper/status field and still probes `/snapshot` to advance learned login-point safety, but the gate is satisfied solely by `loginPointSafety.satisfied`: 12 consecutive safe `/snapshot` samples at the learned login coordinate. A `/snapshot` error or unsafe sample still resets login-point safety to `0/12`, so connectivity failures remain blocking through the 12-sample gate rather than through a separate `3/3` row. Live validation should confirm that once `登录点安全 12/12` is reached, automatic relogin is not additionally held by any `/snapshot` connectivity streak, and the panel no longer renders `快照接口连通性`.
- Tampermonkey `0.4.58` and extension `0.1.37` shorten the network-quality panel dots to numeric-only visible labels (`123ms`, `0.0%`) so the header row fits beside `BOT`/`WS`/`日志`; the `延迟`/`丢包` meanings and source/evidence details remain in hover tooltips.
- `bootstrap-0.4.241` stabilizes ordinary profit target selection. AFK Drop eligibility now suppresses targets for `afkRecentActivityCooldownMs = 12000` after observed movement, firing, or 5s stamina consumption, so a player who recently acted and then briefly stands still is not immediately treated as AFK profit. Opportunity hysteresis now keeps a held visible/native high-value coin (`amount >= 10`) from being stolen by an AFK Drop target during the hold window; defensive combat, safety, disappearing coins, and stamina unaffordability still override the coin. Decision metadata marks this path with `opportunityChoice.highValueCoinHold`. Live validation should confirm recent-action idle players are skipped as AFK Drop targets, and a selected `10+` visible/native coin no longer flips back and forth with a high-Drop AFK target unless safety or target availability changes.
- `bootstrap-0.4.240` adds runtime network-quality observation without changing combat/profit decisions. The bot now estimates effective latency from recent movement action confirmation with a native WebSocket state-frame fallback, estimates packet-loss-like state-frame gaps over a 30s rolling window, tracks movement and attack feedback counters, exposes the summary as `status().networkQuality`, and embeds it in combat-log `runtime.networkQuality`. Severe latency/loss/stall changes are throttled into `misc` logs with `type: "network-quality"`. The Tampermonkey and extension panels show compact numeric latency/loss dots in the same header row as `BOT`/`WS`/`日志`; tooltips explain the source and current frame/loss evidence. Live validation should confirm the panel displays plausible `123ms / 0.0%` values after native WebSocket frames arrive, combat logs contain `runtime.networkQuality`, and no strategy behavior changes solely because these metrics are observed.
- `bootstrap-0.4.239` fixes the ordinary low-value coin priority mismatch seen in the latest combat-log review. Ordinary visible/native coin safety now keeps the previous invulnerable-player blockers but also merges non-whitelisted, non-invulnerable realtime/native Active players with combat/activity evidence into the coin-threat list, so a `1` coin whose route ends inside that Active player's coin danger band is skipped instead of stealing control from combat/safety flow. The high-value shortcut remains separate: `10+` visible/native coins still use the narrower invulnerable-only threat list and now explicitly treat an ordinary Active coin-threat block as a reason to enter `high-value-visible-coin-priority`, so healthy/safe high-value pickups can still outrank combat. Self-tests cover the same geometry for `amount=1` blocking and `amount=10` high-value bypass; static verification checks the split threat lists. Live validation should confirm that low-value coins near realtime Active players show as threat-blocked/fall through to wait or combat, while healthy `10+` visible/native coins can still be selected with `high-value-visible-coin-priority`.
- `bootstrap-0.4.238` restores manual login priority after the screenshot where the side panel replaced the manual login entry with a disabled `等待安全` button. Tampermonkey `0.4.56` and extension `0.1.35` keep the inline side-panel button visible as `立即登录` even when login-point safety or snapshot connectivity is not satisfied; blocked safety evidence is retained as bypass diagnostics instead of disabling the manual control. Manual login now sets a short native-login bypass marker, can use the raw page `startLinuxDoLogin` saved by older remote scripts, and `maybeStartGameLogin(..., force/manual)` no longer returns early just because the bot is paused/stopped. The remote bot's `forceLoginNow()` also bypasses snapshot/login-point gates, pause state, login suppress, and cooldown while still recording audit/log bypass details. Automatic relogin remains gated by snapshot connectivity and login-point safety. Live validation should confirm that after manually pausing/stopping the bot, the side button still says `立即登录`, clicking it starts the page login/join flow, and genuine automatic relogin still waits for the safety rows.
- `bootstrap-0.4.237` raises `globalRefreshTimeoutMs` from `1500` to `3000` because live screenshots showed `/snapshot` returning HTTP 200 in roughly `1.53-2.34s` while the bot panel still recorded `/snapshot timed out after 1500ms`. The request status was healthy, but the script-side timeout fired just before the response resolved, keeping both the 3-sample `/snapshot` connectivity gate and 12-sample learned login-point safety gate at `0`. The new timeout remains below the 5s global refresh cadence, so ordinary refreshes should not overlap, but it leaves enough room for current endpoint latency. Live validation should confirm that screenshots with 1.5-2.3s successful `/snapshot` requests no longer show `snapshot timed out after 1500ms`, and the `/snapshot` connectivity and login-point safety streaks advance when the login point is otherwise safe.
- `bootstrap-0.4.236` removes the old defensive time-based relogin waits for combat disadvantage, injury, pursuit, reconnect churn, server-position stall, unsafe offline exits, and repeated same-enemy exits. These exits still reset the login snapshot gate and learned login-point safety evidence, so relogin remains blocked until the 3 successful `/snapshot` connectivity samples and 12 safe login-point samples pass. The stamina-budget and long-window stamina holds are unchanged, and leave HTTP 403 still keeps its one-hour risk-control fallback. Log analysis now defaults `--min-unsafe-delay-ms` to `0`, while reason-specific required-delay auditing still enforces the 30 minute `stamina-budget-coin-leave` hold. Live validation should confirm that after a combat/injury/pursuit/offline danger exit the panel shows no defensive cooldown row beyond ordinary login/snapshot/login-point gates, while stamina-budget exits still show the 30 minute wait.
- `bootstrap-0.4.235` fixes the repeated "picking coins, Active player enters range, page freezes, then exits as network offline and dangerous" samples by removing ordinary combat-log persistence from the per-frame hot path. The 2026-06-27 local samples around 01:41, 01:54, and 02:04 CST showed no `/snapshot`/`/minimap` sampling outage; instead `runtime.lastCombatLogRecordMs` jumped to `4025-5164ms` while `lastCombatLogBuildMs` stayed around `5-10ms`, matching a script-side combat-log record stall. Ordinary queued combat frames now only mark pending persistence dirty instead of rewriting `localStorage.graspRatCombatLogPendingEntries` on every queue operation. Forced persistence still runs before leave-success/session-mismatch/page reloads, failed flush persistence is throttled by `combatLogPendingPersistMinMs = 5000`, persisted ordinary entries are capped at `combatLogMaxPersistedEntries = 160`, and routine POST batches default to `combatLogBatchMaxEntries = 12` to keep synchronous payload stringification bounded. Live validation should confirm that new Active-in-range combat starts keep `runtime.lastCombatLogRecordMs` below second scale, do not produce 4-5s `main-loop-gap` frames tied to combat logging, and no longer cascade into `websocket offline` / `网络连接离线且周围危险` exits solely at combat entry.
- `bootstrap-0.4.234` changes the low-risk `session-mismatch-recovery` flow from immediate takeover to one controlled preflight refresh. When self is missing, the page/auth UI looks logged out, and native WebSocket or fresh snapshot evidence still proves the current game session is online, the existing takeover helper must still prove there is no pending exit, relogin hold, login suppress, exit-reset snapshot gate, reconnect churn, offline-ish WebSocket state, recent offline context, or recent unsafe exit. If those blockers are clear, the first action records `localStorage.graspRatSessionMismatchRecovery`, exposes `status().sessionMismatchRecovery`, emits `session-mismatch-refresh`, persists pending combat-log entries, and refreshes the page without closing the important session; if the marker cannot be recorded, it waits instead of treating the refresh as satisfied. Only if the same user/session mismatch survives a page load whose `performance.timeOrigin` follows that request does the original guarded takeover path call `maybeStartAutoLogin('session-mismatch-recovery')`; the takeover still may bypass only the `/snapshot` connectivity streak and still cannot bypass learned login-point safety. The marker expires after `sessionMismatchRecoveryReloadMaxAgeMs = 120000` and is cleared once self is restored or the mismatch resolves. Live validation should confirm that the first low-risk mismatch shows `session-mismatch-refresh` / `界面显示未登录但原生会话仍在线，先刷新页面确认状态`, and only a repeated same-user mismatch after that refresh reaches `session-mismatch-recovery` or the login snapshot gate.
- `bootstrap-0.4.233` makes the learned login-point safety radius depend on last-exit HP. Exit trigger and exit confirmation now persist `loginPointSafety.lastExitSelfHp` when self HP is available. If that HP is at least `80`, relogin only needs the learned login point to be safe within `20000cm`; low HP or missing/unknown exit HP keeps the existing conservative `60000cm` radius. The safety streak, dangerous-player criteria, missing-point handling, and pre-login-only gate behavior from `.232` are unchanged. Live validation should confirm that after a high-HP exit the panel/status shows the active login-point safety radius as `20000cm` and allows login after 12 safe samples inside 200m, while unknown/low-HP exits still require `60000cm`.
- `bootstrap-0.4.232` narrows the learned login-point safety gate to the pre-login phase only. Auto/manual/fallback/native login paths still require the combined `/snapshot` connectivity and learned login-point safety gate before starting login, so a visible unsafe login point remains blocked before entry. Once an alive self entity is available after entry, the bot no longer runs the post-entry `login point safety gate` hard exit or leaves solely because the learned point streak is below `12/12`; it records the current login point and proceeds through normal movement, combat, injury, stamina, offline, and combat-disadvantage exit logic. Live validation should confirm that after a permitted entry, `登录点安全 0/12` or a freshly reset login-point streak does not by itself trigger `offline-leave` / `登录点安全快照未满足`; exits after entry should come only from normal combat, damage, stamina, no-self, or network logic.
- `bootstrap-0.4.231` adds lightweight runtime timing diagnostics for the repeated "enemy enters range, page freezes, then exits as network sampling timeout" observation. Runtime now records the latest main tick duration/source, global refresh duration, per-`/snapshot` and `/minimap` request durations, and combat-log build/record durations without changing combat decisions or exit thresholds. `globalState.samplingOutage` / `offlineSafety.samplingOutage` expose `refreshDurationMs`, `snapshotDurationMs`, `minimapDurationMs`, `lastTickDurationMs`, `lastTickSource`, `lastCombatLogBuildMs`, and `lastCombatLogRecordMs`; combat-log `runtime` also includes `lastRefresh`, `lastTickDurationMs`, `lastCombatLogBuildMs`, and `lastCombatLogRecordMs`. Live validation should inspect the next `global sampling outage` event: if tick/log durations stay small while snapshot/minimap durations hit timeout, the likely cause is endpoint/page sampling; if tick or combat-log build/record duration jumps into seconds, investigate script-side combat/log workload.
- `bootstrap-0.4.230` fixes the remaining pre-login bypass seen after `.229`, where the page could still enter first and only then be forced out by `login point safety gate`. The likely bypass was outside `maybeStartAutoLogin()` logs: bootstrap fallback/manual code or native page login recovery could call the page's login/join path after the short suppress expired while persisted login-point safety was still `0/12`. The Tampermonkey and extension bootstraps now read `graspRatLoginPointSafety` when the bot is absent, disable the inline immediate-login button as `等待安全`, block fallback `maybeStartGameLogin()` before it calls `startLinuxDoLogin()` or clicks a login button, and no longer clear relogin holds after a bot manual-login request that returns `snapshot-gate`. Both bootstraps also install capture-phase login-button interceptors plus a `startLinuxDoLogin` guard. The remote bot installs the same native login event/function guard on startup so the final runtime also blocks page/native login calls while the combined login gate is unsafe. Live validation should confirm that while `登录点安全 0/12` or a danger actor is displayed, no new `session-start`/alive-self entry occurs; the panel should remain in the login-snapshot-gate / waiting-safe state until the point safety reaches `12/12`.
- `bootstrap-0.4.229` fixes the follow-up unsafe relogin case observed around 2026-06-26 23:19 CST, where the page entered and immediately ran profit/safety movement while `globalState.loginSnapshotGate.pointSafety.remaining = 12`. The `.228` entry guard checked the right condition, but the alive-self branch first called `maybeRecordLoginPoint(currentSummary)`, which refreshed the login point to the current unsafe entry coordinate and cleared the old safety evidence before `unsafeReloginEntryGateStatus()` ran. The guard now evaluates persisted login-point safety before recording a fresh post-login coordinate; only entries that pass the gate update the point. Static verification now enforces that ordering so an unsafe `0/12` or missing-point relogin cannot be hidden by the new alive-self coordinate. Live validation should confirm that after an exit/relogin context, an alive self with login-point safety below `12/12` immediately stops and leaves with `login point safety gate` / `登录点安全快照未满足，退出等待安全重连`, with no coin/combat/avoidance action first.
- `bootstrap-0.4.228` adds a post-entry hard guard for the 2026-06-26 22:56 CST relogin sequence. Local logs showed the actual unsafe login/session-start was still on `bootstrap-0.4.226`, then `.227` hot-updated after the bot was already alive; at that point `loginPointSafety` still showed `0/12` with a nearby same-day damage actor, but the normal alive-self profit/combat path could continue because no login button click remained to gate. The new guard persists `loginPointSafety.resetAt/resetReason` through reloads and, whenever an alive self appears from an exit/relogin context while the login point is missing or not `12/12`, immediately stops motion and exits through `leaveOffline('login point safety gate')` with `offlineSafety.loginPointSafetyGate`. This guard intentionally blocks only missing/unsafe login-point state, not a lost in-memory `/snapshot` connectivity streak after a safe entry. The existing click/manual/session-mismatch login gate remains unchanged. Live validation should confirm that if a new alive self appears while the panel/logs show `登录点安全 0/12`, the next decision is `offline-leave` or `login-snapshot-gate` with summary `登录点安全快照未满足，退出等待安全重连`, and no coin/combat action runs before the leave attempt; after `12/12`, entry is allowed even if the in-memory snapshot connectivity streak had reset during a reload.
- `bootstrap-0.4.227` fixes the relogin case where the panel showed `登录点安全 0/12` but `session-mismatch-recovery` still started login after the cooldown and `/snapshot` rows became green. The root cause was that the explicit live-session takeover bypass marked `snapshotGate.liveSessionTakeoverBypass = true`, and `maybeStartAutoLogin()` treated that flag as enough to bypass the whole combined gate, including `loginPointSafety`. The bypass now marks and allows login only when learned login-point safety is already satisfied; it may bypass the `/snapshot` connectivity streak for takeover, but it cannot bypass missing/unsafe login-point state. Auto and manual login both call `loginSnapshotGateAllowsLogin()`, static verification checks the helper and the no-direct-bypass invariant, and bot self-tests cover the `0/12` unsafe takeover block plus the still-valid safe takeover case. Live validation should confirm that while the panel shows `登录点安全 0/12` or a nearby danger, no auto login is attempted and the wait reason stays `login-snapshot-gate` / `等待登录点安全快照`; after `12/12`, session-mismatch takeover may proceed as before.
- `bootstrap-0.4.226` fixes false `combat tick gap` exits while the bot is already back in ordinary profit flow. The 2026-06-26 11:37:56 CST sample exited with `lastDecisionReason = best-opportunity-coin-route`, `previousCombatActive = false`, `currentCombatActive = false`, and `combatLogActive = false`; only `recentCombatFrameContext = true` remained from the fight that ended about 15s earlier, so the natural stop of combat-frame logging was misdiagnosed as `combat-log-gap-with-active-tick`. Combat-frame-gap exits now require live combat context (`previousCombatActive || currentCombatActive || combatLogActive`) in addition to the frame gap. Recent combat-frame context is still preserved for main-loop/reentry combat tick gaps, but it no longer exits by itself when current action is a coin route or other non-combat action. Static verification checks the `liveCombatContext` guard, and bot self-tests cover the coin-route regression plus the still-valid active/log-active frame-gap diagnosis. Live validation should confirm no `control-combat-tick-gap`/`leave-success:combat tick gap` exit appears when runtime diagnostics show only `recentCombatFrameContext = true` and current action is `best-opportunity-coin-route` or another coin action.
- `bootstrap-0.4.225` keeps relogin decision behavior unchanged but exposes the full pre-login gate state in `status().reloginGate` and the embedded Tampermonkey/extension panels. During relogin-related states the panel now renders three explicit rows: `冷却时间: remaining / total`, `快照接口连通性: streak / required`, and `登录点安全: streak / required`, including missing login-point, last danger, or last error details when present. The summary reuses existing login suppress/hold/cooldown, `loginSnapshotGate`, and `loginPointSafety` state; it does not trigger extra `/snapshot` requests. Static verification now checks the runtime status field and both panel rows. Live validation should confirm screenshots before relogin show the same cooldown, snapshot `?/3`, and login-point safety `?/12` state that the bot is using for its login decision.
- `bootstrap-0.4.224` fixes stale visible/native coin pursuit after another player picks up the selected coin. The root cause is that `buildMissingHeldOpportunity` intentionally recreated a missing held coin for the remaining `opportunityMissingHoldMs` / `opportunitySwitchHoldMs` window to suppress target jitter during source gaps. That was correct when native/render coin sources briefly disappeared, but wrong when the current visible coin sources were available and clearly no longer contained the selected coin. Missing-held coin construction now checks current native/render coin sources: if the held target is inside visible-authority range and the visible sources do not match it by id or nearby coordinates, the bot clears `lastTarget`, `opportunityChoice`, coin progress/approach locks, briefly ignores that stale id with reason `visible-coin-disappeared`, and immediately reselects from current visible profit. Snapshot/far coins and genuine visible-source outages keep the old short missing-hold anti-jitter behavior. Bot self-tests cover both branches, and static verification requires the clear helper/reason and regression test. Live validation should confirm that when a visible coin disappears because another player collected it, the next action switches to another visible coin/enemy/wait instead of walking to the old coordinate for several seconds.
- `bootstrap-0.4.223` fixes the visible 1-coin / invulnerable-player oscillation path from the screenshots. The root cause is that ordinary coin safety previously used only `activeThreats.filter(isAvoidanceThreat)`, so a realtime visible invulnerable player that was not classified as an Active threat could still trigger `avoid-invulnerable-target` after the bot had already selected a nearby ordinary coin. Ordinary coin safety now merges active invulnerable threats with native/realtime visible invulnerable humans before scoring visible/native coins, while snapshot-only invulnerable players do not block visible coin pickup. Invulnerability detection now checks each tick/ms/remaining alias independently, including `invulnerableRemainingMs` / `invulnerable_remaining_ms`, so an earlier zero alias cannot hide a positive remaining-time field. Low-value coins near visible invulnerable players should now show as `threat-blocked` in coin diagnostics and fall through to wait/other profit instead of repeated pickup/flee oscillation. The `.221` healthy high-value shortcut is unchanged: `10+` visible/native coins can still override invulnerable avoidance at healthy HP by design. Live validation should confirm screenshots/log frames on `.223` no longer choose ordinary 1-coins near visible invulnerable players, and diagnostics identify the skipped coin as threat-blocked.
- `bootstrap-0.4.222` keeps coin strategy behavior unchanged but improves observability for the visible-coin selection issue. Loader/status reason maps now distinguish `best-opportunity-coin-route` as `综合收益最高：金币路线` from `best-opportunity-visible-coin` as `综合收益最高：前往可见金币`, and the loaders fill the other known action reason gaps found in the map audit. The loader panel now renders `当前时间：HH:MM:SS` so screenshots can be matched to log timestamps. Runtime status and combat logs now expose `coinDiagnostics` for visible/nearby coin candidate state within `coinDiagnosticsNearDistance = 50000`, including `nearestRealtimeCoins`, `ignoredNearCoins`, `snapshotOnlyNearCoins`, and `filteredNearCoins`. Filtered entries record reasons such as `ignored`, `threat-blocked`, `stamina-unaffordable`, `route-stamina-unaffordable`, `max-distance`, and `snapshot-only`; a throttled misc `coin-diagnostics` log is emitted when filtered/ignored/snapshot-only nearby entries exist. Static verification checks the distinct Chinese labels, panel clock, reason maps, and diagnostics plumbing. Live validation should confirm future screenshots can be matched by panel time, and if a closer visible coin is skipped, the matching log frame or misc `coin-diagnostics` entry explains whether it was ignored, threat-blocked, stamina-blocked, snapshot-only, or outside the final visible candidate distance.
- `bootstrap-0.4.221` makes the high-value visible/native coin shortcut match the intended priority rule. Coins worth at least `highValueCoinPriorityAmount = 10` now beat active combat, recovery wait, incoming-bullet pressure, close non-invulnerable combat, and invulnerable-player avoidance whenever self HP is at least `highValueCoinPriorityHealthyHp = 50`; in that healthy state the shortcut ignores active-threat coin safety filters. When HP is below `50`, the shortcut still requires no incoming bullet, no current combat target, and no nearby non-invulnerable real threat. Low-value coins still lose to real incoming-bullet combat. Self-tests cover healthy high-value overrides for combat, incoming bullets, close Active players, and invulnerable avoidance, plus low-HP guard behavior; static verification requires the updated coverage. Live validation should confirm a healthy bot takes nearby `10+` visible/native coins before fighting or fleeing from nearby invulnerable/Active players, while low-HP frames still leave/fight instead of forcing unsafe pickup.
- `bootstrap-0.4.220` keeps ordinary attack, profit-combat, aim, and fire range at `combatAttackRange = 14500cm`, but widens dodge-only defensive detection by `combatDodgeRangeBuffer = 1000cm`. A native/visible opponent just outside attack range can now be selected only when real incoming bullet evidence points to that opponent, or an unknown incoming bullet is paired with that opponent firing. In that buffered `14500-15500cm` band combat returns `combat-out-of-range-dodge` with `shoot=false` and marks `combatDodgeOnly`, so the bot actively dodges extreme-range hand-controlled shots without refreshing ordinary combat engagement or firing out of range. Static verification now checks the buffer config, dodge-only candidate pool, no ordinary engagement refresh, and the new self-tests for out-of-range incoming-bullet dodge. Live validation should confirm buffered real-bullet frames log `combatState.dodgeOnly` / `combat-out-of-range-dodge`, no out-of-range shots are sent, and ordinary no-bullet targets beyond `combatAttackRange` still do not start combat.
- `bootstrap-0.4.219` no longer treats a successful `leave` API response as proof that the server accepted the exit. Normal leave success now marks the active pending exit with a durable `reloadConfirmation`, persists the pending-exit context in localStorage, persists ordinary unsent combat-log entries alongside exit-audit and important logs, flushes logs when possible, then refreshes the page without prematurely closing the important session. After reload, restored pending-exit state is marked as reloaded: refreshed offline/no-self evidence or local token/chat/self-missing evidence confirms the exit, refreshed alive-self evidence schedules the original pending-exit retry and resumes normal action selection, and unknown refreshed state waits briefly (`leaveSuccessReloadUnknownGraceMs = 12000`) before retrying. `pendingExitPersistMaxMs = 3600000` bounds stale restored exits. Static verification now requires the storage keys, startup restore path, no direct plain `leave-success` confirmation, no `leave-success` session-end log before confirmation, durable retry/update/clear paths, and the dedicated reload path not calling `closeCurrentImportantSessionBeforeReload`. Bot self-tests cover refresh-before-confirm, restored reload markers, still-online retry, and offline confirmation. Live validation should confirm one normal leave-success cycle records `leave-success-refresh-confirmation`, survives the refresh with pending-exit context/log queues intact, and then either confirms only when refreshed state is offline or retries if the refreshed state is still online.
- `bootstrap-0.4.218` moves the target whitelist out of runtime defaults and into `dist/target-whitelist.json`. The remote bot derives the whitelist URL from the remote script URL by default, fetches it at startup, polls every `targetWhitelistPollMs = 10000`, times out after `targetWhitelistTimeoutMs = 7000`, and caps parsed usernames at `targetWhitelistMaxNames = 100`. There is no built-in default whitelist: before the first successful fetch the effective whitelist is empty; if a later poll fails, the bot records the error but keeps the last successful username set. Matching is now exact trimmed username only; target/user ID whitelist config and ID matching are removed. The published whitelist contains `文月` and `Firefox`. Static verification now requires the standalone whitelist file, remote polling path, no `targetWhitelistIds`, username-only matching, and failure preservation. Live validation should confirm `status.targetWhitelist.loaded = true`, `count = 2`, the URL ending in `target-whitelist.json`, no attacks on `文月` / `Firefox`, and no protection for an ID-only collision whose displayed name is not whitelisted.
- `bootstrap-0.4.217` analyzes the 2026-06-26 11:05 biliee fight where the bot spent opening 5s stamina on burst fire before the target's first owned real bullet appeared, leaving only about `2442ms` 5s stamina at the first pressure frame and then dropping near zero under bullet pressure. New Active engagements now enter a guarded opponent-probe fire window for the first `combatOpponentProbeMs = 6000` unless target-owned real-bullet evidence has already appeared or a higher-priority low-risk/pressure exception applies. During that window shooting keeps a `combatOpponentProbeReserveMs = 5600` 5s reserve and uses `combatOpponentProbeEveryMs = 520` point-fire cadence, while movement/spacing/dodge decisions keep their normal action reason. Passive-runner, finish-low-threat, target-owned pressure, and confirmed post-probe behavior remain separate guarded windows. Offline replay on `combat-log-service/logs/2026-06-26/combat/20260626030537-self-28886-vs-biliee.jsonl` lines `96-225` shows the probe would keep `5/14` opening shots before first target-owned pressure, skip `9`, save about `4500ms` stamina, and project first-pressure 5s stamina from `2442ms` to `6942ms`, satisfying the `5600ms` reserve.
- `bootstrap-0.4.216` fixes the 2026-06-26 OAuth/callback relogin gap where the bot could enter the game and fight before the learned login-point safety gate had a usable coordinate. `loginPointSafety` now treats the persisted point as durable localStorage state: runtime hot updates preserve `lastLoginAt`, localStorage coordinates win over empty/stale in-memory state, exit resets clear only the safety streak/evidence and never clear the point, and successful first alive-self/session recovery updates the point for the current login. Missing login-point coordinates are exposed explicitly as `missingPoint` and no longer satisfy the 12-sample safety gate or accumulate safe streaks. Static verification covers the persisted-point merge, missing-point block, OAuth/session timestamp inference, and non-clearing reset behavior; bot self-tests cover `lastLoginAt` preservation.
- `bootstrap-0.4.215` keeps the `.213` sampling-outage and `.214` combat tick/frame gap behavior, and tightens combat-log observability for the related control wait states. Top-level combat-log `exit` summaries now treat `control-global-sampling-outage` and `control-combat-tick-gap` as exit-like even when a frame has no `leave` detail, so diagnostic wait frames still carry explicit outage/gap reasons instead of disappearing from exit-summary audits. Bot self-tests cover both control outage reasons without leave detail, and static verification now requires the shared exit-summary regex to cover `control-ws`, `control-global`, and `control-combat` consistently in source and generated runtime.
- `bootstrap-0.4.214` treats combat/recent-combat decision-loop, tick-reentry, or combat-frame gaps of at least `combatTickGapOfflineMs = 5000` as a network-fluctuation exit condition, even when the native WebSocket still reports healthy. The main tick now records previous/current tick timing and combat context, and `combatTickGapOfflineState()` exits immediately through `leaveOffline('combat tick gap')` when the main loop itself gaps (`diagnosis = main-loop-gap`, likely JS/main-loop pause), a new tick arrives while the previous tick is still stuck (`diagnosis = tick-reentry-gap`, likely stuck async/main-loop await), or combat frames stop while ticks continue (`diagnosis = combat-log-gap-with-active-tick`, likely state/log gating gap). Recent combat-frame context is preserved as evidence even if the current decision state has already been cleared or misclassified. Combat and exit-audit logs now include a top-level `runtime` diagnostic summary with tick gap, tick-in-progress gap, combat-frame gap, combat context, diagnosis, likely cause, and the last `offlineSafety.combatTickGap` evidence. Offline summaries render explicit combat-loop gap wording, unsafe relogin delay applies after confirmed combat-gap exits, bot self-tests cover combat/non-combat gap behavior, cleared-decision recent-frame context, and frame-gap/reentry diagnosis, and static verification checks source/dist invariants.
- `bootstrap-0.4.213` treats hidden-page combat `/snapshot`/`/minimap` sampling failures as a network fluctuation mode even when the native WebSocket still reports open. `refreshGlobalState()` records structured `globalState.samplingOutage` evidence with timeout/error fields and status exposure, triggers an immediate native tick when the outage satisfies the gate, and the main offline branch now stops motion and exits through `leaveOffline('global sampling outage')`. The default gate is conservative: `globalSamplingOutageOfflineEnabled = true`, `globalSamplingOutageMinErrors = 1`, `globalSamplingOutageMinAgeMs = 0`, and `globalSamplingOutageCombatOnly = true`, so ordinary non-combat profit flow does not leave on a transient sampling miss. Offline summaries now render explicit network sampling timeout wording, unsafe relogin delay applies after confirmed sampling-outage exits, bot self-tests cover combat-triggered and non-combat-guard behavior, and static verification checks the source/dist invariants.
- `bootstrap-0.4.212` refines action priority around ROI. Active targets at `Drop <= 3` no longer start proactive defensive/profit combat, or retreat-ignore waiting, unless they own an incoming bullet, pair an unknown incoming bullet with a firing signal, or recent injury evidence makes them the likely shooter; existing low-value engagements are cleared once that threat evidence disappears. Visible/native coins worth at least `10` can take an early priority path before combat/recovery/invulnerable avoidance when the existing coin safety filters allow it and self HP is at least `50`, or when low HP has no nearby real threat. Low-HP existing combat still keeps combat HP/exit rules. Self-tests cover low-Drop incoming/no-incoming behavior, high-value coin overrides, low-HP combat non-interruption, and low-HP no-threat pickup; static verification checks the new thresholds and coverage.
- `bootstrap-0.4.211` stabilizes native visible coin route first-target selection. When a selected route is still visible, safe, and overlaps at least two previous route coins, the route planner keeps the current first coin unless the competing first-coin route is better by more than `coinRouteSwitchMargin = 3000` or `coinRouteSwitchRelativeMargin = 0.1`. This addresses near-tie route replans that could otherwise send the bot back and forth between adjacent 1-coin route anchors, while still allowing clearly better route scores to switch. Self-tests cover both the held-first-coin case and the clear-switch case, and static verification requires the held-route helper.
- `bootstrap-0.4.210` keeps visible coin route ROI selection but rejects routes whose first coin would clearly skip a much closer safe local coin. A safe non-route coin within `22000cm` blocks a route when the route's first leg is both more than `1.45x` that coin's distance and more than `6000cm` farther, so detached far clusters no longer override an obvious nearby pickup while routes that start locally still work. Self-tests and static verification cover the closer-first guard.
- `bootstrap-0.4.209` lets a fresh `/snapshot` alive self entry count as authoritative live-session evidence for the guarded `session-mismatch-recovery` takeover path. The snapshot self evidence uses the existing `snapshotSelfFreshEnough()` age gate, is recorded on `noSelfGameSession` / `liveSessionTakeover` details, and can prevent false exit confirmation the same way native live-session evidence does. It does not create a global immediate relogin rule: the `.208` blockers still apply, including pending exit, login suppress, relogin hold, exit-trigger/exit-confirmed snapshot reset, reconnect churn, offline-ish WebSocket state, recent offline context, and recent unsafe exit. Self-tests and static verification now cover snapshot-self live evidence plus the existing post-exit blockers.
- `bootstrap-0.4.208` restores only a narrow fast takeover path for the `.192` live-session mismatch case. `session-mismatch-recovery` can bypass the login snapshot/learned-point gate only when `liveSessionMismatchTakeoverState` proves a timed-out authoritative mismatch with current user id, cleared token, no login-required UI, native WebSocket open/connecting, and no pending exit, exit motion lock, relogin hold, login suppress, exit-trigger/exit-confirmed snapshot reset, reconnect churn, offline-ish WebSocket state, recent offline context, or recent unsafe exit. Blocked cases continue through the ordinary offline/exit or relogin-safe path, so WebSocket-disconnect exits still cannot immediately log back in near an unsafe login point. Self-tests and static verification now cover both explicit takeover bypass and post-exit/reconnect blockers.
- `bootstrap-0.4.207` removes the session-mismatch recovery snapshot-gate bypass. After a WebSocket-disconnect exit, if the page later looks logged out while the native game session still appears online, the bot still treats relogin as pending but now waits for the normal `loginSnapshotGate` plus learned login-point safety instead of forcing an immediate restart. The recovery branch also stops reloading while the gate is pending, so the 12-safe-snapshot login-point requirement can actually finish. Self-tests and static verification now cover the non-bypass expectation.
- `bootstrap-0.4.206` adds a 5s stamina-consumption cooldown for AFK opportunity targets. The bot records visible target stamina by id; an observed 5s stamina decrease starts a 60s cooldown that blocks only out-of-range AFK Drop candidates. In-range AFK targets still use the old immediate ROI attack behavior, and newly visible AFK-like targets without observed consumption remain eligible to approach immediately. The short cooldown map is preserved across remote hot updates. Self-tests cover new target eligibility, observed-drop cooldown, and in-range bypass.
- `bootstrap-0.4.205` keeps coin movement/ROI selection behavior unchanged but preserves selected route metadata when the best route and best single coin share the same first coin. The action reason remains `best-opportunity-coin-route`, status text maps it to `综合收益最高：金币路线`, and the overlay keeps drawing the full route points while the bot approaches the first pickup instead of collapsing back to a single-coin line. Self-tests cover same-first-coin route display persistence and route-leg threat blocking; static verification requires the route metadata merge and overlay route-point support.
- `bootstrap-0.4.204` makes the learned login-point safety gate more conservative by expanding the safety radius to `60000cm`, matching the measured `50000cm` active view radius plus a `10000cm` margin. The gate remains additive: future relogins still honor all existing suppress/cooldown/enemy/offline holds, then require 12 consecutive safe `/snapshot` samples around the learned point before login. Dangerous nearby players still include same-day damage actors, `Drop >= 2`, `Active` mode, recent movement over `500cm`, non-full 5s stamina, or firing evidence. It also fixes relogin hold display text so the reason row uses the current remaining hold time before falling back to the original delay, avoiding stale "等待1分钟" text during longer stamina/offline holds. Static verification checks the widened radius, existing danger rules, and remaining-hold display preference.
- Current objective: optimize live action logic using measured game parameters. Priorities are survival first, then stamina-aware ROI, reduced wasteful movement/target jitter, and low-noise runtime behavior.
- Long-term combat iteration direction: before hitting the local machine's compute limit, combat logic may continue becoming more flexible and more complex, including combat-only higher decision/react frequency, replay/evaluation driven tuning, and shared combat stance/trend layers for target, movement, shooting, and exit decisions. Each battle upgrade must preserve or improve existing winnable matchups; prefer guarded windows, self-tests, and log/replay evidence over broad global threshold changes that could make previously beatable opponents harder to beat.
- Combat optimization validation workflow: whenever combat logic is changed based on one or more battle records, run the new logic through an offline replay of the referenced battle before calling the change done. Use `cd combat-log-service && npm run replay -- --file <jsonl> --start-line <first-combat-frame-line> --end-line <last-combat-frame-line> --self-id <self-id> --target-id <target-id> [--target-name <name>]`, or add a replay self-test for the reference. The replay must show a concrete improvement over the old/logged behavior for that fight, such as more estimated hits, earlier target damage, or strictly better survival with no lost damage opportunity. If replay shows no improvement, continue iterating instead of shipping. Also run `node grasp-rat-bot.js --self-test`, `cd combat-log-service && npm test`, and `node scripts/verify-objective-build.js` after rebuilding.
- Fresh `bootstrap-0.4.89` combat logs on 2026-06-11 showed two lost fights plus a relogin-after-loss bug. `bootstrap-0.4.90` fixes combat leave pending/HP-based relogin hold after attempted leave request errors, and prevents recovery HP from re-engaging combat except to leave. `bootstrap-0.4.92` changes 1h stamina budget exhaustion around coin pickup to leave immediately and wait 5 minutes before relogin. `bootstrap-0.4.93` avoids coin routes toward nearby invulnerable active players and shortens pursuit exit thresholds for non-full HP/invulnerable chasers. `bootstrap-0.4.94` removes the similar-ROI pre-choice wait and relies on selected-target hold/switch margin to avoid target jitter. `bootstrap-0.4.95` automatically clicks the native zoom-out control 6 times after a self entity is detected for a new/renewed login session. Tampermonkey `0.4.38` / extension `0.1.17` reset embedded workspace positioning so tall/narrow viewport native `inset` rules no longer shift the game center. `bootstrap-0.4.96` treats any in-range Active enemy as a defensive combat target, so combat beats coin pickup and HP disadvantage exits fire before taking a bullet. `bootstrap-0.4.97` primes a local login suppress after attempted unsafe exits so page reloads cannot immediately reconnect after injury/combat/offline danger, while still allowing in-game pending cover/retry until exit confirmation; combat log frames also include a top-level `exit` summary. `bootstrap-0.4.98` reuses a shared/tested top-level combat-log `exit` summary helper and records hold/pending suppress minimum/HP-delay fields for unsafe exits. `bootstrap-0.4.99` also records decision-level relogin hold fields in top-level `exit` for confirmed `enemy-leave-wait` / `offline-leave-wait` frames where `leave` is already null. `bootstrap-0.4.100` adds `login` context to combat frames/start/end, including suppress remaining/reason, enemy/offline hold remaining, last login result, decision login, and manual-login bypass context; analyzer latest-exit output prints these flags. `bootstrap-0.4.101` includes top-level `exit` summaries in combat-start/combat-end session events too, so suspended/end events are not misread as missing exit summaries. `bootstrap-0.4.102` treats join-mode Active players as non-AFK even when stationary, full-stamina, and zero-drop, so a non-whitelisted/non-invulnerable Active target inside combat range forces combat instead of coin pickup. Tampermonkey `0.4.39` / extension `0.1.18` compact the embedded panel, replace the text pause button with an accessible dot control, and hide empty remote-log/Active rows. Tampermonkey `0.4.40` / extension `0.1.19` finish the compact panel: BOT pause/resume, WS state, and remote-log state are header dots; the log dot toggles combat logging and blinks while flushes are pending; metric labels are tooltip/ARIA-only; stamina shows native remaining/limit values. `7f73829` adds `combat-log-service/analyze-logs.js` / `npm run analyze` to audit collected JSONL for missing top-level `exit` and unsafe exits below the 60s minimum delay. `750818d` adds `npm run monitor` / `--watch` mode for continuous log-directory monitoring without CDP. `6bf31a8` adds `--since`, `--min-version`, and `--version` filters so current-version monitoring can ignore historical logs. `12b463a` adds manifest-based `analyze:current` / `monitor:current` commands that read `../dist/manifest.json` and filter to its version. `7e46348` adds `npm test` / `--self-test` coverage for the analyzer's issue detection and manifest current-version filtering. `eb26896` adds bot self-tests for combat-log top-level exit summary fields. `f8591f2` adds a bot self-test for decision-level hold fallback in top-level `exit`. `57c30a4` adds `--require-entries` / `validate:current`, so current-version validation fails clearly with `no-matching-entries` when no fresh logs have arrived. `6c49ebd` adds relogin context to combat logs/analyzer output. `e59e84d` adds combat-start/end top-level exit coverage. `8bde91d` makes the analyzer flag automatic login attempts during active exit suppress/hold and manual login clearing exit holds. `68ee15e` adds `npm run monitor:current:strict` and README workflow docs for strict current-version live monitoring. `3f89f9a` adds analyzer behavior-regression issues for old ambiguous opportunity waiting and coin actions while a non-invulnerable Active player is inside attack range. `7829da0` adds `--require-exit-events`, `validate:objective`, and `monitor:objective` so current-version objective validation requires at least one exit sample instead of treating ordinary logs as proof. `f12b217` adds reason-count summaries for exit and behavior events so live/current logs can quickly show the dominant exit cause. `2300170` adds exit safety delay summaries, including unsafe exits that meet or miss the minimum delay. `ebea208` adds reason-specific required delay auditing, currently including the 5 minute stamina-budget exit requirement. `f9d35b8` makes `--manifest` validation check source hashes and makes the collector propagate payload version/sourceHash to entries that lack them. `4d18e3d` adds `node scripts/verify-objective-build.js`, a static release verifier for manifest/source hash alignment, objective constants, runtime removal of ambiguous-wait strings, bootstrap layout CSS, bootstrap version consistency, and manifest sourceHash wiring. `ea7807a` makes `validate:objective` / `monitor:objective` require positive current-version evidence for Active-in-range combat response and HP-disadvantage combat exits, not only absence of regressions. `6d998d5` expands `verify-objective-build.js` to check compact panel dot controls, removed visible section/status titles, tooltip-only metric labels, and raw remaining/limit stamina formatting. `087a87e` expands the same verifier to check post-login zoom scheduling: 6 clicks, 80ms interval, 1s self-missing re-arm, session keying, native zoom-out control discovery/clicking, result counters, and status exposure. `1fe4da9` expands the verifier to require join-mode Active non-AFK combat handling in source/dist plus the stationary full-stamina Active coin-override self-test. `3ff7d78` makes log audits treat `mode/current_join_mode=Active` as Active-in-range evidence even when logged `active:false`, so objective validation will catch stationary/full-stamina Active coin actions. It still needs fresh live validation on current remote `bootstrap-0.4.102`.

- `74ee7bd` makes log audits also flag `lastLogin.ignoredSuppressMs` as `login-attempt-during-exit-hold`, so immediate reconnect evidence is caught even when the attempted login is only present in last-login context.
- `63a8722` builds `bootstrap-0.4.103`; combat logging now ends/suspends active combat sessions with `suspended:login-suppressed` when an unsafe exit hold is blocking relogin, and `verify-objective-build.js` checks this invariant.
- `cc5d27e` builds `bootstrap-0.4.104`; combat logging also ends/suspends active combat sessions with `suspended:manual-login`, so manual hold-clearing reconnect attempts are explicit in combat session boundaries.
- `546fb51` makes log audits read top-level combat-end `reason` values and count `suspended:login-suppressed` / `suspended:manual-login` as relogin/session-end exit events without false missing-exit issues.
- `ca24819` adds analyzer self-test coverage for top-level `suspended:manual-login` combat-end reason counts, matching the existing `suspended:login-suppressed` coverage.
- `c5934e3` builds `bootstrap-0.4.105`; confirmed and pending exit suppress helpers now always use the longer of the computed delay and required minimum delay, including fixed condition delays.
- `0e95656` makes log audits flag `missing-exit-reason` when a top-level `exit` object is present but lacks explicit `exit.reason`, and documents the issue in the combat-log README.
- `74c0b36` adds `npm run monitor:objective:fresh`, which runs objective monitoring from invocation time with current manifest filtering and latest-event output, so fresh post-release monitoring does not require manually adding `--since now --latest 10`.
- `44b5284` builds `bootstrap-0.4.106`; combat-log top-level `exit.reason` now preserves the outer specific decision reason during leave retry cooldowns instead of collapsing to generic `cooldown`.
- `7622891` makes log audits flag `generic-exit-reason` when top-level `exit.reason` is a generic value such as `cooldown`, and documents it in the combat-log README.
- `d2dbe5b` builds `bootstrap-0.4.107`; confirmed ordinary safe offline exits no longer create an offline relogin suppress/hold, while unsafe offline, reconnect churn, server-position stall, stamina reset, and stamina-budget exits still wait according to their required delay.
- `505597e` builds `bootstrap-0.4.108`; top-level combat-log `exit` summaries now include `safeReloginAllowed` and `offlineSafety`, and log audits treat marked safe offline exits as safe zero-delay relogin evidence instead of unsafe-delay issues.
- `5393b63` builds `bootstrap-0.4.109`; combat decision targets now log mode/life/active/firing/invulnerable fields, and log audits use current target fields as Active-in-range evidence when `nearbyEntities` is absent or capped.
- `f473872` adds `npm run monitor:objective:observe`, a current-version fresh monitor that still prints missing-evidence status but exits non-zero only for real audit issues or parse errors, so long-running observation is not treated as failed solely because required live samples have not happened yet.
- `6127583` adds `node scripts/objective-status.js`, which combines static objective verifier results with current-version live log evidence and reports requirement-by-requirement completion; `--fail-on-incomplete` exits non-zero until fresh `.109` logs prove the required exit, Active combat, and HP-disadvantage samples.
- `d7f7407` adds `node scripts/objective-status.js --self-test`, covering both missing-evidence and synthetic complete current-version log fixtures for the objective completion gate.
- `eb84718` refines `objective-status.js` so each requirement uses its own evidence predicate; exit/relogin can be proven by current exit evidence without requiring unrelated Active/HP samples, while Active combat and HP-disadvantage samples remain separate blockers.
- `cff703e` tightens `objective-status.js` so safe zero-delay exits alone cannot prove the exit/relogin requirement; completion now requires at least one current unsafe exit or reason-required-delay exit with a sufficient observed hold.
- `1008228` makes `objective-status.js` human output include `unsafeOrRequiredDelayExits`, so the completion gate's exit/relogin evidence count is visible without reading JSON.
- `3acf4dd` builds `bootstrap-0.4.110`; exit audit logs are critical and persisted until successfully POSTed, record `exit-trigger`, each `leave-request` with send/complete/duration/result/error, and `exit-confirmed`, and block reload, Cloudflare/BunkerWeb refresh, auto login, and manual login while any exit audit log remains pending/sending/persisted. Failed or errored leave attempts stay as pending exits, and auth/login page evidence is treated as weak confirmation after a leave error instead of proving server-side exit.
- `bootstrap-0.4.111` changes exit confirmation/retry strategy: a completed leave success confirms exit; a non-successful/errored leave can confirm only when token is cleared, the left chat contains `left user <currentUserId>`, and the own entity is absent from native/local/fresh snapshot state; pending leave retries are throttled to at least 10s and asynchronous so live control continues while self remains alive; pending exit with alive self no longer lets local token/ws clearing force the offline stop branch; injury-trigger leave starts in the background and normal combat/flee control continues; leave HTTP 403 is treated as confirmed exit plus at least 1 hour relogin suppression for risk-control cooldown. Static verifier now checks these invariants.
- `bootstrap-0.4.112` suppresses ordinary injury-trigger leave while a combat state is active. Combat state includes the current combat action, pending combat leave, `lastSafety.engagedCombat`, or a recent `bot.combatTarget`, so battle HP-drop handling stays under combat leave thresholds instead of the ordinary threat/injury exit path. Static verifier now checks this invariant in source and dist.
- `bootstrap-0.4.113` makes active pending exit a global leave lock: new offline/injury/pursuit/combat/enemy-hold leave triggers return `skippedNewLeave` and reuse the earliest pending exit summary, while `retryPendingExit` is explicitly allowed through as the original exit flow. Stamina must-leave no longer starts a second leave while pending exit is active, injury/pursuit skip intents are logged on the normal action, and `issueLeaveCommand` blocks any non-`pendingExitRetry` leave command when `bot.pendingExit` exists. Static verifier checks these invariants in source and dist.
- `bootstrap-0.4.114` makes leave HTTP 403 risk-control holds recover early after 5 consecutive successful `/snapshot` responses while keeping the 1h delay as fallback; `/snapshot` and `/minimap` refreshes are audited independently. It also delays post-login zoom by 350ms and requests native resize before/during zoom. Tampermonkey `0.4.41` / extension `0.1.20` dispatch native `resize` after sidebar layout/style/panel insertion and on DOMContentLoaded/load, fixing the stale canvas backing-store band seen after A converted the sidebar from overlay to occupied layout. Full-page BunkerWeb 403 refresh is now 10 minutes instead of 5 seconds. Static verifier checks the new constants and resize hooks.
- `bootstrap-0.4.115` keeps recovery-state engaged combat targets under combat priority. If an `engagedCombatTarget` still exists, including grace-range reengage or a real shooter overriding the engaged target, recovery returns the combat action instead of clearing engagement and falling through to flee/recover; non-engaged recovery threats still use the old safety path. Static verifier checks this invariant in source and dist.
- `bootstrap-0.4.116` reduces post-login zoom-out from 6 to 4 native clicks and preserves `postLoginZoom` state across remote bot hot updates, so replacing the bot script while already in a game session does not trigger another zoom pass. Tampermonkey `0.4.42` / extension `0.1.21` add a landscape/short-viewport `#world` crop rule: the canvas extends 368px left and is clipped by `.map-shell`, matching the native left-reserved `screenCenter()` offset without double-shifting the visible game center right. Static verifier checks the 4-click invariant, zoom-state preservation, and the landscape crop offset rule.
- `bootstrap-0.4.117` analyzes the 2026-06-12 lockcc fights and changes combat fire discipline: `combat-low-hp-no-damage-leave` is no longer a runtime exit trigger, ordinary combat shooting no longer force-bypasses `shootEveryMs`, 5s stamina now gates shooting with hard/dodge/reserve bands, low stamina can pause fire for dodge recovery, reserve-band fire is throttled, and long no-damage pressure-close can continue down to HP 60. Static verifier checks the fire-discipline constants, shooting plan wiring, no no-damage leave branch, and self-test coverage.
- `bootstrap-0.4.118` stops pure join-mode Active from forcing defensive combat when the target has no real activity signal. Moving, firing, non-full-stamina, incoming-bullet, and profitable Drop targets still enter combat, but a stationary full-stamina Active target with Drop 0 no longer beats coin pickup. Log audits now require real activity evidence for Active-in-range behavior issues/evidence, and static/self-tests cover the zero-drop stationary Active case.
- `bootstrap-0.4.119` stops native motion immediately when leave requests complete successfully/with HTTP 403 and again when pending exits are confirmed. The stop path now cancels precision pulse timers and clears native keys, `currentVel`, `targetVel`, `velocity`, `lastVel`, and touch movement so the map cannot keep moving after the own entity has left. Static verifier checks this invariant in source and dist.
- `bootstrap-0.4.120` analyzes the recent 2026-06-12 `.118` fights. The fugua sample held own HP at 100 while reducing target HP 94->37, confirming stamina-aware fire discipline helped survival. The xmsthc/小鱼 sample was not a clear disadvantage but became a long 100->67 vs 100->67 exchange; high-HP, non-disadvantaged combat now lowers the effective dodge reserve from 3800ms to 3000ms and keeps burst fire pressure without force shooting. Mid/low HP and hard-low-stamina cases still preserve dodge stamina. Static verifier and self-tests cover the new high-HP fire window.
- `bootstrap-0.4.121` refactors opportunity hold state to keep coin targets stable across short native/source candidate gaps. Opportunity choices now carry coordinate/amount/action metadata, match same physical coins by coordinate when ids change, synthesize a held coin candidate within the hold window when the current target briefly disappears, and clear held choices on confirmed collection or stale-coin ignore. Self-tests cover the 54m vs 151m same-value coin jitter case.
- `bootstrap-0.4.122` fixes a visible-coin wait case where the bot could fall through to `wait-for-snapshot-coin` despite on-screen coins. Native coin discovery now accepts render/state getter functions, object maps, and single coin objects in addition to arrays/Map/Set, so visible coin sources are less likely to be missed. Coin threat filtering is route-aware for non-invulnerable Active threats: a coin inside an Active danger radius is allowed when the route stops before the threat buffer, while routes ending too close and invulnerable Active routes remain blocked. Self-tests cover the visible-coin-before-Active case and the route-aware blocker.
- `bootstrap-0.4.123` handles the logged-in/no-self reconnect trap: if a current user/session is present but the own entity stays missing, reconnect churn exits immediately and a 30s no-self timeout also exits via the offline leave path. Every actual relogin attempt is now gated by `loginSnapshotGate`, which resets on exit trigger/confirmation and requires 3 consecutive successful `/snapshot` fetches before auto or manual login can proceed; existing cooldown/suppress conditions still apply. Static verifier checks source/dist for the no-self exit branch, snapshot gate wiring, reset points, and status/log exposure.
- `bootstrap-0.4.124` enforces the 500m realtime coin authority from the screenshot issue: `nativeCoinAuthoritativeRadius` is now 50000cm, snapshot-only coins at or inside that radius are suppressed, and any safe affordable realtime/native coin inside the local authority radius blocks far snapshot competition/idle fallback and is used as a visible-coin fallback target. Static verifier and self-tests cover the 500m boundary and local-realtime-vs-far-snapshot-field case.
- `bootstrap-0.4.125` treats stationary/full-stamina join-mode Active targets with Drop as ordinary non-combat profit attacks instead of combat state. These passive profit targets use AFK ROI/stamina/range handling, do not set `combat:true` or combat HP summaries, do not become offline combat threats, and only real activity/incoming bullets can promote them back into defensive combat. Static verifier and self-tests cover the non-combat profit attack path.
- `bootstrap-0.4.126` adds a non-interactive in-game target line overlay above `#world`. It draws a faint dashed line from self to the current decision target: yellow for coin targets, green for non-combat player targets, and red for combat targets. The overlay canvas is aligned to the actual `#world` rect so it follows the existing landscape crop/offset rules and is removed on manual bot stop.
- `bootstrap-0.4.127` fixes the target line overlay center in the current cropped landscape layout by sizing and projecting against the visible `.map-shell` viewport instead of the hidden extra-wide `#world` canvas rect.
- `bootstrap-0.4.128` refines the target line endpoint by resolving the current decision target back to live native/render data before drawing: coins use matching native coin sources by id or nearby coordinate/amount, player targets use current native entities by id/name, and the self endpoint prefers the current `getSelf()` entity over the summarized decision self.
- `bootstrap-0.4.129` changes target overlay projection to use visual coordinates where available. Self now prefers `getRenderEntities()` / `state.localVisual` / visual-self fields before falling back to server `getSelf()`, and player targets prefer render entities before native/server entities. This addresses overlay drift when the displayed own character and server-position circle are separated.
- Tampermonkey `0.4.43` / extension `0.1.22` refine the embedded panel: BOT/WS/log indicators are left-aligned labeled dot controls, version text reads `加载器 ... / 远程脚本 ...`, stamina displays second-scale remaining/limit values, HP/Drop/Active share one line, and the script immediate-login button is inserted at the native login button position when logged out.
- `bootstrap-0.4.130` standardizes visible distance text to meters, including stamina-budget and pursuit leave summaries; freezes session uptime while self is missing after exit/no-self states; and expands the static verifier for these display invariants.
- Tampermonkey `0.4.44` / extension `0.1.23` polish the embedded panel: relogin countdown is folded into the current-behavior line, visible loader/remote version labels no longer add a `v` prefix, and panel section horizontal padding is increased to match the native sidebar.
- Tampermonkey `0.4.45` / extension `0.1.24` refine panel readability: the loader version row has more top spacing, loader/current-target/HP rows use segmented colors, and the reason row is muted gray.
- Tampermonkey `0.4.46` / extension `0.1.25` changes the combat HP comparison to `selfId selfHp VS targetHp targetId`, with green self ID, red target ID, threshold HP colors, and fixed 3ch HP width to avoid numeric jitter.
- `bootstrap-0.4.131` prevents known/local coin-field migration from overriding a closer safe realtime coin: a safe affordable non-snapshot coin within 300m and closer than the selected field representative blocks the `migrate-to-known-field` candidate. Self-tests cover the near realtime coin versus known field case.
- `bootstrap-0.4.132` analyzes fresh `.131` fights: mango was positive at 100 HP while target dropped 100->1; the first 小鱼 fight ended 100->83 while target dropped 100->1; the second 小鱼 fight stayed too close for most frames, with server-position stall active and real bullet pressure preventing spacing merge, then exited safely at 31 HP vs 34 HP. Emergency close spacing now merges through real-bullet pressure, low-HP close-risk exits trigger when under 50 HP and only within 5 HP of the target at close range, combat-start/end logs include sourceHash, pending-exit combat frames get top-level `exit`, and canonical combat leave reasons are preserved in top-level `exit.reason`. Static verifier and self-tests cover these invariants.
- `bootstrap-0.4.133` improves combat log quality for future analysis. Combat frames now include `combatMetrics` with frame delta, self/target HP deltas and damage taken, self/target movement, target distance delta, action/shooting/spacing summary, shot attempt result and cadence-blocked status, target no-damage timing, bullet source/threat counts and nearest threat timing, incoming bullet summary, and server-position stall state. `combat-start`/`combat-end` carry the same metrics snapshot, recent shot/metric state is preserved across hot updates, and the static verifier enforces the logging invariant.
- Tampermonkey `0.4.47` / extension `0.1.26` hide the combat-log panel control until a log endpoint has been configured from the console, persist a `combatLogEndpointConfigured` flag, and migrate previously stored endpoints. `bootstrap-0.4.134` also treats an unconfigured endpoint as no logging endpoint, so critical exit-audit logs do not create hidden pending flush/relogin blockers before the URL has been set. Static verifier checks the hidden-control gate and unconfigured endpoint blocker.
- Tampermonkey `0.4.48` / extension `0.1.27` convert the combat HP comparison row into a full-width panel with self/target names, HP values, percentage bars, threshold colors, and ARIA text. Static verifier checks the panel structure and version consistency.
- `bootstrap-0.4.135` adds important daily-summary logging. The remote bot persists structured data under localStorage key `graspRatImportantLogs`: `sessions` contain login/exit timestamps, exit reason/summary, login duration, 1d stamina spent, picked/earned coins, kill rewards, pure refreshed coin estimates, and kill details; `combats` contain enemy identity, self/enemy HP start/end/min/max/delta, result, and result reason; `events` are replayable `important-log` records for `session-start`, `session-end`, `kill`, and `combat-summary`. If a combat-log endpoint is configured, important logs are posted to the same log service even when normal combat logging is disabled, but they are non-critical and do not block reload/login like exit-audit logs. Static verifier now checks these invariants.
- `bootstrap-0.4.136` expands opportunity ROI comparison to the full 500m visible pool. Safe visible coins, known local coin fields, and AFK profit targets now share the same priority tier and are selected by reward per stamina instead of the old 180m near-priority split; global coin, AFK approach, and global AFK target candidate ranges are 50000cm. Self-tests cover 200m-vs-150m ROI selection, a 490m high-Drop AFK target beating an opposite 1-coin pickup, and high-value visible coins beating low-Drop AFK targets when ROI is better. Static verifier checks the new 500m constants and visible-opportunity self-tests.
- `bootstrap-0.4.137` fixes player Drop ROI cost. Player opportunities now price full travel to the target's feet plus estimated shooting stamina, because killed-player coins drop at the target position; attack range no longer reduces the movement cost in `opportunityEnemyStaminaCost`. Self-tests cover 500m Drop=5 losing to 100m 1 coin and same-distance Drop=10 losing to 10 coins because killing has extra stamina cost. Static verifier checks that player Drop pricing uses full pickup travel.
- `bootstrap-0.4.138` lets valuable post-combat dropped coins interrupt recovery. During recovery, `pickPostAttackDropCoin` can search to the 500m visible edge, but only if the dropped coin's ROI score is at least `60000` (equivalent to 100m 1 coin), so 500m/5 coins is collected while 500m/4 coins still waits to heal. Ordinary non-recovery post-attack pickup keeps the existing 220m distance. Static verifier checks the recovery max-distance/min-score wiring and self-tests cover both high-ROI pickup and low-ROI recovery wait.
- `bootstrap-0.4.139` adds an opportunity ROI oscillation lock: after more than 5 back-and-forth switches between the same two opportunity targets, the bot pins the current target until one of the pair is acquired or disappears. It also adds a short high-Drop post-kill wait: after an actual `attack`/`opportunistic-shot` against an AFK target with Drop >= 8, if the target disappears and no dropped coin is visible yet, the bot moves toward the target's last position for up to 2.5s within 500m, stopping within 900cm. Static verifier and self-tests cover both behaviors.
- `bootstrap-0.4.140` makes the high-Drop post-kill wait strictly targetless: the wait action moves by direction toward the killed AFK target's last position but does not set `action.target`, so UI/strategy state does not treat it as a selected coin/player target while waiting for the next communication tick. Metadata remains under `postAttackTarget` for logs/status. Static verifier now enforces that invariant.
- `bootstrap-0.4.141` improves daily statistics and kill reward attribution. `combat-log-service/daily-summary.js` now scans all JSONL files for same-day `important-log` records, merges split session-start/session-end events, shows missing exits with next-login context, explains `no-self` as a logged-in session with the own entity missing, and keeps inferred exits visible. Important kill logs now mark CHAT/snapshot confirmations and Drop-equal post-attack coin matches; visible post-attack coins whose amount equals the last recorded target Drop are attributed as kill rewards before pickup, with pickup still updating session coin totals without duplicate kills. `npm run daily` / `daily:self-test` are available in `combat-log-service`, and static verifier checks the statistics invariants.
- `bootstrap-0.4.142` analyzes fresh 2026-06-13 logs, especially the `.138` Wen fight that lasted 111s and exited at 49 HP vs 61 HP while `serverPositionStall` and close real bullet pressure were active. Close real bullet pressure within 80m now opens a mid-HP fire window: when self HP is at least 60 and the target is not ahead by more than 5 HP, the dodge fire reserve drops to 2600ms without force-shooting. If self HP falls below 60 under the same close real bullet pressure and the target is ahead by at least 5 HP, combat exits early via `combat-hp-disadvantage-leave` instead of waiting for the old <50 HP threshold. Offline replay of the Wen frames showed 502 close-pressure fire-window frames, 250 previously suppressed reserve-for-dodge frames that would now fire, and an earlier exit at 55/61 instead of 49/61. Self-tests and static verifier cover the new pressure fire/exit invariants.
- `bootstrap-0.4.143` increases the fixed relogin cooldown after `stamina-budget-coin-leave` from 5 minutes to 30 minutes (`staminaBudgetReloginDelayMs = 1800000`) because 2026-06-13 logs showed many repeated 1h-stamina-budget exits. Combat-log analyzer default reason-specific auditing, README docs, objective-status self-test fixtures, static verifier, source, dist, and manifest are all updated to the 30 minute requirement.
- `bootstrap-0.4.144` adds the requested two-dimensional daily report pipeline. Important kill logs now carry AFK/active player category fields, session summaries include AFK/active kill count/reward buckets, active combat summaries include combat-window stamina spent, and `combat-log-service/daily-summary.js` prints separate login statistics and active-player combat statistics tables. Historical logs without the new fields still render, but combat stamina may remain 0 and unknown kill categories are not force-counted as active. AGENTS.md records that future "日报" requests should use these two dimensions.
- `bootstrap-0.4.145` analyzes the 2026-06-14 `anyi` losses: both active-player fights left on HP disadvantage with enemy HP still 100, target frame movement mostly 0, long no-damage windows, and aim jitter repeatedly widened to 0.14rad. Stationary/zero-current-speed targets now enter `steady` aim after 6s with no damage instead of continuing jitter widening, logs expose `aimTarget.steady`, and a guarded steady-aim fire window lowers dodge reserve to 3000ms only when self HP is at least 75 and the target lead is no more than 15 HP. Static verifier and self-tests cover the new steady-aim invariant.
- `bootstrap-0.4.149` fixes the same-target combat/pursuit conflict from the latest exit record: pursuit timers reset while the current combat action is against the same enemy, and pursuit summaries expose `combatSuppressed`. It also makes engaged combat target switching require a near/soon incoming bullet instead of distant synthetic pressure, and adds a guarded long no-damage active-duel fire window so high-HP non-losing fights can resume reserve-band pressure without force-shooting or weakening low-HP exits. Static verifier and self-tests cover same-target pursuit suppression, stricter target switching, and no-damage duel fire discipline.
- `bootstrap-0.4.150` starts the broader flexible combat architecture: `combatTrendState` centralizes high-HP, close-pressure, steady-aim, and long no-damage duel stances for the shooting plan and future movement/target/exit reuse; runtime native WebSocket ticks now use `combatNativeTickMinMs=80` only while combat is active or recently active, leaving ordinary tick cadence at 120ms. Static verifier and self-tests now require the trend layer, combat-only tick interval, and the new stance/tick cases.
- `bootstrap-0.4.155` analyzes fresh 2026-06-14 `.150` disadvantage fights. The latest xmsthc/Pyro losses had 70%-88% server-position stall, 25s+ no-damage windows, real bullet pressure, and little/no target damage before HP disadvantage exits. Combat now triggers guarded `combat-hp-disadvantage-leave` when server position is stalled, a real incoming bullet is present, no target damage has landed for at least 25s, and the target leads by at least 5 HP; the exit summary/log carries `serverStallNoDamage` evidence. Offline replay estimates earlier exits for three latest disadvantage samples by roughly 20.7s, 15.5s, and 11.7s while leaving the shorter nullsnow sample unchanged. Static verifier and self-tests cover the new invariant. Current `.155` has static validation only; fresh live objective evidence is still missing.
- `bootstrap-0.4.157` replaces the `.156` wait-to-switch precision idea with behavior-driven combat aim strategy. `combatAimDynamicStrategyState` switches immediately to `live-precision` when native/live target coordinates diverge from the selected decision target, when server-position stall makes live aim more trustworthy, or when live target movement is mostly radial; long no-damage precision remains only as a fallback. Combat logs expose `aimTarget.strategy`, `strategyReason`, `sourceDivergenceCm`, live distance, server-stall, radial, and fallback fields. `combat-log-service/replay-combat.js` is the standalone offline replay tool; the xmsthc reference replay (`2026-06-14/-_-_-_-.jsonl` lines `12167-12351`) shows old logged aim `0/45` estimated hits, dynamic strategy `14/45`, and `9/39` before the grace exit. `npm test` in `combat-log-service` now includes replay self-test coverage.
- `bootstrap-0.4.158` fixes post-exit UI/motion residue after successful leave. Leave success/HTTP 403/exit confirmation now clears target overlay, last target, held opportunity state, stale-coin escape state, and publishes a targetless post-exit wait decision. Confirmed pending-exit waits no longer carry the old target/combat cover. Native motion cleanup also clears `lastNonZeroVel`, `lastInputAt`, and refreshes `lastStopAt`; an 8s post-exit motion lock blocks stale ordinary actions from sending movement before hold/no-self state settles. Static verifier covers the new invariants.
- `bootstrap-0.4.159` prevents non-full-HP ordinary Active enemies from dropping the bot into avoidance/recovery flee. Ordinary avoidance now only treats invulnerable players as flee threats, recovery returns combat actions for non-invulnerable defensive targets, non-full defensive target classification can use the reengage range, active engaged targets keep the out-of-range grace for the combat stick window, and combat engagement is remembered even on attack frames where shooting is throttled. Static verifier and self-tests cover the new no-flee and invulnerable-still-flee invariants.
- `bootstrap-0.4.160` lets fleeing combat targets go instead of chasing them until long no-damage exits. A receding combat target outside `combatAttackRange` clears the current engagement, enters a 15s retreat-ignore window, and returns a targetless wait instead of `combat-reengage` or server-stall no-damage leave. A receding target at the outer edge of range suppresses shooting with `target-retreating-edge` so extreme-range kiting does not waste stamina. Real incoming bullets can still reselect an ignored shooter for defense, but the retreating-target disengage/fire-suppression rules still apply. Static verifier and self-tests cover these invariants.
- `bootstrap-0.4.161` analyzes the 2026-06-15 xmsthc and 菈菲爾 battle records as a general coordinate-authority problem, not a named-opponent rule: under server-position stall or long no-damage pressure, opponents can appear to exploit divergence between native/live coordinates and fresh snapshot/server coordinates. Combat now resolves a fresh snapshot/server aim source, switches to `snapshot-authority` when it diverges from native/live by the configured threshold under pressure, and suppresses fire with `authority-target-out-of-range` when the authoritative target is outside attack range. Logs expose snapshot/authority evidence. Replay self-tests cover the 2026-06-14 xmsthc reference plus the new 2026-06-15 xmsthc (`3->57` estimated hits) and 菈菲爾 (`5->73`) authority-divergence fights.
- `bootstrap-0.4.162` analyzes the newer 2026-06-15 `.160/.161` xmsthc windows where real incoming bullets and large native/snapshot coordinate divergence caused wasted shots before server-stall/no-damage pressure fully took over. Snapshot authority can now activate from real bullet pressure from the current target, so a divergent authoritative target inside range is aimed precisely and one outside range suppresses fire immediately instead of spending stamina at stale native coordinates. Replay of the `.161` 05:37 window shows pressure authority would keep the same 5 estimated in-range hits while suppressing 47 old shots with `suppressedLoggedHits=0`; the earlier `.160` 08:37 window keeps the dynamic aim improvement (`3/92` logged hits to `30/62` pressure-authority hits) while suppressing 30 no-hit out-of-range shots. Static verifier and replay self-tests cover the new pressure-authority invariant.
- `bootstrap-0.4.163` fixes the post-manual-login enemy-hold stall seen in the UI: when a stale enemy relogin hold remains but the page has recovered to an alive self entity and an online native WebSocket, the bot clears the enemy hold/persistent state/login suppress and resumes normal strategy instead of continuing `enemy-leave-wait`. Manual relogin hold clearing now also resets stale enemy/offline wait-duration fields. Static verifier checks source/dist for the recovery clear path and stale-duration cleanup.
- `bootstrap-0.4.164` fixes important daily combat-summary HP logging. `updateImportantCombatHp` now parses existing start/min/max fields with `importantHpValue`, so missing `null` values no longer behave like numeric zero; the first valid observed HP sample backfills start/min/max and deltas are computed from that real start value. Self HP sampling now uses `knownHpValue(self)` so aliases such as `knownHp` are accepted. Static verifier checks source/dist for these logging invariants.
- `bootstrap-0.4.165` makes invulnerable-target avoidance a global safety movement instead of a recovery-specific branch. Nearby invulnerable targets now use `avoid-invulnerable-target`; important combat logging does not immediately close combat summaries on safety-avoidance reasons, and daily active-combat statistics filter both the new reason and legacy `recovery-avoid-humans` closures out of the combat table. Bot self-tests cover full-HP nearby invulnerable avoidance, daily self-tests cover safety-avoidance filtering, and static verifier checks source/dist plus daily summary wording.
- `bootstrap-0.4.166` folds target-loss endings into `敌方逃离` in daily active-combat reports. `战后恢复` and `交火停止` are no longer visible combat results: recovery waits are treated as a post-combat observation period in important logging, and if the target does not return before the post buffer expires the summary closes as `post-combat-timeout`, rendered as enemy flee/target disappeared. Daily self-tests cover recovery-wait and generic timeout folding; static verifier rejects reintroducing the old visible result labels.
- `bootstrap-0.4.167` analyzes the 2026-06-16 mango/Vekotov logs where native/live target coordinates could stay inside range while fresh snapshot/server coordinates had already diverged outside `combatAttackRange`, causing early stale-direction shots before server-stall/no-damage authority activated. Fresh divergent snapshot/server targets outside attack range now suppress fire immediately with `authority-target-out-of-range`, and the aim target logged during suppression points at the authoritative snapshot/server coordinate instead of the stale native coordinate. Replay of the mango window suppresses 6 stale no-hit shots with `suppressedLoggedHits=0`; the early Vekotov window suppresses 1 no-hit stale shot. Bot self-tests and replay self-tests cover the immediate out-of-range authority guard.
- `bootstrap-0.4.169` removes snapshot authority from live combat: target, aim, and fire now use native/realtime visible state only because the visible range is already larger than attack range, so combat no longer needs snapshot parsing. Ordinary profit now also prefers visible/native coins and visible AFK targets first, with snapshot data only as fallback when no visible/realtime profit exists. Replay/self-tests still keep the visible-profit ordering and live/native combat behavior covered. `combat-log-service/replay-combat.js` loads selected JSONL line ranges with streaming reads, so replay self-tests can cover the large 2026-06-16 combat log without hitting Node's maximum string length.
- `6b2f02c` tightens daily active-combat result wording into mutually exclusive top-level outcomes. Visible results now start with exactly one of `胜利`, `失败`, `我方主动退出`, `敌方逃离`, or `目标切换`; details after the colon are evidence within that category, not another competing result. Daily self-tests cover HP-disadvantage, low-HP, critical-HP, relogin-wait exit, enemy-flee, and target-switch labels.
- Tampermonkey `0.4.51` / extension `0.1.30` make the bootstrap watchdog probe a stale-but-running bot with `bot.step('bootstrap-watchdog-stale-probe')` before reinstalling cached source. This prevents repeated same-version hot replacements when the page timer is delayed but the bot can still tick on demand; true missing/stale/mismatched states still fall through to cached reinstall.
- Daily report stamina columns are displayed as unitless rounded second-scale numbers; do not append `s` or another unit in human daily report tables.
- Daily active-combat result labels now use mutually exclusive top-level outcomes: `胜利`, `失败`, `我方主动退出`, `敌方逃离`, or `目标切换`. Details after the colon explain the reason or evidence inside that outcome. Enemy retreat evidence from `combat-target-retreating` is rendered as `敌方逃离：目标脱离交火范围`; generic target loss, target exit, teleport, or post-combat timeout is rendered as `敌方逃离：目标消失或脱离交火范围`; invulnerable-target safety avoidance is not a combat state and is excluded from active-combat statistics, including legacy `recovery-avoid-humans` summaries.
- `bootstrap-0.4.170` rebuilds the remote bot from the live-first `.169` combat logic so the manifest/version advances after the 2026-06-16 combat-optimization handoff. Runtime behavior remains the `.169` native/realtime combat target/aim/fire and visible-profit-first logic; this release publishes that state under a fresh remote version after replay-loader maintenance.
- `bootstrap-0.4.171` moves runtime movement/shoot command dispatch to direct sends on the existing native page WebSocket. The bot still lets the page own the WebSocket lifecycle and native state, but `sendNativeVelocity` now prefers `native.ws.send('vel ...')` over the page `sendVelocity()` wrapper, repeats movement commands every 50ms for a short 220ms hold, repeats zero-velocity stops three times, and sends shots as direct `shoot ...` messages before falling back to page helpers. Expected realtime benefit: direct sends remove wrapper/key-computation scheduling overhead and repeat at the 50ms server tick instead of the page's non-forced 100ms gate, so sustained commands should be accepted earlier/more continuously; they cannot remove server tick/network/render-delay latency, and combat target/aim/fire decisions remain native/realtime-visible only. Static verifier now checks source/dist for direct native WebSocket movement/shooting and the new repeat constants.
- `bootstrap-0.4.172` implements the first P0 recommendation from `docs/combat-intelligence-improvements.md`: moving-target combat aim now uses a closed-form quadratic intercept solver with measured bullet speed, 2-tick render-delay compensation, bullet range/max-flight guards, confidence-weighted small spread, and jitter fallback only when no valid intercept exists. Aim logs expose intercept flight time, lead distance, and confidence. The review's P0/P1/P2 direction is generally reasonable: physical intercept and bullet-threat modeling are high-ROI and can be validated offline; continuous movement remains deferred because it requires explicit CDP/live validation that arbitrary float `vel dx dy` is accepted. This release changes combat behavior but is not based on a specific battle record, so no battle-record replay was required; existing combat replay self-tests and bot/static checks passed.
- `bootstrap-0.4.173` implements the next compatible combat-intelligence improvement after CDP validation: native `vel` remains 8-direction quantized, so continuous float movement is not shipped. Real incoming bullets are now collected into a multi-bullet threat field and the bot scores only the 8 legal integer movement directions using closest-point-of-approach, direct-hit count, and time-to-impact. The selected strafe keeps `dx/dy` in `-1/0/1`, while logs expose `incomingBullet.threatCount` and `combatState.strafe.threatField` for live review. This release changes general combat movement behavior but is not based on a specific battle record, so no battle-record replay was required; bot self-tests include the 8-direction multi-bullet field and existing combat replay self-tests remain unchanged.
- `bootstrap-0.4.174` implements the P1-compatible combat-intelligence follow-up: the engaged target now stores a rolling motion/HP sample window, derives a lightweight opponent profile from lateral flips, velocity stability, and kiting/receding behavior, scales intercept confidence from that profile, throttles far low-confidence moving-target fire to a 520ms point-fire cadence, and exits losing active exchanges early with `tradeEstimate` evidence when recent self damage predicts death before kill. Logs expose aim confidence, opponent profile, and trade-estimate context. This release changes general combat behavior but is not based on a specific battle record, so no battle-record replay was required; bot self-tests cover low-confidence fire gating and trade-estimate exits.
- `bootstrap-0.4.175` changed direct WebSocket movement to avoid updating page local `keys` / predicted movement state before sending. Follow-up review found that this could change the self-position cadence consumed by combat distance, dodge, spacing, and exit decisions, so it should not remain the default action path.
- `bootstrap-0.4.176` restores safe default direct movement semantics: nonzero direct `vel` sends again synchronize page local `keys` / predicted movement state by default before the direct WebSocket send, preserving combat/action input semantics while retaining the direct transport and repeat cadence from `.171`. An optional `directWsServerMarkerProbe` diagnostic flag can defer that sync for server-marker investigation, but it is off by default and exposed in `status().control`. Console status logs now default to 30s instead of 1s and can be disabled with `statusEvery=0`; Tampermonkey `0.4.52` / extension `0.1.31` migrate the old 1000ms default to 30000ms. `combat-log-service` now writes split JSONL by kind (`combat`, `important`, `audit`, `misc`) under each day directory, while analysis/daily tools recurse through split files and `--flat-files` preserves the legacy layout. Shared exit-summary helper functions moved to `src/shared/exit-summary.js`; generated remote output remains a single script.
- `bootstrap-0.4.177` continues source modularization without behavior changes: runtime JSON/clone/log-id utilities, display formatting helpers, preserved browser state initialization, and runtime default configuration now live under `src/shared/`, while `grasp-rat-bot.js --print-source` still inlines those helpers into one generated browser script. Static verification now checks both sides: source imports shared modules, generated `dist/grasp-rat-remote-bot.js` has no `require('./src/shared/...')`, and the inlined helpers/defaults remain present.
- Follow-up source-only modularization after `.180`: the large Node-only `runSelfTest()` block now lives in `src/node/run-self-test.js`, while `grasp-rat-bot.js` keeps the CLI/CDP entrypoint and browser runtime generation. Static verification now treats the extracted self-test module as the canonical location for bot strategy regression cases instead of requiring those case definitions to remain inline in the main source file. Generated remote output remains single-file and behavior-equivalent.
- Follow-up source-only modularization after `.180`: browser UI runtime fragments for target overlay and status panel now live in `src/browser/target-overlay-source.js` and `src/browser/status-panel-source.js`. `grasp-rat-bot.js` injects those source factories into the generated runtime, while `verify-objective-build.js` now checks both the extracted source modules and the final single-file `dist/grasp-rat-remote-bot.js`.
- Follow-up source-only modularization after `.180`: the browser-side combat-log and exit-audit runtime block now lives in `src/browser/combat-log-source.js`. The main source keeps only shared helper injection plus `${combatLogSource(...)}` wiring, while static verification now treats the extracted combat-log source factory as part of the canonical source runtime and still requires the generated remote output to stay single-file.
- Follow-up source-only modularization after `.180`: the browser-side important-log, session-summary, and important-combat logging runtime block now lives in `src/browser/important-log-source.js`. The main source keeps only the browser runtime wiring, while static verification now treats the extracted important-log source factory as part of the canonical source runtime and still requires the generated remote output to stay single-file.
- Follow-up source-only modularization after `.180`: browser-side reload, Cloudflare-error handling, login/snapshot gate, post-login zoom scheduling, pause sync, and relogin-hold helper code now lives in `src/browser/control-login-source.js`. The generated remote output remains one single browser script, and static verification now checks the control-login source factory plus its injected stamina-window helper.
- Follow-up source-only modularization after `.180`: browser-side native state/control, realtime entity/coin/bullet normalization, snapshot freshness helpers, server-position stall summaries, self/session summaries, and bounded-list helper code now live in `src/browser/native-state-source.js` and `src/browser/runtime-summary-source.js`. `grasp-rat-bot.js` is below 10k lines, and static verification treats the new source factories as part of the canonical runtime while still requiring generated remote output to stay single-file.
- `bootstrap-0.4.178` analyzes fresh 2026-06-17 `.177` battle records. The new direct-control/intercept stack performed well against many mango fights, but xmsthc and Motor samples showed real incoming bullet pressure plus high native/logged aim divergence where logged/intercept aim estimated only `0/49` and `1/48` live-target hits. Combat aim now switches moving targets to live/native precision under real incoming bullet pressure before applying intercept lead, logging `strategyReason=real-bullet-pressure` and `aimTarget.realBulletPrecision`. Replay shows xmsthc improving to `28/49` estimated hits and Motor to `25/48`. If a non-invulnerable Active threat is inside `combatAttackRange` but is temporarily unavailable as a combat target, such as during retreat-ignore, the bot now waits with `combat-active-threat-wait` instead of taking foot/visible coins in the danger window. Bot self-tests, replay self-tests, and static verifier cover the new behavior.
- `bootstrap-0.4.179` delays non-critical disadvantage exits so one early bad read does not immediately abandon a fight. High-HP static HP-gap and trade-estimate disadvantages now record a target-bound observation and require the same target to stay disadvantaged for `combatDisadvantageConfirmMs = 2500`, at least `combatDisadvantageMinEngageMs = 3500` of engagement, and at least `combatDisadvantageMinSamples = 4` before `combat-hp-disadvantage-leave`. Critical HP, low-HP disadvantage, close-pressure disadvantage, and server-stall no-damage exits still leave immediately for survival. The observation clears on target retreat, combat engagement clear, target switch, or no-longer-disadvantaged frames, and is preserved across hot updates. Bot self-tests and static verifier cover the delayed and immediate-exit invariants.
- `bootstrap-0.4.180` analyzes fresh `.179` xmsthc/osdjf logs where combat decisions were marked `live` but aimed at stale `nativeState.entities` coordinates while the visible/logged current target position had already diverged by about 65-212m. Runtime realtime entity resolution now merges render-visible sources (`getRenderEntities`, `state.getRenderEntities()`, `renderEntities`, and `render_entities`) with native state entities and prefers visual/render coordinates for combat target, aim, and fire without reintroducing snapshot combat authority. Combat log entity merging now preserves realtime/native/render coordinates when later snapshot records describe the same player. Offline replay of the referenced windows showed the stale logged aim improving from `1/48` to `15/48` for xmsthc, `1/55` to `22/55` for the second xmsthc window, and `0/32` to `6/32` for osdjf when using the current visible target point.
- `bootstrap-0.4.181` analyzes the fresh 2026-06-18 Noah_Z, lockcc, and tyshine losses. Those fights were not mainly about named opponents or one-off thresholds: the shared failure mode was lateral live targets under server-stall or pressure where `.180` still preserved current visible coordinates but often fell back to `live-precision`, throwing away the physical intercept lead that those targets still required. Combat aim now separates live source resolution from shot-solution choice, adds `live-intercept` as an explicit dynamic strategy branch, keeps quadratic intercept against live/native/render target coordinates when movement is mostly lateral under real-bullet pressure or server-position stall, and leaves radial targets on live precision. Offline replay of the referenced windows improved dynamic-strategy estimated hits from `0/56` to `5/56` for Noah_Z, `1/75` to `8/75` for lockcc, and `14/252` to `36/252` for the long tyshine fight; the pure `live-intercept` scenario reached `7/56`, `7/75`, and `38/252`, confirming the core direction even where other runtime constraints still limit the full carry-through.
- `bootstrap-0.4.182` analyzes the 2026-06-18 BeingS high-HP exit window. The root issue was not just one named opponent: short recent trade windows can show `0` target damage under latency or temporary aim mismatch even while the remaining HP horizon is still safe, so a zero-damage `tradeEstimate` must not force `combat-hp-disadvantage-leave` by itself at high HP. Trade-estimate exits now require pure zero-damage windows to be meaningfully unsafe first: either self HP has dropped into a lower remaining-HP band or projected death is within 30s. Offline replay of the referenced BeingS window (`20260617160308-self-28886-vs-BeingS.jsonl` lines `520-594`) still shows the old logged aim at `0/27` estimated hits while current dynamic/live-intercept reaches `7/27` in the same window, confirming that the earlier `85 HP vs 61 HP` leave was too aggressive once aim latency/visible-target uncertainty is accounted for. Bot self-tests and replay self-tests cover the guarded zero-damage trade-estimate case.
- `bootstrap-0.4.183` keeps combat entry at `combatAttackRange = 14500` and adds `combatDisengageRange = 17000` hysteresis. New combat targets do not enter outside 145m; an already engaged target from 145m to 170m remains in combat state, but the bot holds position/recovery instead of chasing with `combat-reengage`; beyond 170m it clears combat state without installing a retreat-ignore cooldown, so the target can re-enter normally if it comes back inside 145m. Bot self-tests and static verifier cover outside-range non-entry, out-of-range combat hold, disengage-range clearing, and wait-frame engagement refresh.
- `bootstrap-0.4.184` analyzes the 2026-06-19 mango/osdjf retreating-edge logs. The shared issue was not a named opponent: healthy-target edge suppression saved stamina, but low-HP targets at 138-145m could leave while the bot had high HP and no finishing shots. Combat now adds `combat-finish-pressure` only for low-HP retreating edge targets while self HP is high, preserving 145m entry/out-of-range hold and 170m disengage. Replay now reports hypothetical finish-pressure shots; the 04:59:19 mango window improved from old `0` shots to `2/10` estimated finish-pressure hits, and the 03:37 osdjf window showed `1/4` finish-pressure estimated hits. Bot self-tests and static verifier cover ordinary retreating-edge suppression plus the low-HP finish-pressure exception.
- Follow-up analyzer maintenance after `.174`: `combat-log-service/analyze-logs.js` now streams JSONL in chunks instead of reading each file into a single string, so `node scripts/objective-status.js` can audit the current large local log directory without hitting Node's single-string size limit. The current `.174` objective status now completes and reports missing live samples (`entries=3/121863`, no exit/Active/HP-disadvantage evidence) instead of crashing.
- `bootstrap-0.4.185` adds native/realtime visible coin micro-route planning based on the Gold Runner reference analysis, without adding manual/mobile/display changes. A route is a bounded coin opportunity scored by total visible route reward over full estimated route stamina, uses the first coin as the movement target, stores route ids/value/stamina/leg count in action metadata, requires at least three safe visible coins, and safety-checks each route leg against active/invulnerable coin danger. The route score participates in the existing opportunity pool, so high Drop AFK/player targets still compete by stamina ROI and can beat weak routes.
- `bootstrap-0.4.193` analyzes the later 2026-06-20 `蕉灼の仓鼠` disadvantage window where the target drifted just outside `combatAttackRange` at roughly `14565-14728cm` but was still contesting with real target-owned bullets. The shared issue was broader than a named opponent: the bot already kept engaged targets until `combatDisengageRange`, but the ordinary out-of-range hold branch was too absolute for slight overshoot windows, so it could sit still, accumulate `noDamageMs`, and give away the trade while the opponent kept pressure. Combat now adds a guarded `combat-out-of-range-reengage` branch up to `15000cm`: only engaged/reengage targets qualify, no shots are fired out of range, self HP must stay at least `60`, the HP gap cannot already be worse than `10`, and the trigger still requires either real target-owned bullet pressure or very fresh just-left-range contact without a retreat signal. Ordinary retreat-only or unhealthy states still keep `combat-out-of-range-hold`. Offline replay on `logs/2026-06-20/20260620083709-self-28886-vs-81992.jsonl` lines `1-263` improved the logged baseline `dynamic strategy vs live target` from `4/38` hits to `44/45` for the new `out-of-range reengage dynamic vs live target` scenario, with active start at line `209` (`14565cm`), simulated end distance `11498cm`, and simulated approach `3230cm`.
- `bootstrap-0.4.194` adds actual battle profit reporting for daily summaries. The daily report now has an `实际战斗收益统计` section that lists only confirmed kills and failure/HP-disadvantage exits, excludes enemy-flee and target-switch rows, and separates actual picked rewards from confirmed unpicked drops and failed unearned drops. Important kill logs now include optional non-combat battle-window/stamina fields for future AFK and ordinary Drop kills; older standalone kill logs still contribute kill/reward rows but may show unknown fight stamina.
- `bootstrap-0.4.195` analyzes the 2026-06-21 `mango` fight (`combat-log-service/logs/2026-06-21/20260621011341-self-28886-vs-mango.jsonl`) where the target was pushed down to `HP 1`, self stayed `HP 100`, but late fight pressure stalled because ordinary dodge-reserve logic kept suppressing shots while there was no meaningful incoming bullet threat. The fix is a guarded `finish-low-threat` firing window: self HP must stay high, target HP must be very low, the HP gap must not already be unfavorable, the target must still be within a bounded finishing range, and real incoming bullet pressure must be absent. Inside that window, combat keeps burst fire pressure while lowering the effective dodge reserve from the normal `3000ms` band to `1800ms` instead of removing reserve discipline globally. Offline replay on the referenced fight improved `finish-low-threat burst vs live target` to `192/498` estimated hits from the logged `178/456`, adding `42` extra simulated shots without introducing bullet-risk conditions. Bot self-test coverage adds a dedicated low-threat finish case, and replay self-test coverage now includes this 2026-06-21 mango window.
- `bootstrap-0.4.196` reviews the broader 2026-06-21 `mango` high-stamina fights. The `.195` window helped only once the target was at `HP <= 10`, but several 200-400+ stamina kills had already spent most of the fight before that point, and the 06:57 fight left mango at `HP 1` for nearly 16 minutes before a stamina-budget exit. The low-threat finish window now starts at `target HP <= 55` while keeping the same high-self-HP, no-real-bullet, bounded-range, and non-losing-HP-gap guards, still only lowering the effective dodge reserve to `1800ms` inside that low-risk window. Offline replay improves the 06:43 high-cost kill from logged `95/486` estimated hits to `164/612` with `126` extra simulated low-threat shots, improves the 09:13 fight from `178/456` to `334/774`, and improves the 06:57 prolonged window from `133/2209` to `250/4878`, confirming the change addresses the high-stamina no-threat finishing pattern rather than only the final `HP 1` tail.
- `bootstrap-0.4.197` analyzes fresh 2026-06-21 pressure fights after the mango review. The shared issue was broader than a named target: when the opponent is still applying real target-owned bullet pressure, the old close-pressure fire window was too narrow at 80m and the bot could keep reserving fire while losing the trade; in one biliee fight, extra pressure shots did not improve estimated hits and the better answer was earlier stop-loss. Combat pressure fire now keys specifically on target-owned real bullets, extends the guarded pressure range to 145m with self HP at least 60 and HP gap no worse than 10, and keeps the existing dodge reserve floor. A new sustained pressure exit fires when target-owned real bullet pressure continues, self HP is at most 70, target HP leads by at least 10, no target damage has landed for 10s, and distance is within 145m. Replay improved the 07:56 biliee pressure window from logged `3/56` to `11/72` with 16 extra pressure shots, improved the 09:21 biliee window from `5/112` to `38/192`, improved the ahy6a pressure sample from `9/76` to `13/90`, and in the 10:15 biliee stop-loss sample triggers exit at line `664` (`67/79`, `11596ms` no damage), about 13s before the logged end.
- `bootstrap-0.4.198` analyzes 2026-06-22 actual battle profit rows. Predictable passive coin-runners such as mango were still expensive because the previous passive-runner branch only closed to 75m, leaving long bullet flight time against coin-pickup direction changes; passive-runner close now uses the 45m minimum spacing band while keeping the high-self-HP, no-real-bullet, no-recent-damage, non-firing, moving, reward-positive gates. Replay on `20260621201149-self-28886-vs-mango.jsonl` improves passive-runner estimated hits from logged `78/664` to `131/660` with `4451cm` simulated approach. For losing target-owned pressure trades, sustained pressure no-damage stop-loss now triggers at self HP `<=80` instead of `<=70`, still requiring target-owned real bullets, target HP lead at least 10, no target damage for 10s, and distance within 145m. Replay on `20260622022724-self-28886-vs-biliee.jsonl` triggers at line `436` (`79/91`, `12319ms` no damage), saving about `18528ms` before the logged end.
- `bootstrap-0.4.199` reworks the 2026-06-22 pressure-loss follow-up around the user's preference to win recoverable disadvantage fights before leaving. The `.198` 80HP sustained-pressure stop-loss caused the 10:27 biliee sample to leave at `79/91`; this release restores the sustained-pressure stop-loss gate to `<=70HP` and instead widens only target-owned pressure out-of-range reengage from the ordinary `10` HP-gap guard to `20`. The intent is to press still-fighting targets that just crossed the 145m attack boundary back into range without firing out of range, while keeping ordinary fresh-contact reengage conservative. Offline replay on `20260622022724-self-28886-vs-biliee.jsonl` now shows `out-of-range reengage dynamic vs live target` at `23/28` estimated hits, active from line `467` at `14510cm`, with `5675cm` simulated approach and no sustained-pressure exit at `79HP`.
- `bootstrap-0.4.200` analyzes 2026-06-23 high-cost `mango` kills and pressure-loss fights. Passive runner fights were spending many frames in `reserve-for-dodge` despite high self HP and no real incoming bullet pressure, so eligible passive-runner combat now lowers the movement shooting reserve to `1800ms` while keeping the hard stamina floor. Target-owned bullet pressure is split into two guarded paths: if self is leading by at least 5 HP and the target is already at `<=75HP`, a winning-pressure finish window lowers reserve to `1800ms` after 6s without target damage; if the target is still healthy (`>=75HP`) and leads by at least 10 HP, sustained pressure no-damage stop-loss now fires at self `<=80HP`. Offline replay on 2026-06-23 `mango` samples improved passive-runner reserve-fire estimated hits from logged `64/261` to `68/331` with 70 extra shots, and from logged `81/236` to `98/296` with 60 extra shots. Replay on the 2026-06-23 `Noah_Z` low-target pressure sample added 23 winning-pressure shots and improved dynamic estimated hits from `20/144` to `25/167`; replay on the `biliee` low-target pressure sample added 3 shots and improved `3/52` to `3/55` with no early stop-loss because the target was already low.
- `bootstrap-0.4.201` tightens passive-runner activation without rolling back the profitable mango path. The issue in recent live losses was not the 45m close-in or 1800ms reserve direction by itself, but that ordinary defensive fights could inherit the same branch immediately and keep doing so after the opponent had already shown real bullet ownership. Passive-runner combat now waits `2500ms` before activating, records `combatOriginIntent` / `combatOriginReason` plus passive-runner confirmation state in combat logs, and permanently disables passive-runner reuse for that engagement once the target has been seen owning a real incoming bullet. Offline replay still keeps strong mango upside on `20260621201149-self-28886-vs-mango.jsonl`, where delaying activation to the new window preserves `115/610` estimated passive-runner hits versus logged `78/664`, while the 2026-06-24 `biliee` loss no longer gets an opening passive-runner window after the target-owned bullet evidence appears and the earlier passive-runner sample collapses from `0/15` to `0/0`. The 2026-06-22 `BennBo` sample similarly drops the old marginal passive-runner branch from `2/23` to `0/0`, confirming the branch is now reserved for sustained non-return-fire runner fights instead of brief live-fighter openings.
- `bootstrap-0.4.202` keeps coin route selection behavior unchanged but exposes per-coin route points in selected `best-opportunity-coin-route` actions and target metadata. The target overlay now connects self to each selected route coin in order, so live review can see the full higher-ROI route instead of only the first pickup target. Bot self-tests cover route point metadata, and static verification checks both source/dist route metadata plus overlay rendering support.
- `bootstrap-0.4.186` analyzes the 2026-06-20 loss to `蕉灼の仓鼠`. The shared pattern is a long active duel beyond 100m where neither side should have a fundamental range advantage, but continuing the far exchange without target damage lets the opponent's volume and movement pattern win the trade. Combat now starts a far no-damage close-in branch after 6s at `>10000cm`, pressing toward 75m when self HP is at least 60 and the HP gap is not already worse than 10. This branch does not lower the shooting dodge reserve; low-stamina far pressure saves reserve for movement/dodge instead of turning the answer into "fire more bullets." Offline replay of `logs/2026-06-20/20260619165028-self-28886-vs-.jsonl` lines `1-859` shows the branch starting at line 100 at `11559cm` and simulating an approach from `11773cm` end distance to `2627cm` (`9146cm` closer), while preserving the existing dynamic aim baseline.
- `bootstrap-0.4.187` tightens the `.186` far no-damage close-in branch so it does not chase targets already classified as retreating at the outer edge. Low-HP retreating targets still keep the existing `combat-finish-pressure` exception, but ordinary retreating targets now block far no-damage close-in movement and keep `target-retreating-edge` fire suppression. Bot self-tests now cover the 140m retreating-edge case where far no-damage pressure would otherwise try to keep closing.
- `bootstrap-0.4.188` analyzes the latest 2026-06-20 mango fights where the target was a predictable Active coin-runner that did not fire back. The low hit rate was caused by treating the target as synthetic `target-pressure`, which forced tangent dodge and kept the fight at the outer range, plus server-stall/local-prediction mismatch where some frames needed deterministic live intercept instead of precision/current-point aim. Passive coin-runner combat is now intent-gated (`defensive`, `engaged`, `profit`, `reengage`), high-self-HP, no-recent-self-damage, no-real-bullet, non-firing, moving, and reward-positive; it clears synthetic `target-pressure`, prefers zero-spread live intercept after coordinate-divergence protection, and closes toward 75m only when higher-priority finish/retreat/far-no-damage/ordinary pressure-close branches are inactive. Offline replay on `20260619180524-self-28886-vs-mango.jsonl` improved dynamic estimated hits from logged `29/112` to `35/112` and moved first estimated hit from line 310 to line 203; replay on `20260619180740-self-28886-vs-mango.jsonl` showed the passive-runner close/intercept scenario improving from logged `18/93` to `21/93` with `15153cm` simulated approach. Manual pause now removes the target overlay immediately, and overlay rendering suppresses paused decisions.
- `bootstrap-0.4.189` analyzes the 2026-06-20 login stall where the bot was logged out with panel stamina around `10/10 1135/3000 12658/20000` but still showed a 14h daily relogin hold. The log root cause is `important-20260620.jsonl` lines 27-29: after a real session ended with `stamina1dLastRemaining=12658674`, the next login saw a startup placeholder self named `User 28886` with `stamina5s/1h/1d=0/0/0`, immediately wrote `stamina exhausted`, and persisted a 1d reset hold to the next UTC+8 day. Runtime now treats all-zero long-window stamina immediately after login as an unsettled sample for `staminaExhaustionPostLoginGraceMs=15000` instead of exiting, and stale offline stamina holds are cleared when preserved self/session stamina evidence contradicts the recorded long-window exhaustion. The release hash is `7558d23a5987821535212788a8c39a8fe639dc510add2db9086bfa1ebb398358`.
- `bootstrap-0.4.190` analyzes the new 2026-06-20 `蕉灼の仓鼠` fight and follow-up Firefox sample. The long fight was close (`self 100 -> 55`, target `100 -> 49`) but ended as disengaged/post-combat-timeout after the target crossed 145m and eventually 156m. The shared issue was not a named opponent: ordinary retreating-edge suppression is still useful against pure escape, but a target that is retreating at the outer edge while still producing real target-owned bullet pressure is still contesting the fight and should not block all far no-damage close-in pressure. Combat now adds a guarded `retreating-fighter-close` exception under real target bullet pressure, no target damage for at least 6s, self HP at least 60, and HP gap no worse than 10; ordinary retreating targets still keep `target-retreating-edge` suppression. It also adds `combat-finish-reengage` for the narrow 145-160m low-HP finish window: target HP at most 55, self HP at least 55, self not lower HP than target, and target damaged within 10s; this moves back into range without firing out of range. Offline replay on `logs/2026-06-20/20260620022230-self-28886-vs-.jsonl` lines `1-4287` kept the old dynamic baseline at `18/284`, while the far no-damage close simulation improved to `50/275`, first estimated hit line `208`, active start line `146`, and simulated approach `3613cm`. The Firefox sample remains governed by the existing HP-disadvantage exit path.
- `bootstrap-0.4.192` fixes the no-self fake-exit/session-mismatch trap seen when the sidebar/login UI drops to an apparent logged-out state and chat shows `left user <id>`, but the native page websocket/game session is still alive. Local pending-exit confirmation now rejects that evidence when authoritative native session signals still show an active session, so the bot no longer treats `token cleared + left user + self missing` as confirmed exit during a live session mismatch. Separately, when self stays missing and the page shows logged out while native session evidence remains online, the bot now escalates to `session-mismatch-recovery` after a short timeout and immediately forces relogin/rejoin instead of waiting for the ordinary snapshot/login gate path. This is intended to shorten the window where the character is still online in-scene and vulnerable while the UI/control state is inconsistent.
- 2026-06-20 local daily-summary fix: report-side collected-coin normalization now treats explicit `pickedCoins=0` as authoritative, falls back to `coinsGained` only when pickup totals are missing, and recomputes refresh-coin totals from the normalized collected-coin value. This removes placeholder short-session fake `1000币` refresh/total收益 rows from daily reports without changing the remote bot/runtime version.
- `bootstrap-0.4.191` fixes the high-Drop AFK post-kill Drop wait. The previous wait was tied to the last attack timestamp, so bullet travel/render delay could consume the window before the target actually resolved; it also lacked coverage for short-lived residual dead/zero-reward entities. The wait now starts when the recently attacked target is no longer attackable, lasts 1s, ignores already-visible dropped coins, and then lets ordinary profit selection resume. Self-tests cover delayed resolution, residual zero-reward targets, alive targets, unshot disappearances, and expired waits.
- Latest live-validation target is `bootstrap-0.4.243`; any older "current remote" note in the historical summary is superseded. Current `.243` should confirm the embedded panel shows profit metrics as `today(current login)` for login time, stamina spent, coin profit, and kill count; coin profit has no visible `+`; header latency/loss widths stay stable at four-digit latency and `00.00%` loss precision; the log toggle reads `Log`; and after an exit/page refresh the previous exit reason plus 1d remaining stamina still render from preserved state. Continue watching the `.242` relogin-gate simplification: automatic relogin is gated by `登录点安全 12/12` only, the panel should not show `快照接口连通性`, and `/snapshot` errors still reset the login-point safety row. Continue watching the `.241` profit stabilization: recent-action idle players should not be selected as AFK Drop targets for roughly 12s after movement/firing/stamina-use evidence, and an already selected `10+` visible/native coin should not be repeatedly interrupted by a high-Drop AFK target while the coin remains visible, safe, and affordable. Continue watching the `.240` panel/network metrics: combat frames should include `runtime.networkQuality`, and severe frame stalls/loss should produce throttled `network-quality` misc logs when combat logging is enabled, without changing decisions solely because the metrics are observed. Also continue watching that manual sidebar/native login remains available as `立即登录`; after an alive self appears the bot does not exit solely for `login point safety gate` / `登录点安全快照未满足`; no combat-gap exit occurs when only `recentCombatFrameContext` remains true and the current decision is a coin action such as `best-opportunity-coin-route`; and selected visible/native coin disappearance still clears stale `opportunityChoice`/`lastTarget` immediately with `visible-coin-disappeared` while genuine short native/render coin-source gaps still use the missing-held anti-jitter behavior.
- Local combat-log review markers are stored in ignored file `combat-log-service/logs/analyzed-combats.json`. It currently marks reviewed lockcc historical sessions, the positive fugua `.118` sample, the limited fugua low-HP sample with missing lead-up, and the xmsthc/小鱼 `.118` sample that drove `.120`; skip those unless testing a new hypothesis.
- 2026-06-12 log review for the overnight loss: `combat-log-service/logs/2026-06-12/-_-_-_-.jsonl` has a combat-log gap from 01:55:14 to 08:26:22 CST. The last pre-gap state was `.96`, alive/full HP, `death_count=2`, `death_drop_coins=533`. Fresh `.109` logs at 08:26 show `death_count=3`, `death_drop_coins=56`, and new `death_tick=2644244`; using later `globalState.tick` samples estimates the death around 02:17 CST and the preceding active join around 02:12 CST. There is no direct combat frame for the 02:17 fight/exit, but current `.109` leave retries after 08:26 repeatedly show `Unexpected token '<', "<!doctype "... is not valid JSON`, consistent with the leave API receiving an HTML auth/login page instead of JSON while the page/auth flow is unhealthy.
- Follow-up code review confirms a possible login-before-confirmed-exit path: attempted unsafe exits with API errors set only a finite `pending unsafe ...` login suppress, and injury/pursuit/offline exits remember a durable `pendingExit` only on `attempted && !error`; combat exits remember in-memory `pendingExit` on `attempted`, but that state is lost across full page navigation/reload. After the suppress expires, the no-self branch can call `maybeStartAutoLogin`, so a tab can enter the linux.do login flow without a confirmed game exit. Also, while a pending exit exists, `pendingExitSelfState` treats a login-required/control page as confirmed exit evidence, which proves page auth state but not necessarily server-side leave success.
