# Current Strategy Summary


- Survival has priority over ROI:
  - low HP recovery/flee paths remain first-class;
  - combat critical HP and HP disadvantage can trigger leave;
  - 1d long-window stamina exhaustion triggers leave/relogin hold until reset; 1h exhaustion uses a fixed 30 minute relogin delay because 1h stamina appears to recover continuously rather than at hourly reset.
  - after login, an all-zero 5s/1h/1d stamina placeholder is treated as an unsettled sample for a short grace window instead of immediately triggering a 1d reset hold.
  - hostile leave/relogin messages include the enemy summary and the actual wait time only when a reason-specific hold exists.
  - repeated successful exits from the same identified enemy no longer add extra defensive relogin backoff; learned login-point safety is the anti-camping gate.
  - enemy/offline leave reason and target/player context are retained for reporting; only reason-specific waits such as stamina and 403 risk control persist a remaining relogin wait.
  - enemy/offline relogin hold waits are display-only when present; they stop movement and show the persisted reason/countdown, but do not keep sending leave requests.
  - stale enemy/offline relogin holds are cleared once the page has recovered to an alive self entity with an online native WebSocket, so manual or external successful relogin cannot leave the bot stopped in an old wait state. Stale stamina offline holds are also cleared when preserved session/self evidence contradicts the recorded long-window exhaustion.
  - attempted combat/injury/pursuit/offline leave requests establish pending exit even when the transport reports a timeout/HTML error, so exit audit and conservative confirmation continue without adding a defensive relogin timer.
  - stamina-budget leave requests prime a non-offline login suppress immediately after an attempted leave command, so a page/context refresh before exit confirmation does not cause immediate auto-login.
  - attempted unsafe exits from injury, combat disadvantage, pursuit, reconnect churn, server-position stall, or unsafe offline conditions no longer prime a defensive time-based login suppress; every later login attempt is still blocked by the post-exit snapshot connectivity gate and learned login-point safety until those gates pass.
  - confirmed ordinary safe offline exits are marked `safeReloginAllowed` and do not create an offline relogin suppress/hold; unsafe offline exits also avoid defensive time holds unless a reason-specific hold applies. Stamina exits still create the required hold.
  - exit suppress delay selection still takes the maximum of fixed condition delay and required minimum delay for reason-specific holds such as stamina budget and leave HTTP 403 risk control.
  - repeated native WebSocket reconnect churn triggers offline leave immediately instead of resetting the continuous offline timer on every brief reconnect.
  - leave retries have a 10s floor and async completion handling; an unconfirmed exit remains pending, but if self is still alive/in-scene the bot continues ordinary combat/flee control instead of stopping in place.
  - confirmed exit cooldown is entered after completed leave success, leave HTTP 403, or conservative local evidence from all three signals: token cleared, left chat showing `left user <currentUserId>`, and own entity absent from native/local/fresh snapshot state. Login-required/auth pages alone are weak evidence after leave errors.
  - that conservative local evidence is now explicitly rejected when authoritative native session signals still show an active game session while the page token/login UI has already dropped, preventing fake-exit confirmation during a live session mismatch.
  - completed leave success/HTTP 403 and pending-exit confirmation stop native motion immediately before exit audit/log bookkeeping, so stale `keys`/`currentVel`/`lastVel` cannot keep the map moving after self disappears.
  - leave HTTP 403 is treated as exit confirmation plus a risk-control relogin hold of at least 1 hour.
  - WebSocket reconnect churn is treated as an offline exit condition as soon as the rolling reconnect window reaches the threshold, even if the page has just re-opened the socket.
  - combat/recent-combat global sampling outage is also treated as a network fluctuation. If `/snapshot` or `/minimap` refresh records a structured `samplingOutage`, the bot exposes it in status, triggers an immediate tick, stops motion, and uses the offline leave path with reason `global sampling outage`; by default this stays combat-only so ordinary profit flow does not exit on a transient sampling miss.
  - combat/recent-combat tick, tick-reentry, or combat-frame gaps are treated as another network/runtime fluctuation. If the main tick gap or stuck in-progress tick reaches `combatTickGapOfflineMs = 5000` while combat or recent-combat context is present, the bot stops motion and exits immediately with reason `combat tick gap`; queued combat-frame gaps also require live combat context (`previousCombatActive || currentCombatActive || combatLogActive`), so a stale recent combat frame alone cannot exit while the bot is already moving along a coin route.
  - logged-in/no-self reconnect traps are treated as offline exits: reconnect churn exits immediately, and a 30s no-self timeout exits even without churn.
  - if self stays missing while the page looks logged out but the native websocket/session is still authoritatively online, or a fresh `/snapshot` still contains the current alive self, the bot can enter a dedicated `session-mismatch-recovery` path. When the mismatch is otherwise low risk, the first response is now a controlled one-time page refresh recorded under `graspRatSessionMismatchRecovery` for `sessionMismatchRecoveryReloadMaxAgeMs = 120000`; if that marker cannot be stored, the bot waits instead of taking over. If the same user/session mismatch survives the recorded refresh, the existing fast takeover path runs. Fast takeover is allowed only when the live-session mismatch helper proves there is no pending exit, relogin hold, login suppress, exit-reset snapshot gate, reconnect churn, offline-ish WebSocket state, recent offline context, or recent unsafe exit. The takeover bypass may skip the `/snapshot` connectivity streak, but it cannot skip learned login-point safety: `登录点安全 0/12`, missing login point, last danger, or last error still blocks login.
  - unattended relogin must pass `loginSnapshotGate` after the exit reset; any reason-specific cooldown/suppress waits and the 3 consecutive successful `/snapshot` gate must both be satisfied before automatic login clicks. Explicit manual login from the sidebar/native control bypasses these pre-login gates and records `manualLoginBypass` evidence instead of disabling the user control.
  - learned login-point safety is the main anti-camping gate for unattended relogin. After the bot actually starts a login and then sees an alive self, it records that coordinate locally. Future unattended relogins require 12 consecutive safe `/snapshot` samples, about one minute at the 5s refresh cadence, before login can proceed. If the last recorded exit HP is at least `loginPointSafetyHealthyHpThreshold = 80`, the safety radius narrows to `loginPointSafetyHealthyRadius = 20000cm`; otherwise, including unknown last-exit HP, it keeps the conservative `loginPointSafetyRadius = 60000cm`. A player inside the active radius blocks login if they damaged self today, have `Drop >= 2`, are in `Active` mode, moved by at least `500cm` across recent samples, have non-full 5s stamina, or show firing evidence; low-Drop, full-stamina, stationary passive nearby players can remain without permanently blocking automatic relogin.
  - relogin gate observability is explicit: `status().reloginGate` and the embedded script panel show cooldown remaining/total, `/snapshot` connectivity streak out of 3, and login-point safety streak out of 12 before relogin. These rows reuse existing gate state and do not trigger extra sampling.
- Control transport:
  - the page/native WebSocket remains the only game WebSocket; the bot does not create or reconnect a second socket;
  - runtime movement now prefers direct `native.ws.send('vel dx dy')` on that existing socket, with 50ms short-hold repeats for sustained movement and repeated zero-velocity stop commands;
  - nonzero direct movement sends keep the page's local `keys` / predicted movement state synchronized by default, preserving the self-position cadence used by combat distance, dodge, spacing, and exit decisions; the optional `directWsServerMarkerProbe` diagnostic mode can defer that sync for server-marker checks, but it is off by default;
  - runtime shooting now prefers direct `native.ws.send('shoot targetX targetY startX startY')` on the same socket, falling back to native page helpers if direct send fails;
  - runtime network quality is observed but does not yet change combat/profit decisions. The bot estimates an effective latency from movement action confirmation with a native WebSocket state-frame fallback, estimates packet-loss-like state-frame gaps over a rolling window, records movement/attack feedback counters, and exposes the summary in `status().networkQuality` plus combat-log `runtime.networkQuality`.
  - behavior logic remains based on native/realtime visible state, so combat target selection, aim, firing, spacing, exits, and profit priority do not switch back to snapshot/server-coordinate parsing because of the faster transport.
- Cloudflare error pages are detected by the remote bot and bootstrap layers and refresh every 5 seconds while the error page remains; full-page BunkerWeb 403 pages refresh every 10 minutes.
  - Bootstrap panels render with DOM/textContent instead of innerHTML so Trusted Types/CSP protected error pages do not break the panel.
- Tampermonkey and extension panels are embedded into the native left sidebar between Entity Control and Chat. The native title, Runtime Metrics, View Control, minimap dock, and native login button are hidden with `display:none`, and the script immediate-login button appears in the native login button position in Entity Control while the page is logged out or a relogin hold is visible. The button remains `立即登录` and clickable for explicit manual login even when the automatic safety gate is blocked. The injected layout forces `.app` to horizontal flex, `.side` to `min(336px,100vw)`, and `.workspace` back to a relative flex/grid item with `inset:auto`; `.map-shell`/`#world` fill the remaining viewport so tall/narrow native media rules cannot shift the game center or stretch the side panel across the page. During relogin-related states, the panel renders the three gate rows `冷却时间`, `快照接口连通性`, and `登录点安全` next to the current behavior/reason. The header row now also shows `延迟` and `丢包` next to `BOT`/`WS`/`日志`, using the runtime network quality estimates described above. Tampermonkey panel version is `0.4.57`; extension panel version is `0.1.36`.
- Manually pausing the BOT stops motion and clears the target line overlay; overlay rendering also suppresses any paused decision so stale target guidance does not remain on the page while paused.
- Stamina-aware ROI is used for opportunities:
  - movement cost is modeled at about `1ms` stamina per `1cm`;
  - accepted shot cost is modeled at `500ms`;
  - estimated damage is about `3HP` per accepted shot;
  - non-survival actions are skipped when estimated cost cannot fit the 1h/1d stamina budget.
  - if the 1h budget cannot afford the nearest safe coin candidate, the bot exits immediately with `stamina-budget-coin-leave` instead of waiting in game; this exit waits a fixed 30 minutes before relogin.
  - AFK Drop targets enter opportunity scoring from Drop `3`; active/profit combat still uses the higher attack gates.
  - out-of-range AFK Drop targets are skipped for 60s after the bot observes their 5s stamina decreasing. In-range AFK targets keep the old immediate attack behavior, and newly visible AFK-like targets without observed stamina consumption can still be approached immediately.
  - opportunity stick bonus is disabled and switch hysteresis is small, so a clearly better nearby ROI target can replace an older far target promptly.
  - similar same-tier opportunities are chosen immediately; once a target is selected, switch hold/margins keep the bot from jumping to another similar-ROI target unless it is clearly better.
  - held coin opportunities are matched by id or nearby coordinates and can survive short candidate-source gaps for the remaining hold window, so a temporarily missing current coin does not cause a same-tier 1-coin target flip.
  - held visible/native coin opportunities are released immediately when current native/render coin sources are available and no longer contain that target inside the visible-authority radius. This prevents continuing toward a stale coin coordinate after another player picks it up, while retaining the short missing-hold only for real source gaps.
- Coin routing:
  - nearby and foot coins are prioritized before broader opportunities;
  - post-attack dropped coins are prioritized when appropriate;
  - after an actual `attack`/`opportunistic-shot` against a high-Drop AFK target resolves before the dropped coin is visible, the bot moves toward that target's last coordinates for 1s from the resolution moment, without selecting a coin/player target, then resumes normal profit selection;
  - realtime/native visible coins always win before any snapshot fallback;
  - visible/native AFK Drop targets also win before snapshot fallback, so ordinary profit flow uses visible收益 first and only touches snapshot targets when no visible/realtime profit exists;
  - native/realtime visible coin groups can be planned as a bounded short route in the same opportunity pool as single coins and AFK/player Drop targets. The route is scored by total visible route reward over total estimated movement stamina, starts at the first coin to pick up, records route ids/value/stamina/leg count plus per-coin route points in the action, and only enters the pool when at least three safe visible coins form a beneficial route;
  - visible coin routes are rejected when their first coin would clearly skip a much closer safe local coin: within `22000cm`, a non-route first coin blocks the route if the route's first leg exceeds both `1.45x` that coin's distance and a `6000cm` extra-distance slack;
  - selected visible coin routes keep their current first coin through near-tie replans when the rebuilt route still overlaps at least two route coins and the competing first-coin route is not better by `3000` score or `10%`; clearly better route scores can still switch first coin;
  - coin routes are safety-checked leg by leg. Sampled route points inside active/invulnerable coin danger are rejected, so route planning cannot walk through a hostile danger band just because the final coin is safe;
  - when a selected action uses `best-opportunity-coin-route`, the target overlay connects self to each route coin in order with the existing coin guidance line so live review can see the full chosen route instead of only the first pickup target. If the same first coin is also the best single-coin candidate, route metadata is still preserved for display so the overlay does not collapse back to a one-coin target line while approaching that coin;
  - known coin-field migration is scored in the same opportunity pool as visible coin routing, so it shares ROI comparison and switch hysteresis instead of competing as a separate fallback branch.
  - far low-value single snapshot coins are gated by distance/value during normal selection, but after 60s of continuous snapshot-coin waiting the bot chases a safe, fresh far snapshot coin instead of idling, even when ordinary long-window stamina ROI budget would otherwise keep it waiting.
  - visible/native coin opportunities use visible coin reasons, not snapshot navigation reasons.
  - a visible/native coin worth at least `highValueCoinPriorityAmount = 10` can take an early priority path before combat, recovery, incoming-bullet pressure, ordinary Active coin-threat blocking, or invulnerable-player avoidance. When self HP is at least `highValueCoinPriorityHealthyHp = 50`, this path ignores active-threat coin safety filters and goes for the high-value coin; when HP is below that threshold, it only runs with no incoming bullet, no current combat target, and no nearby non-invulnerable real threat. Invulnerable players do not block the high-value shortcut. This shortcut is used when it must beat a blocking combat/recovery/avoidance condition or when a normal coin path would reject the high-value coin because of an ordinary Active coin threat; ordinary high-value coin selection still flows through the normal ROI/hysteresis opportunity pool.
  - ordinary visible/native coin safety uses active invulnerable threats, native/realtime visible invulnerable players, and non-whitelisted non-invulnerable realtime Active players with combat/activity evidence. This blocks ordinary low-value coin pickup near a visible Active player, including the 1-coin control-steal case, while snapshot-only players do not suppress otherwise visible profit and very close foot coins still keep the existing short pass-distance exception.
  - when no action is chosen because only non-coin targets exceed the 1h/1d stamina budget, the wait reason remains `wait-for-stamina-budget`; coin-budget waits are no longer allowed to reach the 60s snapshot fallback when the nearest safe coin is unaffordable by 1h stamina.
  - invulnerable active players use a wider coin danger radius and a heading/lane block, so the bot avoids walking toward them for coins even when the coin point itself is outside the old danger bubble.
- Coin movement:
  - far aligned coin movement uses dominant-axis approach to reduce snaking;
  - near target handoff/brake prevents overshooting;
  - close pickup uses shorter brake pulses and clears stale axis locks at exact tolerance.
  - pickup pulses now shorten by distance bands as the bot nears the coin coordinate, and repeated close pickup misses further reduce pulse length before giving up on the coin.
- Combat:
  - defensive combat beats normal coin logic when a real threat exists;
  - non-whitelisted, non-invulnerable Active enemies inside `combatAttackRange` remain defensive combat targets only when they are real threats or have worthwhile Drop. Active targets with `Drop <= combatLowValueActiveDropMax = 3` are ignored for proactive combat and profit combat unless they own an incoming bullet, an unknown incoming bullet is paired with their firing signal, or recent injury evidence makes them the likely shooter;
  - if a non-invulnerable Active threat is inside `combatAttackRange` but is temporarily unavailable as a combat target, such as during a retreat-ignore window, the bot waits with `combat-active-threat-wait` instead of taking foot/visible coins in that danger window. Low-Drop Active targets without real threat evidence do not create this wait state;
  - combat HP disadvantage checks run as soon as the Active target enters combat selection, before waiting for a bullet/injury trigger, but high-HP static HP-gap and trade-estimate disadvantages now enter a short observation window instead of leaving on the first detection;
  - close combat backs away while shooting inside the shorter 45m spacing threshold;
  - combat logging analysis showed 105-145m fights had very poor hit yield versus 30-75m, so default combat spacing now favors 45-65m instead of 75-105m;
  - when an active duel stays beyond 100m for at least 6s without target damage and self HP is not already meaningfully behind, the bot presses toward 75m first instead of treating the long-range exchange as a reason to retreat or spend extra dodge reserve on more shots; this far no-damage close-in branch stays disabled for ordinary retreating-edge targets, but reopens as `retreating-fighter-close` when that retreating target is still creating real target-owned bullet pressure and the HP gate remains favorable;
  - passive coin-runner combat is recognized by explicit combat intent (`defensive`, `engaged`, `profit`, or `reengage`), high self HP, no recent self damage, no real incoming bullet, no target firing, target movement, and a positive Drop/reward signal. This branch now also requires at least `combatPassiveRunnerConfirmMs = 2500` of sustained engagement before it activates, and it is disabled for the rest of that engagement once the target has been seen owning a real incoming bullet. For these targets the bot removes synthetic `target-pressure` dodge, prefers zero-spread live intercept aim against realtime/native visible coordinates, and uses a dedicated 45m close-in move when finish-pressure, retreating-edge, far-no-damage, and ordinary no-damage pressure are not already active;
  - passive coin-runner combat also uses a low no-threat shooting reserve (`combatShootPassiveRunnerDodgeReserveMs = 1800`) while the target is eligible, so high-HP safe runner fights can keep firing during close-in movement instead of repeatedly pausing under the ordinary dodge reserve;
  - new Active engagements use an opponent-probe shooting window before target-owned real-bullet evidence is seen. For the first `combatOpponentProbeMs = 6000`, combat preserves a high 5s reserve (`combatOpponentProbeReserveMs = 5600`) and limits fire to `combatOpponentProbeEveryMs = 520`, while movement/spacing/dodge action reasons remain visible. The probe does not override confirmed passive-runner, low-threat finish, or target-owned pressure windows;
  - if the same combat target has no HP damage for at least 8s and self HP is at least 60, the bot pressures closer toward 65m while preserving real-bullet dodge priority;
  - low HP plus long no-damage is not a standalone exit trigger; combat continues unless critical HP, low-HP disadvantage, close-pressure disadvantage, or confirmed high-HP/trade disadvantage thresholds fire;
  - new combat targets enter only inside `combatAttackRange`; an already engaged target remains a combat target until it exceeds `combatDisengageRange`, and while it is outside attack range the bot usually holds position/recovery instead of chasing with `combat-reengage`; the exception is a guarded slight overshoot window up to 150m where an engaged/reengage target can be stepped back into range without shooting if self HP is still healthy, the HP gap is not already bad, and the target is still applying real target-owned bullet pressure or has only just slipped out of range without a retreat signal. Target-owned bullet pressure gets a wider recoverable HP-gap band than ordinary fresh-contact reengage, so a still-fighting target that just crossed the 145m attack boundary can be pressed back into range before stop-loss is considered;
  - when a combat target is still inside range but receding at the outer edge (`combatRetreatEdgeRange` and beyond), the bot suppresses shooting with `target-retreating-edge` to avoid wasting stamina on extreme-range kiting;
  - if that retreating edge target is already low HP while self is still high HP, the bot uses `combat-finish-pressure`: it moves closer toward the target inside the 145m attack range and allows low-frequency finish shots; if a recently damaged low-HP target slips just outside attack range but remains within 160m, `combat-finish-reengage` moves back into range without shooting out of range, while ordinary healthy/outdated out-of-range targets still hold.
  - if a live combat target is already down to low HP, self HP stays high, the HP gap is not worse than even, the target remains within the guarded finishing range, and there is no real incoming bullet pressure, the bot can enter a `finish-low-threat` fire window that keeps burst fire pressure while using a reduced dodge reserve instead of the normal conservative reserve band;
  - when the current target is the owner of real incoming bullet pressure, self HP is at least 60, the HP gap is not already worse than 10, and distance is within 145m, combat can keep pressure fire active while still preserving the dodge reserve floor. If the same target-owned pressure continues with self HP at most 70, the target ahead by at least 10 HP, and no target damage for 10s, combat exits via `combat-hp-disadvantage-leave` as a later stop-loss after the reengage/pressure options have had room to work;
  - target-owned pressure now splits winning finish pressure from losing stop-loss: when the target is already low HP (`<=75`), self is still at least `60`, self leads by at least `5` HP, the fight is within 110m, and no target damage has landed for 6s, combat uses a lower 1800ms reserve to keep finishing shots active; when the target is still healthy (`>=75`), target-owned pressure continues for 10s without target damage, self HP is at most 80, and the target leads by at least 10 HP, combat exits earlier instead of waiting for the old 70HP stop-loss;
  - when pressure-close is active, diagonal tangent dodge keeps at least one target-closing axis so long no-damage fights do not keep drifting away at 100m+;
  - incoming bullet pressure takes precedence over spacing and can reselect a retreat-ignored shooter for defense, but it does not bypass disengage-range exits or retreating-edge fire suppression;
  - dodge-only defensive detection treats a real incoming bullet from a visible/native opponent as dangerous up to `combatAttackRange + combatDodgeRangeBuffer` (`155m` by default). This does not extend ordinary attack, profit-combat, aim, or fire range: targets outside `combatAttackRange` get `combat-out-of-range-dodge` movement with `shoot=false`;
  - tangent dodge uses signed bullet lane when available;
  - precise incoming signed-lane direction can override a stale opposite strafe lock; old logs showed conflicting locked strafe signs had worse self-damage/target-damage balance than aligned signs.
  - real incoming bullets are evaluated as a multi-bullet threat field over the 8 legal movement directions; the selected dodge remains integer `dx/dy` in `-1/0/1`, matching CDP validation that native `vel` does not accept continuous float movement vectors.
  - combat logs expose `incomingBullet.threatCount` and `combatState.strafe.threatField` so live reviews can compare the selected dodge direction, direct-hit count, CPA distance, and time-to-impact.
  - if an already engaged target exists but another in-range player is the owner of a real incoming bullet threat, the bot temporarily prioritizes that shooter; synthetic `firing` pressure alone does not steal target focus.
  - combat leave decisions now keep a combat cover action while the page has not yet confirmed exit: the bot continues dodge/spacing movement and planned shooting when self is still visible/alive instead of stopping in place under incoming fire.
  - moving-target aim spread includes measured evasion physics;
  - combat realtime entity resolution now merges render-visible entity sources (`getRenderEntities`, `state.getRenderEntities()`, `renderEntities`, and `render_entities`) with native state entities, preferring render/visual coordinates over stale state coordinates while still avoiding snapshot combat authority;
  - combat aim is behavior-driven: it can switch to live/native/render precision when selected-target and current visible coordinates diverge, when target movement is mostly radial, or when real incoming bullet pressure/server-position stall makes current visible position more reliable than stale state coordinates;
  - when a moving live target is mostly lateral, real incoming bullet pressure or server-position stall no longer forces a full precision fallback by itself; the bot can keep quadratic intercept against the current live/native/render coordinates with `strategy=live-intercept`, so it preserves visible-authority targeting without giving up lead on sideways movers;
  - combat aim and fire use live/native targets only; snapshot data is not consulted for combat target, aim, or fire decisions.
  - real-bullet live precision logs `aimTarget.realBulletPrecision` and `strategyReason=real-bullet-pressure`, while the lateral live-intercept branch logs `aimTarget.liveInterceptAim` with `strategyReason=real-bullet-pressure-intercept` or `server-stall-live-intercept`, so battle reviews can separate pure precision fallbacks from live-coordinate intercept.
  - moving-target combat aim now prefers a physical quadratic intercept solution using measured bullet speed (`500cm/tick`), render-delay compensation (`2` ticks), bullet range/max-flight guards, and a confidence-weighted small spread; the older angular jitter lead remains only as a fallback when no valid intercept solution is available;
  - the current combat target now keeps a short motion sample window. The bot derives a lightweight opponent profile from lateral direction flips, velocity stability, and receding/kiting behavior; this profile scales intercept confidence and is exposed in aim/combat logs.
  - low-confidence distant moving targets are no longer fired at full cadence: when intercept confidence is below `0.6` beyond 90m with meaningful target motion, combat fire uses a 520ms point-fire cadence while keeping ordinary close/high-confidence fire unchanged.
  - active combat tracks recent self/target HP samples over a 6s exchange window. If self is taking enough damage and the predicted time-to-death is worse than predicted time-to-kill by the configured safety factor, combat records a `tradeEstimate` disadvantage observation and exits via `combat-hp-disadvantage-leave` after confirmation; pure zero-damage windows now require either lower remaining HP or a nearer predicted death horizon before that trade-estimate exit can fire, so short high-HP latency windows do not force a premature leave by themselves.
  - high-HP static HP-gap and trade-estimate disadvantage exits require the same target to remain disadvantaged for `combatDisadvantageConfirmMs = 2500`, at least `combatDisadvantageMinEngageMs = 3500` of engagement, and at least `combatDisadvantageMinSamples = 4`; critical HP, low-HP disadvantage, close-pressure disadvantage, and server-stall no-damage exits remain immediate survival exits.
  - long no-damage aim widening is still capped at `0.14rad`, because old delayed-hit attribution showed higher buckets weakening sharply, but intercept aim uses that cap only to bound residual uncertainty/spread rather than to cap the physical lead itself;
  - combat shooting is stamina-aware: ordinary combat no longer force-bypasses `shootEveryMs`, normal combat cadence is 160ms, reserve-band fire is 360ms, opponent-probe fire is 520ms, and fire pauses below hard reserve or when 5s stamina should be saved for dodge. Guarded exceptions include low-risk `finish-low-threat` finishing pressure and target-owned real-bullet pressure fire; both keep explicit HP/range/risk gates instead of relaxing the broader reserve policy.
  - short-term target motion now scales aim jitter/lead down when a target is not meaningfully evading;
  - an already engaged target stays under combat action priority even while recovering/non-full HP until it exceeds `combatDisengageRange`; outside attack range but inside that disengage range the bot normally waits in place to recover instead of pursuing, except for the guarded slight out-of-range reengage window described above. Invulnerable targets are avoided as a global safety movement, not a recovery-only combat state, and non-invulnerable recovery threats can still use the safety/flee/recover path unless combat leave thresholds fire.
  - pursuit leave thresholds remain 5 minutes normally, but shorten to 90 seconds when not full HP, 60 seconds for invulnerable chasers, and 45 seconds when both apply.
  - optional Tampermonkey combat logging records only self-involved, non-AFK combat to local JSONL via `combat-log-service`; offline/relogin/no-self states pause logging and do not flush every tick.
  - combat logs now persist passive-runner confirmation evidence in `combatState.passiveRunner`, including `engagedMs`, `confirmMs`, `confirmed`, `originIntent`, and `seenTargetRealBulletMs`, plus top-level combat-state fields `combatOriginIntent`, `combatOriginReason`, and `targetRealBulletPressure`, so reviews can tell whether a runner close-in branch came from a true profit chase or a later live-fighter engagement.
  - combat logging no longer starts from nearby third-party activity or AFK Drop attacks; AFK attack frames are also excluded from pre/post buffers.
  - matching combat log sessions still include about 10s pre-combat, every combat tick, and about 10s post-combat.
  - safety movement such as `avoid-invulnerable-target` does not immediately close an important combat summary; old `recovery-avoid-humans` summaries are filtered from daily active-combat statistics.
  - important combat summaries treat post-combat recovery waits as an observation period, not a combat result; if the target does not return before the post buffer expires, the daily report folds that target loss into `敌方逃离`.
- combat log frames and session start/end events include final decision/exit reason, a top-level `exit` summary with relogin hold, pending suppress, minimum suppress, and HP-delay fields, login/suppress/hold context, self/target HP, current combat target mode/life/active/firing/invulnerable evidence, aim strategy fields for live/native/render precision, injury/pursuit context, capped nearby entities, capped bullets, control state, snapshot ages, `runtime` tick/frame-gap diagnostics, and `combatMetrics` frame deltas for HP, distance, movement, shot cadence/result, target damage timing, bullet threat counts, nearest incoming bullet timing, and server-position stall state.
- combat log entity merging preserves realtime/native/render coordinates when later snapshot entries describe the same player; snapshot data may still fill missing HP/drop metadata but no longer overwrites the coordinates used for combat review.
- The local combat-log collector writes split JSONL files by default under `logs/YYYY-MM-DD/combat/`, `important/`, `audit/`, or `misc/`; analysis and daily-summary tools recurse through those day subdirectories. Use `node server.js --flat-files` only when the legacy `logs/YYYY-MM-DD/<combatId>.jsonl` layout is needed.
- Targeting:
  - remote whitelisted target usernames must not be attacked. The whitelist is loaded from `dist/target-whitelist.json` by default, is polled every 10 seconds, matches only exact trimmed usernames, and ignores user IDs. A failed poll keeps the last successfully loaded whitelist; before the first successful load, the effective whitelist is empty;
  - invulnerable targets are not valid opportunity/combat targets;
  - AFK Drop targets compete by stamina ROI, not raw drop alone.
